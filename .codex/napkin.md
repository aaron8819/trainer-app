# Napkin

## Corrections
| Date | Source | What Went Wrong | What To Do Instead |
|------|--------|-----------------|--------------------|
| 2026-06-14 | self | While probing JSON exercise metadata, escaped double quotes inside a single-quoted PowerShell `rg -F` pattern and PowerShell split the intended literal into path-like words. | For quote-heavy JSON probes on Windows, search a simpler stable token first and pipe to `Select-String`, or use an unescaped single-quoted literal exactly as PowerShell should pass it. |
| 2026-06-14 | self | In a runtime swap service test, asserted an omitted optional DTO field with `toMatchObject({ caution: undefined })`, which fails because the property is absent rather than present with `undefined`. | For optional response fields that should be omitted, assert the stable object fields first, then use `expect(value.optionalField).toBeUndefined()`. |
| 2026-06-14 | self | Typed-search-only swap caution candidates need the original search query during preview/commit; otherwise the server falls back to the strict default replacement universe and blocks intentional caution matches. | For runtime swap search tiers, keep the UI display-only but pass the active `q` through preview and commit so `runtime-exercise-swap-service` can re-run bounded typed-search eligibility server-side. |
| 2026-06-14 | self | While verifying a swap-menu slice, the log page test crashed because its Prisma mock lacked `preSessionReadinessSnapshot.findFirst`, which the current page loads through execution guidance before mounting the editor. | When running `src/app/log/[id]/page.test.tsx`, keep the Prisma mock aligned with page-level read-model dependencies and default readiness snapshots to `null` unless the test is about readiness guidance. |
| 2026-06-14 | self | While inspecting post-session review snippets, passed multiple range arrays to `Select-Object -Index` as separate arguments and hit a PowerShell conversion error. | For non-contiguous PowerShell line reads, run separate commands or build one parenthesized array expression like `-Index ((35..115) + (380..420))`. |
| 2026-06-14 | self | During a read-only Week 2 retrospective probe, selected non-existent `PreSessionReadinessSnapshot.source`, `targetSlotId`, and `targetWeekInMeso` fields and Prisma rejected the query after expensive DTO setup. | Check the Prisma validation error's offered field list or `schema.prisma` before selecting readiness snapshot fields; current names are `mesocycleState`, `weekInMeso`, `sessionInWeek`, `slotId`, `slotIntent`, hashes, and invalidation fields. |
| 2026-06-14 | self | While wiring weekly retro calibration into `mesocycle-review.ts`, first patched in a `toNullableIsoString` helper as if it already existed there because it existed in the sibling post-session producer. | Before moving helper-shaped code between app-owned read models, re-read the exact local helper area and add the local helper explicitly instead of assuming sibling-file utilities are present. |
| 2026-06-14 | self | While adding post-session performed-reality rows, I initially keyed calibration rows by `exerciseId`, which can collapse duplicate same-exercise rows inside one workout. | For workout review read models, preserve `workoutExerciseId` identity or pair derived rows by the same source-order traversal; do not assume `exerciseId` is unique within a workout. |
| 2026-06-14 | self | Runtime-added post-session performedReality rows had SetLog actuals in producer evidence, but the builder summarized only planned-set slices, causing session-local rows to display `0/0` / missing actuals. | For post-session review evidence, keep planned completion counts separate from calibration/performedReality evidence sets; runtime-added exercise rows should summarize their own sets while preserving session-local/non-consumption flags. |
| 2026-06-14 | self | While changing log execution guidance from a flat name map to an indexed identity/name map, I updated the builder empty case but missed the loader's non-current-workout empty return; `tsc --noEmit` caught the stale `{}`. | When changing DTO container shapes, search every `return {}` / empty fallback in builders and loaders before running tests. |
| 2026-06-14 | self | While adding log execution guidance UI coverage, I first asserted `logSetRequest` with `workoutId` plus `setId`; the existing client API helper actually sends `workoutSetId` and no workout id. | In `LogWorkoutClient.test.tsx`, check `components/log-workout/api` request helpers before asserting mocked payload shapes; keep UI-display tests aligned with existing helper contracts. |
| 2026-06-13 | self | In the direct `v2-debug-artifacts.test.ts` unit, I first asserted through `materializationShard.artifact.data` even though the local variable already held the shard artifact; the serializer test uses a wrapper shape, but the direct builder test unwraps it. | When adding V2 debug-shard assertions, check whether the test variable is the build-result wrapper or `?.artifact` before writing `data` assertions. |
| 2026-06-13 | self | While moving candidate-quality materialized-plan object construction behind a reusable helper, the single-exercise wrapper still passed `laneIds` to a helper input that expects `laneId`; focused tests passed but `tsc --noEmit` caught it. | After introducing a fixture-builder wrapper, run `tsc --noEmit` before broad verification and check wrapper input field names against the new helper type instead of the returned materialized-plan shape. |
| 2026-06-13 | self | The candidate-quality lab materializer-delta slice initially put materialized-plan fixture literals directly in `candidate-quality-lab-fixtures.ts`; the V2 architecture-boundary test rejected the diagnostic key `blockers` inside that pure lab file. | Keep materialized-plan shape literals behind `materialization/*` helpers, and let the lab consume compare-helper outputs instead of owning materializer internals. |
| 2026-06-13 | self | During the candidate-quality lab materializer-delta slice, I repeated the PowerShell `&&` chaining mistake while reading `materialization/types.ts`, causing a parser error before the useful file read. | Keep repo inspection commands separate or use `multi_tool_use.parallel`; do not use Bash-style `&&` in this PowerShell workspace. |
| 2026-06-13 | self | During the low-axial post-accept seed review, the phrase "support intent" could be mistaken for a new accepted-seed/runtime role even though `lower_b:hinge_anchor` still serializes the selected low-axial exercise under the lane's existing seed role (`CORE_COMPOUND`) with `exerciseId` and `setCount`. | Keep low-axial intent as planner/materializer/audit explanation unless a separate explicit behavior slice proves a user-facing need; runtime replay, receipts, and review/history should continue to derive from concrete exercise identity plus existing seed role. |
| 2026-06-13 | self | During the low-axial support-coverage production slice, the first back-extension disallow text check treated `hyperextension` as a true back-extension alias and would also have excluded valid `Reverse Hyperextension` low-axial candidates. | In lane-intent materializer disallow checks, carve out allowed family phrases before applying broad aliases; keep reverse-hyper fixtures in the bounded low-axial tests. |
| 2026-06-13 | self | While updating low-axial materializer tests, briefly changed an old diagnostic fallback expectation instead of the new production-consumed low-axial intent expectation. | After patching adjacent low-axial fixtures, re-read the exact `it(...)` name and rerun the focused materializer/lane-intent suite before broad verification. |
| 2026-06-13 | self | Low-axial support coverage is now a production-consumed `laneSelectionIntent v0` slice for `lower_b:hinge_anchor`, but only through pure V2 lane intent, exercise selection, and materializer filtering/ranking. | Preserve the boundary: audit can compare legacy/no-intent baseline vs current consumed intent, but seed shape, runtime replay, receipts, repair, DB writes, UI, and acceptance thresholds must remain non-consumers. |
| 2026-06-13 | self | During Reverse Hyperextension taxonomy cleanup, first looked for `base-plan-validation.test.ts` under `src/lib/engine/planning/v2/materialization/` even though it lives one level up, then repeated the stdin `tsx` named-export trap when probing taxonomy through static imports. | For V2 base-plan validation, use `src/lib/engine/planning/v2/base-plan-validation.test.ts`; for stdin `tsx` probes, use dynamic imports with `default ?? module` fallback before reading exports. |
| 2026-06-13 | self | While adding the low-axial acceptance projection, used `lowerBPosteriorChainSetDelta` in the returned DTO while the computed variable was named `lowerBPosteriorSetDelta`, causing every benchmark test to fail at runtime. | After adding a new audit DTO field, run the focused owner test immediately and keep computed variable names aligned with output field names before broad verification. |
| 2026-06-13 | self | While adding the lane-intent benchmark, first imported `lane-selection-intent-audit` from a pure V2 benchmark helper and re-exported the audit helper through the V2 index; the architecture-boundary test correctly flagged the `audit` import path even though it was type-only/local. | Keep pure V2 benchmark helpers structural or policy-owned; do not import any `*-audit` module from pure V2 files or the pure V2 index. Let audit/readout modules depend on pure helpers, not the other way around. |
| 2026-06-13 | self | During the lower-b Hamstrings preselection projection slice, repeated the Windows wildcard-path mistake by passing `trainer-app\src\lib\engine\planning\v2\*.ts` directly to `rg`, then had to rerun with directory plus `-g` filters. | On Windows, search directories with `rg ... path -g "*.ts" -g "*.test.ts"`; never pass wildcard path segments directly. |
| 2026-06-13 | self | The lower-b Hamstrings clean-preselection inventory looked like fresh missing evidence, but the bounded materializer projection measured `Seated Leg Curl:3 -> Seated Leg Curl:3` with zero identity/set/lane/blocker deltas. | Treat clean inventory as a hypothesis only; after a zero-delta owner-specific materializer projection, classify it measured no-impact and pivot instead of promoting class/selection behavior. |
| 2026-06-13 | self | The Biceps protect-floor row looked like the highest-ROI fresh Strategy B candidate, but the owner-specific materializer projection measured `Barbell Curl:2 -> Barbell Curl:2` with zero identity/set/lane/blocker/protected-coverage/concentration deltas. | Treat `fresh_strategy_slotdemandallocationbyweek_biceps_protect_floor` as measured no-impact until upstream evidence changes; after a zero-delta projection, pivot instead of chasing the sibling Biceps cap row by default. |
| 2026-06-13 | self | The ROI cutoff review found the remaining fresh/unmeasured V2 evaluator rows are mostly performed-reality protect-floor or cap/redistribution inventory with weak owner proof, while all recent bounded materializer projections are no-impact, stale/noise, or already-promoted baseline. | Do not measure another row from this inventory unless it has a clearly bounded owner-specific expected materializer delta; pivot to exercise ontology / lane-intent / candidate-quality benchmark infrastructure. |
| 2026-06-13 | self | During Biceps projection verification, a stale/pass Calves lane still carried a cap-aware expansion diagnostic and inflated `capAwareExpansionLimitationCount` even though the lane was already satisfied. | For compact V2 gap counts, exclude satisfied/pass stale readout rows from live policy-pressure buckets; preserve raw diagnostics, but do not let stale explanatory markers rank as current owner gaps. |
| 2026-06-13 | self | During fresh V2 candidate inventory inspection, repeated the known PowerShell `&&` mistake while combining `rg` and `Get-Content`, causing a parser error before reading the type. | Keep PowerShell reads separate or use `multi_tool_use.parallel`; do not chain commands with Bash separators in this repo shell. |
| 2026-06-13 | self | While adding the V2 promotion-candidate evaluator tests, first typed broad artifact fixture builders as full `repairPromotionScoreboard` / `v2PlanQualityBenchmark` contracts, causing `tsc --noEmit` to fail on unrelated schema fields the evaluator does not read. | For focused audit readout unit tests, type only the local helper inputs and cast the final fixture slice to the artifact type; keep full-contract coverage in serializer/CLI/debug integration tests. |
| 2026-06-13 | self | During the Side Delts protect-floor diagnostic fix, repeated the Windows `rg` wildcard-path mistake (`workout-audit\\*.test.ts`) before switching to directory search with `-g` filters. | On Windows, search directories with `rg ... path -g "*.test.ts" -g "*.ts"`; do not pass wildcard path segments directly. |
| 2026-06-13 | self | During Side Delts protect-floor verification, ran optional pure V2 class/capacity/selection tests even though no pure V2 code was touched; they failed on existing stale fixture expectations for upper_b Chest support in vertical_press, Calves Week 4 3-set budget, and upper_b targetSessionSets 21. | For diagnostic-output-only changes, keep pure V2 behavior tests out of the required stack unless pure V2 files changed; if run for context, label current fixture drift as residual risk instead of broadening into unrelated cleanup. |
| 2026-06-13 | self | During Side Delts protect-floor cause tracing, a direct stdin `tsx` import of the audit dry-run module failed on the repo's top-level Prisma `DATABASE_URL` guard even though the helper path being probed was pure. | For read-only probes, prefer pure V2 builder imports when possible; if an audit module import is unavoidable and no DB call is made, set a dummy `DATABASE_URL` only to satisfy import-time validation. |
| 2026-06-13 | self | During the bounded Calves implementation docs scan, used a double-quoted PowerShell `rg` pattern containing Markdown backticks and hit an unterminated string parser error. | Use single-quoted `rg` patterns for Markdown/backtick-heavy searches, especially when scanning docs for inline code paths. |
| 2026-06-13 | self | During V2 duplicate/lane warning cleanup verification, direct static stdin `tsx` imports from the V2 barrel reported missing exports or undefined named functions even though Vitest resolves them. | For stdin `tsx` probes against local Trainer barrels, prefer `const raw = await import("./path/index.ts"); const mod = raw.default ?? raw;` before reading named exports. |
| 2026-06-13 | self | During duplicate/class-family and lane-preservation readout cleanup, the standalone V2 base-plan validation fixture had drifted again from 64 clean sets to 63 sets with one flat-allocation quality warning. | Treat the standalone base-plan fixture as current materializer-shape evidence, not a north-star behavior assertion; when no behavior is being promoted, update fixture/docs expectations only after focused benchmark/CLI/serializer/debug tests prove the readout seam still passes. |
| 2026-06-13 | self | During post-promotion Calves readout cleanup, the top-level concentration materializer trial still looked like a no-impact set-cap trial even though the nested slot-week donor proof showed the promoted 3/5 shape was already baseline. | For post-promotion V2 readouts, classify the exact bounded proof as idempotent baseline evidence in benchmark/scoreboard/debug output, and inspect nested slot-week donor rows before treating a top-level no-op as no-impact pressure. |
| 2026-06-13 | self | While adding the V2 strategy-to-demand candidate inventory, first named a pure V2 field `repairedProjectionUsedAs` and used a `debug_readout` limitation, tripping the architecture-boundary guard that bans diagnostic/readout tokens in pure planner modules. | In pure V2 policy/diagnostic modules, keep field names neutral to planner concepts (for example `repairProjectionEvidenceUse`) and leave artifact/readout terminology to audit serializers/debug shards. |
| 2026-06-13 | self | During support-floor/readout cleanup, the live support-floor inventory still showed `direct_floor_below` and `blocked_by_missing_evidence` for an `audit_readout_cleanup` row even though the selected materializer proof was zero-impact, which could push the next queue toward stale policy pressure. | Keep raw evidence labels intact, but add a separate compact readout classification bucket (`measured_no_impact`, `stale_noise`, true owner gap, blocker, etc.) so ranked V2 work queues use measured owner-specific signal rather than raw repair-shaped labels. |
| 2026-06-13 | self | While editing the support-floor cleanup, accidentally invoked the web tool with no useful query. | Stay on local shell/apply_patch tools for repo-only work unless the user explicitly asks for web verification or current external facts. |
| 2026-06-13 | self | During the Calves bounded readiness review, repeated the known Windows `rg` wildcard-path mistake by passing `trainer-app\...\*.test.ts` directly and got an invalid-path error before the useful search. | Search directories with `-g "*.test.ts"` / `-g "*.test.tsx"` filters on Windows; do not pass wildcard file paths directly to `rg`. |
| 2026-06-13 | self | The standalone V2 base-plan validation fixture had drifted from the current clean materialized base shape, expecting 66 sets, 21 exercises, and optional triceps materialization after the current materializer settled at 64 sets, 20 exercises, and no optional triceps lane. | Keep base-plan validation tests anchored to the current materialized fixture shape; when behavior is not being changed, reconcile numeric/test-shape expectations only after proving nearby benchmark/readiness tests still pass. |
| 2026-06-13 | self | While adding V2 slot/week watch classification, the first test expectation counted only explicit Week 1 quality/pass watches and missed that the gate-level `week_1_trainability` warning is also an intentional watch item; the first blocker classification also collapsed a specific hard-blocker code into the generic gate blocker. | For acceptance-projection triage, preserve both gate-level and specific watch/blocker rows. Count the same items the CLI reports, and keep specific `week_1_trainability:<code>` rows so must-fix-before-Week-1 evidence is not hidden by gate summaries. |
| 2026-06-12 | self | While upgrading the V2 concentration trial, the first test expectation treated a single positive concentration delta as a bounded-design candidate before reading representative Weeks 2-4 materializer rows; the cross-week rows showed materializer identity/set regressions instead. | For concentration trials, separate Week 1 trainability from Weeks 2-4 representative materializer evidence; let cross-week non-regression gates override the single-lane delta before recommending policy design. |
| 2026-06-12 | self | During donor-offset regression inspection, the live projection improved concentration warning count but still failed because the intended donor lane absorbed zero sets, dropping net weekly Calves volume and protected coverage across Weeks 2-4. | For donor-offset trials, inspect source/donor set deltas and protected coverage before interpreting warning deltas; no alternate donor is safe unless it measurably absorbs the set and preserves net volume through the materializer dry-run. |
| 2026-06-12 | self | During the first pure V2 slot/week allocation policy trial, moving source/donor demand inside `SlotDemandAllocationByWeek` did create Calves donor absorption in Weeks 2-3, but Week 4 over-absorbed the donor lane and caused net weekly volume/protected-coverage regression. | Treat donor absorption as necessary but insufficient: verify every representative week for net-zero weekly volume, protected coverage, materializer non-regression, and seed/runtime non-consumption before calling a slot/week allocation policy behavior-ready. |
| 2026-06-12 | self | During the donor-offset projection slice, repeated a known PowerShell mistake by chaining `git diff --stat && git diff --check`, causing a parser error before either diff check ran. | Keep PowerShell verification commands separate or run them through `multi_tool_use.parallel`; do not use Bash-style `&&` in this repo shell. |
| 2026-06-12 | self | While adding the slot/week acceptance projection, used a quote-heavy `rg` pattern in double quotes and a wildcard test-file path that PowerShell passed literally/invalidly before getting useful search results. | Use simple single-quoted `rg` patterns for punctuation-heavy searches, and search directories with `-g "*.test.ts"` instead of passing wildcard file paths on Windows. |
| 2026-06-12 | self | While widening V2 base-plan validation tests during the plan-quality benchmark slice, an older calf-duplicate fixture expected the materializer to reuse Standing Calf Raise even though current ranking selects a clean Seated Calf Raise variant when available. | For duplicate-policy validation, explicitly construct the duplicate materialized shape being evaluated instead of relying on current materializer ranking to produce a stale smell. |
| 2026-06-12 | self | During the V2 duplicate/concentration slice, first tied `cleanAlternativePolicy.requiredBeforeDuplicate` to all `required_if_clean_alternative_exists` class-distinctness lanes, which over-tightened non-optional direct lanes and blocked materialization. | For this seam, only promote `requiredBeforeDuplicate` from explicit optional-activation rules that require clean alternatives; leave general class-distinctness as variation preference unless a separate owner-specific policy slice proves it should block. |
| 2026-06-11 | self | While inspecting focused Trainer snippets in PowerShell, used `Select-Object -Index 220..270` without parentheses and hit a range parsing error. | Use `Select-Object -Index (220..270)` for PowerShell index ranges, or use `rg -n` plus a narrower file read when inspecting context. |
| 2026-06-11 | self | Ran a focused Vitest `-t "repair promotion scoreboard"` filter that matched no tests, producing a skipped-file result that could be mistaken for coverage. | Treat skipped focused-filter runs as no evidence; rerun with the exact `it(...)` name from `rg -n "it\\("` or run the full nearby test file. |
| 2026-06-12 | self | During a read-only owner lookup, tried a stdin `tsx` probe with bare `new PrismaClient()` and hit the repo's Prisma adapter/client guard before getting useful evidence. | For Trainer DB probes, prefer existing audit CLI owner flags first; if a direct read-only DB probe is unavoidable, use the repo's Prisma adapter/client pattern rather than bare PrismaClient. |
| 2026-06-11 | self | While enriching pre-session readiness calibration rows, a builder test fixture supplied generated exercises without `prescribedSets`, and the workout preview builder failed before reaching the new calibration assertions. | In pre-session readiness builder fixtures, include the minimal generated preview shape (`orderIndex`, `prescribedSetCount`, `prescribedSets: []`) whenever `sessionSnapshot.generated.exercises` is populated. |
| 2026-06-11 | self | While fixing runtime swaps for Close-Grip Lat Pulldown -> Chin-Up, the first pure eligibility fix made Chin-Up eligible but a catalog-shaped probe showed pullover-style `vertical_pull` metadata could still outrank true pull-up/chin-up class matches in the initial shortlist. | For swap-menu fixes, validate both eligibility and lane-aware ranking against catalog-shaped candidates; true taxonomy class matches should outrank broad movement-metadata fallbacks before calling the user-facing issue fixed. |
| 2026-06-03 | self | While adding a one-off DB repair script, the dry-run executed under `tsx` but `tsc --noEmit` caught that I had typed the root Prisma client as `Prisma.TransactionClient`, hiding `$transaction`, and had loosened a next-session helper input type too far. | For one-off Trainer repair scripts, type the root app client as `PrismaClient`, transaction callbacks as `Prisma.TransactionClient`, and run `npx tsc --noEmit` after the first successful dry-run because `tsx` can miss static contract drift. |
| 2026-06-03 | self | While adding runtime-added exact-exercise load calibration, first scoped calibration history to the current session intent even though the request only required same owner and exact same exercise; `tsc --noEmit` also exposed that deload/evidence load-source unions needed the new source value. | Keep runtime-added calibration exact-exercise keyed, not same-intent keyed, and after adding any `ApplyLoadsAudit.resolvedLoads.source` value update readouts plus evidence/audit source unions before trusting focused Vitest. |
| 2026-06-03 | self | During a read-only prescription calibration probe, assumed the Prisma `Goals` row used `primary` / `secondary`; the live model uses `primaryGoal` / `secondaryGoal`, causing the first replay script to fail before `applyLoadsWithAudit()`. | For live DB probes that call `mapGoals()`, select/pass `goals.primaryGoal` and `goals.secondaryGoal`; verify Prisma field names before wiring app mappers into stdin `tsx` snippets. |
| 2026-06-03 | self | During pre-push verification, `npm run build` failed with `EBUSY` on `.next/codex-dev-server.err.log` because an existing `npm run dev` / `next dev` process in `trainer-app` was still holding build output files open. | Before treating a Next build failure as code-related on Windows, inspect local `node.exe` command lines for `trainer-app` dev-server processes and stop only those repo-local processes, then rerun `npm run build`. |
| 2026-06-03 | self | While splitting Home pre-session readiness display sections, the first focused app/API tests passed but the audit-side re-export test still had stale expectations for mixed calibration/fatigue rows. | When changing an app-owned DTO that audit re-exports, run the app tests plus the matching `src/lib/audit/workout-audit/*` re-export/consumer test before broad verify. |
| 2026-06-02 | self | While adding the post-session review display DTO, a plain import/source guard would not have proven UI-safety by itself because raw contract strings can leak through mapped fields even with safe imports. | For future display DTOs over app-owned contracts, add both source-dependency guards and serialized-output leak tests for raw classifications, evidence codes, decision logs, mutation flags, and internal source prose. |
| 2026-06-02 | self | While adding the post-session review producer tests, the first receipt fixture omitted required `cycleContext` fields, so `readSessionSlotSnapshot()` correctly returned no slot even though the fixture had a `sessionSlot`. | When testing receipt-backed producers, build fixtures with the full parseable `CycleContextSnapshot` shape: `weekInMeso`, `weekInBlock`, `phase`, `blockType`, `isDeload`, and `source`, plus optional duration fields when relevant. |
| 2026-06-02 | self | While adding the first post-session review contract builder, runtime-added exercises were initially counted twice as extra sets: once because the whole exercise was runtime-added and once because every set on that exercise was treated as extra. | For post-session execution summaries, make runtime-added exercise and runtime-added set paths mutually exclusive: an added exercise contributes its performed sets once, while added-set counts apply only to planned exercises. |
| 2026-06-02 | self | A combined move/import/test-source-guard patch for the pre-session readiness builder failed because one test context label had drifted. | For ownership-move patches, split the file move, import rewires, and source-guard additions into separate hunks after inspecting fresh test context. |
| 2026-06-02 | self | While converting pre-session readiness calibration watches from strings to structured rows, `flatMap` inferred the missing-trace object literal as the only row shape and `tsc --noEmit` failed on alternate reason/action codes. | When `flatMap` returns structured union-like DTO rows, provide the generic row type or use an explicitly typed accumulator so TypeScript does not over-narrow to the first literal branch. |
| 2026-06-02 | self | While extracting app-owned pre-session readiness evidence DTOs, first made `weeklyRetro.volumeTargeting` optional even though the builder treats `weeklyRetro` presence as proof that volume-targeting rows exist; `tsc --noEmit` caught the mismatch. | When narrowing broad audit payloads into app evidence DTOs, type only the consumed fields but preserve required nested fields when the builder's runtime contract assumes presence. |
| 2026-06-01 | self | In logging-weekly-volume-guidance tests, scenario-specific `mockReturnValue({})` did not clear queued `mockReturnValueOnce(...)` projected contributions from the shared `beforeEach`, so no-remaining-work cases inherited stale projected sets. | When overriding a Vitest mock that has default `mockReturnValueOnce` queues, call `mockReset()` before setting the scenario-specific return values. |
| 2026-06-01 | self | The first structured pre-session readiness contract live smoke let `Iso-Lateral Front Lat Pulldown` satisfy the Side Delts isolation matcher because it searched for the broad token `lateral`, producing an unsafe optional add-on row. | For muscle-specific optional add-on matchers, use exercise-identity phrases such as `lateral raise` / `side raise` instead of broad substrings that appear in unrelated machine names; keep a live `pre-session-readiness --no-artifact --operator-debug` smoke after formatter tests. |
| 2026-06-02 | user | Older napkin/startup notes over-specified a manual first-tool Napkin ceremony even though Napkin is automatic in the current workflow. | Future prompts should start with `git status --short`, stop on unrelated dirty files, and rely on automatic Napkin activation rather than restating manual Napkin reads. |
| 2026-06-01 | self | During pre-push verification startup, carried forward an outdated manual Napkin-first workflow note. | Keep startup prompts focused on `git status --short` preflight and dirty-file handling; do not add manual Napkin-first instructions to templates. |
| 2026-06-01 | self | While adding weekly-retro post-session calibration rows, made the new nested `postSessionReview.calibrationRows` optional for legacy fixtures but the new test accessed it as always defined; `tsc --noEmit` caught the mismatch. | When extending optional audit artifact sections for backward-compatible fixtures, use `?? []` in tests/formatters unless the local test explicitly narrows the parent shape first. |
| 2026-06-01 | self | While adding an apply-loads exact-history regression, used `upper` in an `Exercise.splitTags` fixture even though that type only allows library tags such as `push`, `pull`, and `legs`; `tsc --noEmit` caught it. | Keep `sessionIntent` values separate from exercise `splitTags` in fixtures; use `pull`/`push`/`legs` tags and reserve `upper`/`lower` for session intent fields. |
| 2026-05-31 | self | While adding accepted-seed-draft successor recovery, the first CLI summary labeled compatible slot ids as changed slots instead of comparing current vs candidate slot contents. | For recovery summaries, derive `changedSlotIds` from actual per-slot seed diffs in the API payload, then format that field in CLI output. |
| 2026-05-31 | self | While adding V2 acceptedSeedDraft fail-closed guards, first guarded completed-source route/recovery helpers but missed the completed-source branch inside `acceptPreparedMesocycleHandoffWithProvenanceInTransaction()`; the focused handoff test caught the silent return. | For handoff retry guardrails, check every completed-source return path: route retry helper, repair helper, and prepared transaction branch. |
| 2026-05-31 | self | During live successor seed recovery verification, repeated the PowerShell-piped `tsx` module-wrapper mistake by statically importing named exports from `@/lib/db/prisma`, causing a syntax error before the read-only probe ran. | For every stdin `tsx` probe, use dynamic imports and unwrap `const mod = raw.default ?? raw` before accessing local module exports. |
| 2026-05-31 | self | While adding Strategy B capacity materializer projection, narrowed on `candidateChange` but still passed nullable `capacityDiagnostic` into the projection summarizer; `tsc --noEmit` caught it. | When a diagnostic field is optional, narrow the parent diagnostic and the nested change together before handing both to typed helper functions. |
| 2026-05-31 | self | Ran a read-only `tsx` live DB probe from the repo root while importing `./scripts/audit-cli-support`, which only resolves from `trainer-app/`. | Run Trainer `tsx` app probes with `workdir=trainer-app` when importing app-local scripts or use `./trainer-app/scripts/...` from the repo root. |
| 2026-05-31 | self | Repeated the stdin `tsx` module-wrapper mistake on `audit-cli-support`, destructuring `loadAuditEnv` before unwrapping the module default. | For every PowerShell-piped `tsx` probe, unwrap each local module with `const mod = raw.default ?? raw` before destructuring. |
| 2026-05-31 | self | In a Prisma live probe, wrote nested selects as `muscle: { name: true }` / `equipment: { type: true }`, causing a Prisma validation error. | Nested Prisma relations need `select`, e.g. `muscle: { select: { name: true } }` and `equipment: { select: { type: true } }`. |
| 2026-05-31 | self | While inspecting audit artifact serialization, used Bash-style `&&` in a PowerShell command and hit a parser error before reading the file. | Keep PowerShell repo reads as separate commands or use `multi_tool_use.parallel`; do not chain with shell-specific separators. |
| 2026-05-31 | self | While searching docs for mesocycle-explain text, put Markdown backticks inside a double-quoted PowerShell `rg` pattern and hit an unterminated string parser error. | Use single-quoted `rg` patterns whenever the search text contains Markdown backticks, quotes, pipes, or punctuation. |
| 2026-05-31 | self | After compaction in the Strategy B capacity design slice, followed an older manual Napkin-first habit that no longer matches the prompt workflow. | On compaction/resume, keep the durable prompt rule as `git status --short` plus stop-on-unrelated-dirty-files; Napkin activation is automatic. |
| 2026-05-31 | self | While adding Strategy B capacity lane taxonomy, initially classified any row whose broad limitations contained the word `duplicate` as `redundant_duplicate`, which over-labeled unrelated rows from generic duplicate-avoidance guidance. | For capacity taxonomy, classify duplicate rows only from explicit lane-local duplicate status, duplicate gap causes, or direct duplicate diagnostics; generic limitation text is context, not ownership evidence. |
| 2026-05-31 | self | After a context compaction in the Strategy B capacity slice, relied on a stale handoff warning that manually enforced Napkin-first startup. | Treat compaction as a fresh Trainer startup with `git status --short` preflight and unrelated-dirty-file stop; do not reintroduce manual Napkin-first template text. |
| 2026-05-31 | self | While aligning V2 capacity diagnostics to pure slot caps, initially expected post-fix capacity pressure to drop to 1 even though the fixture repeats the remaining Upper A row pressure across Weeks 1-4. | When diagnostics evaluate all accumulation weeks, assert removal of the specific false-positive lane and compute aggregate counts across all evaluated weeks, not just Week 1. |
| 2026-05-30 | self | While inspecting a mesocycle-explain V2 debug artifact, passed a wildcard filename directly to `rg` on Windows and then read `process.argv[1]` from a `node -` stdin probe, which is the literal `-`, not the first user argument. | For Windows artifact searches, search the real directory with `-g "<pattern>"`; for `node - <arg>` probes, read user arguments from `process.argv[2]` onward. |
| 2026-05-30 | self | After a context handoff in the Strategy B architecture track, treated Napkin as a manual prompt ceremony. | After every handoff or resumed Trainer task, begin the prompt/process with `git status --short` and stop on unrelated dirty files; Napkin remains automatic background context. |
| 2026-05-30 | self | In an isolated Trainer git worktree, focused Vitest first resolved Rollup from `C:\Users\aabloch\node_modules` and failed on the missing optional native package, then import-time Prisma guards failed because ignored `.env.local` was absent. | For isolated Trainer worktrees, either install dependencies or junction `trainer-app/node_modules` to the main worktree's `node_modules`; for mocked/import-only tests, set a dummy `DATABASE_URL` before Vitest/tsc, and point live read-only audits at the original `.env.local` with an absolute `--env-file`. |
| 2026-05-30 | self | The live `ops:refresh-next-seed-draft` script refreshed successfully but failed its nested audit step with empty stdout/stderr because Node `spawnSync("npm.cmd", ...)` returned `EINVAL` in this Windows execution environment. | For Windows operator scripts that spawn nested npm commands, invoke through `cmd.exe /c npm ...` and include `result.error` in failure messages so subprocess launch failures are visible. |
| 2026-05-30 | self | In Stage C `rear_delt_direct` materializer consumption, `row_only` disallow logic treated any `horizontal_pull` movement metadata as row-only collateral, so live-shaped Cable/Dumbbell Rear Delt Fly, Reverse Pec Deck, and Face Pull rows could be filtered even when taxonomy/directness were otherwise valid. | For laneSelectionIntent direct-lane disallow rules, distinguish broad movement metadata from class/name evidence: use real catalog-shaped fixtures with movement patterns plus stimulus/classes, and make row-only collateral checks reject row identities/classes without rejecting direct isolation variants that share `horizontal_pull` metadata. |
| 2026-05-30 | self | In Stage C laneSelectionIntent materializer consumption, the first rear-delt intent movement-pattern check allowed rear-delt/reverse-fly/face-pull text but accidentally rejected Reverse Pec Deck even though taxonomy classified it as valid rear-delt isolation. | When promoting intent consumption, test named expected variants from the prompt against both taxonomy and intent filters; keep movement-pattern text guards at least as broad as the existing validated taxonomy for that lane. |
| 2026-05-30 | self | While adding the V2 next-seed refresh operator script, the first teardown path would have imported the Prisma module even after an early origin-validation failure with no `DATABASE_URL`, masking the clearer wrong-origin/env failure. | For operator scripts that can fail before DB access, make Prisma teardown conditional on `DATABASE_URL`/prior DB import so preflight errors stay focused and do not create secondary failures. |
| 2026-05-30 | self | During Stage B laneSelectionIntent materializer work, repeated the stdin `tsx` default-wrapper import mistake on `buildV2PlannerMesocyclePolicy` while doing a read-only plan probe. | For every PowerShell-piped `tsx` local import, unwrap `const mod = raw.default ?? raw` before destructuring or calling exports; don't skip the known workaround even for quick inspection. |
| 2026-05-30 | self | During `laneSelectionIntent v0` Stage A, guessed `row_support` lived on `upper_a` and left a skeleton-only risk variable outside its scope; focused V2 tests caught both. | Use the actual generated `ExerciseSelectionPlan` lane/slot map before asserting high-risk coverage, and keep diagnostic risk derivation scoped separately for plan-backed and skeleton-only rows. |
| 2026-05-30 | self | During the V2 candidate optimization pass, repeated the stdin `tsx` default-wrapper import issue for the Prisma module and then referenced a `PlanSlot` test helper type before declaring it. | For ad hoc `tsx` probes, always unwrap `raw.default ?? raw`; when adding typed test helpers, declare both slot and lane aliases before using them and run `tsc` before treating focused Vitest as final. |
| 2026-05-30 | self | While probing V2 base-plan warnings through a PowerShell-piped `tsx` script, accidentally left a `nodeRepl.write` call from MCP habits and had to rerun the read-only probe. | Keep shell-executed `tsx` snippets plain Node/console code; reserve `nodeRepl.*` helpers only for the Node REPL MCP tool. |
| 2026-05-30 | self | In a live next-seed refresh validation, assumed a `SetLog` could be counted through `workoutSet.workoutId`; Prisma requires traversing `workoutSet.workoutExercise.workout`. | For log-count safety snapshots, inspect the actual relation chain first: `SetLog -> WorkoutSet -> WorkoutExercise -> Workout`. |
| 2026-05-30 | self | Repeated the PowerShell-piped `tsx` module-wrapper mistake while importing `v2-materialization-live-inventory` for a read-only inventory probe. | Use dynamic import plus `const mod = raw.default ?? raw` for local TS modules in stdin `tsx` probes, especially when they import path-aliased app modules. |
| 2026-05-30 | self | While adding a V2 materializer regression, one broad patch missed its context and the follow-up insertion briefly de-indented the previous test block even though Vitest still passed. | Patch large test files in smaller context-stable hunks, then inspect the diff before treating green tests as proof that formatting stayed clean. |
| 2026-05-30 | self | In V2 draft-refresh tests, asserted full seed JSON equality for an idempotent rebuild even though the refreshed serializer may add current inert `acceptedPlannerIntent` metadata, and an unknown-source fixture overwrote `sourceMesocycleId` through spread order. | For refresh idempotency, compare executable rows/source rather than full explanatory metadata; put fixture overrides after spreads when intentionally preserving source identity. |
| 2026-05-30 | self | A `refresh-next-seed-draft` route call returned `409 Mesocycle handoff draft is not refreshable` even though the persisted draft source was `v2_materialized_seed`; the current draft stayed stale and Lower B Calves remained 3 sets. | Before treating a live V2 draft refresh as successful, capture the route body and re-check `nextSeedDraftJson.acceptedSeedDraft.refreshedAt`, source, slot count, and key candidate rows. |
| 2026-05-29 | self | Repeated a PowerShell quoting mistake by putting a regex alternation with escaped double quotes inside a double-quoted `rg` command while checking updated audit fixtures. | Use single-quoted `rg` patterns in PowerShell whenever the pattern contains quotes, pipes, or grouping; split into simpler searches if needed. |
| 2026-05-29 | self | Added a live-shaped V2 seed fixture with `as const`, and `tsc --noEmit` caught that the readonly slots did not match the exported mutable seed DTO. | Type V2 accepted-seed fixtures as `MesocycleSlotPlanSeed` at declaration time instead of using `as const`; still run `tsc --noEmit` after focused Vitest. |
| 2026-05-29 | self | Added a new V2 handoff refresh caller that referenced a re-exported production-gate constant without importing it locally; Vitest caught the missing binding. | When a module both re-exports and consumes a helper constant, import it into the local scope as well as re-exporting it. |
| 2026-05-29 | self | A focused Vitest pass succeeded before `tsc --noEmit` exposed loose V2 fixture types (`intent: string`, incomplete base-plan validation shape, nullable audit summary). | For V2/seed fixture additions, run `tsc --noEmit` before assuming green Vitest means the seam is done; type fixtures with exported DTOs or cast narrowly at the fixture boundary. |
| 2026-05-29 | self | Asserted the accepted-seed compare source on a nonexistent `result.provenance` path instead of the actual `provenanceNoWriteBoundary` / `comparedPreparationAvailability` fields. | Before adding audit assertions, inspect the returned DTO type/file and assert the public payload fields actually consumed by downstream audit output. |
| 2026-05-28 | self | Tried to read the napkin skill from `.codex/skills/.system/napkin` even though this install exposes it under `.agents/skills/napkin`. | Use the skill path shown in the session skills list; the repo napkin still must be the first tool call, then skill reads can follow from their listed absolute paths. |
| 2026-05-29 | self | In a read-only Trainer DB probe, ordered `Workout` rows by nonexistent `createdAt` and had to rerun the query. | Check Prisma schema/select hints before adding timestamp ordering; `Workout` can be ordered by `scheduledDate` but does not expose `createdAt`. |
| 2026-05-28 | self | Exported a lifecycle helper for audit readout but initially forgot to add that export to the Vitest module mock, causing focused generation-runner tests to fail. | When importing a newly exported helper from a mocked seam, update the test mock in the same patch before rerunning focused tests. |
| 2026-05-27 | self | In the first weekly-retro target-semantics cleanup pass, removing broad under-target attention also dropped near-MAV caution from the payload status path. | When demoting above-MEV/below-target rows, re-check cap rows separately and preserve near/over-MAV caution before running focused tests. |
| 2026-05-27 | self | During V2 materializer capacity work, repeated two known probe mistakes: static named imports in PowerShell-piped `tsx` and a wildcard `rg` path under `planning/v2/*.test.ts`. | For stdin `tsx`, always dynamic-import and unwrap `raw.default ?? raw`; for Windows `rg`, search concrete directories with `-g "*.test.ts"`. |
| 2026-05-27 | self | While locating handoff projection symbols, repeated the Windows wildcard-path mistake with `trainer-app\src\lib\api\mesocycle-handoff*`. | Use `rg <pattern> trainer-app\src\lib\api -g "mesocycle-handoff*.ts"` instead of wildcard path arguments in PowerShell. |
| 2026-05-27 | self | Repeated the Windows recursive glob mistake by passing `trainer-app/src/lib/engine/planning/v2/**/*.ts` directly to `rg`, which produced an invalid path error. | Search the concrete directory and add `-g "*.ts"` instead of using shell-style recursive glob paths in PowerShell. |
| 2026-05-27 | self | The requested mesocycle-explain `-t "chest_second"` command initially matched no tests because nearby titles used prose/hyphenated "chest second" wording. | For lane-id diagnostic contracts, include the exact lane id in narrow test titles so required focused commands actually execute assertions. |
| 2026-05-27 | self | While adding narrow planning diagnostic readout fixtures, left an unquoted object key with a space (`Side Delts`) and the new Vitest file failed to transform. | Quote muscle-name record keys in dense test fixtures, especially when using display names as object keys. |
| 2026-05-27 | self | In the first weekly-retro readout fixture expectation, treated over-MAV rows as also appearing in the near-cap bucket even though the formatter intentionally keeps over-cap separate. | For target-semantics tests, keep below/within/near-cap/over-cap buckets mutually distinct unless the formatter explicitly documents overlap. |
| 2026-05-27 | self | A broad mesocycle-explain wording patch failed atomically because one large test-context hunk was stale. | For large audit fixture files, patch production strings first, then patch test expectations from a fresh `rg` list in smaller hunks. |
| 2026-05-27 | self | In the first next-mesocycle acceptance-gate refinement, prior optional top-up evidence still produced a candidate warning even when the candidate cleared the relevant MEV floors with margin, causing the focused test to fail. | Keep completed-block evidence separate from required fixes/watch items: only escalate prior evidence when the persisted candidate repeats the risk or leaves a razor-thin margin. |
| 2026-05-27 | self | While adding zone-aware runtime dose tests, the first stretch-miss fixture landed exactly at the near-MAV suppression buffer, so the expected status contradicted the new cap rule. | When testing stretch/preferred misses, set projected volume below the near-MAV buffer; reserve buffer-edge fixtures for explicit near-MAV suppression tests. |
| 2026-05-26 | self | Repeated the Trainer startup convention miss by batching `.Codex/napkin.md` with `git status` and skill reads, and used `Get-Content` without the exact `-Raw -LiteralPath` shape. | The first actual Trainer tool call must be a single unbatched `Get-Content -Raw -LiteralPath .Codex/napkin.md`; only after it returns should status, skills, docs, or parallel reads run. |
| 2026-05-25 | self | Read `.Codex/napkin.md` first, but repeated the known startup convention miss by omitting `-Raw`. | The first Trainer tool call must be exactly `Get-Content -Raw -LiteralPath .Codex/napkin.md`; do not simplify the command shape. |
| 2026-05-22 | self | Repeated the napkin startup mistake by reading the napkin skill first, then batching `.Codex/napkin.md` with a skill read during pre-push verification. | In Trainer, the first actual tool call must be exactly `Get-Content -Raw -LiteralPath .Codex/napkin.md`; no skill reads, no wrappers, no exceptions. |
| 2026-05-20 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status` and docs reads at the start of weekly-retro stdout work. | The first Trainer tool call must be exactly one unbatched `Get-Content -Raw .Codex/napkin.md`; run status/docs only after it returns. |
| 2026-05-21 | self | Read `.Codex/napkin.md` first, but used `Get-Content` without `-Raw`, repeating the startup convention miss in a smaller form. | The first Trainer tool call must be exactly `Get-Content -Raw -LiteralPath .Codex/napkin.md`; do not improvise the command shape. |
| 2026-05-21 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status` and skill reads at the start of weighted top-up readout work. | The first Trainer tool call must be exactly one unbatched command: `Get-Content -Raw -LiteralPath .Codex/napkin.md`; only then run status, skills, or docs. |
| 2026-05-22 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status` and skill reads at the start of pre-session readiness formatter work. | The first Trainer tool call must be exactly one unbatched command: `Get-Content -Raw -LiteralPath .Codex/napkin.md`; do not use the parallel wrapper for the session-start napkin read. |
| 2026-05-05 | self | Batched `git status` with the session-start napkin read instead of reading `.Codex/napkin.md` first. | Read the repo napkin as the first file/tool action in every Trainer session, then proceed with other inspection. |
| 2026-05-05 | self | Repeated the session-start napkin batching mistake while also running `git status` and reading skills. | Make the first tool call exclusively `Get-Content -Raw .Codex/napkin.md`; only then run status or skill reads. |
| 2026-05-06 | self | Repeated the napkin startup mistake again by reading skill files and running `git status` before `.Codex/napkin.md`. | In Trainer, the very first tool call must be only `Get-Content -Raw .Codex/napkin.md`; do not batch it with anything else, even required status checks. |
| 2026-05-06 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with skill and docs reads before release verification. | For Trainer, make the first tool call a single `Get-Content -Raw .Codex/napkin.md` call, no parallel wrapper and no other reads. |
| 2026-05-07 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with required skill reads for the timer correction. | In Trainer, no exceptions: first tool call is only `Get-Content -Raw .Codex/napkin.md`; read skills/status/docs after that. |
| 2026-05-10 | self | Repeated the napkin startup mistake by reading skill files in parallel before the repo napkin. | Start every Trainer session with exactly one tool call: `Get-Content -Raw .Codex/napkin.md`, then run required status/skill/doc reads. |
| 2026-05-10 | self | Repeated the napkin startup mistake again by reading skill files before `.Codex/napkin.md` while starting explainability coverage work. | Do not let mandatory-skill reads preempt napkin startup; first tool call must be only the repo napkin, then everything else. |
| 2026-05-10 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status` and skill reads at the start of Program page work. | Make the first Trainer tool call a single napkin read only; run required status checks immediately after. |
| 2026-05-10 | self | Repeated the napkin startup mistake again by reading skill files before the repo napkin, then batched the eventual napkin read with `git status`/docs. | The first tool call in Trainer must be exactly one command: `Get-Content -Raw .Codex/napkin.md`; no skill reads, no parallel wrapper, no required status check in the same call. |
| 2026-05-10 | self | Repeated the napkin startup mistake at the start of compact Projected Week Finish work by batching skill reads and `git status` before reading `.Codex/napkin.md`. | For Trainer, ignore every competing startup instinct: first tool call is only `Get-Content -Raw .Codex/napkin.md`, then run required safety/status/skill reads. |
| 2026-05-10 | self | Repeated the napkin startup mistake during pre-push verification by batching `.Codex/napkin.md` with skill and git-status reads. | The first tool call in Trainer must be a single unbatched `Get-Content -Raw .Codex/napkin.md`; required skills/status come only after. |
| 2026-05-12 | self | Repeated the napkin startup mistake by running `git status` and skill reads before reading `.Codex/napkin.md` for weekly-retro audit work. | In Trainer, the first tool call must be only `Get-Content -Raw .Codex/napkin.md`; required user safety checks happen immediately after, not before or batched. |
| 2026-05-12 | self | Repeated the napkin startup mistake during pre-push verification by reading skill files before `.Codex/napkin.md` and then batching the napkin read with status/docs. | In Trainer, the first actual tool call must be a single unbatched `Get-Content -Raw .Codex/napkin.md`; no skills, no status, no docs, no parallel wrapper. |
| 2026-05-14 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status` and skill reads for the rest timer default change. | Start Trainer sessions with only `Get-Content -Raw .Codex/napkin.md`; required safety checks come immediately after, never in the same parallel call. |
| 2026-05-14 | self | Repeated the napkin startup mistake by reading skill files before `.Codex/napkin.md` for Add Set focus work. | The first tool call in Trainer must be exactly one unbatched `Get-Content -Raw .Codex/napkin.md`; do not let mandatory skill reads jump the queue. |
| 2026-05-14 | self | Repeated the napkin startup mistake again during pre-push verification by reading skill files before `.Codex/napkin.md`. | For Trainer, first actual tool call must be only `Get-Content -Raw .Codex/napkin.md`; mandatory skill reads happen afterward, without exception. |
| 2026-05-14 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status` and skill reads during no-artifact audit work. | The first Trainer tool call must be a single unbatched napkin read, even when the user explicitly requests an early safety/status check. |
| 2026-05-15 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status` and skill reads at the start of swap-modal work. | In Trainer, first tool call must be exactly one unbatched `Get-Content -Raw .Codex/napkin.md`; do safety/status checks immediately afterward. |
| 2026-05-15 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with skill reads before rest-timer-default work. | First tool call must be only `Get-Content -Raw .Codex/napkin.md`; no parallel wrapper and no skill reads in that call. |
| 2026-05-15 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status`, skills, and docs while starting audit CLI help work. | Start Trainer sessions with exactly one unbatched napkin read, even when the user explicitly asks for an early `git status --short`. |
| 2026-05-17 | self | First Program Impact MEV-closure ranking treated any from-zero planned contribution and one-set floor touch as a final-opportunity closure, which made existing tests fail and could crowd out the intended chest/triceps story. | For readout-only MEV closure labels, require existing pre-session weekly stimulus plus a meaningful contribution threshold; focused UI tests should include neutral readiness and low-priority in-range rows. |
| 2026-05-17 | self | Initial pre-session readiness add-on ranking reused broad current-week dose diagnostics directly, so `--operator-debug` recommended future-slot and primary-muscle top-ups instead of conservative session-local add-ons. | Keep dose guidance broad when debugging, but rank session-local add-ons only from generated next-session, low-risk accessory rows; put primary/fatigue-dense additions in `Avoid` unless a future policy explicitly says otherwise. |
| 2026-05-26 | self | The first live `next-mesocycle-acceptance-gate` smoke let diagnostic-preview volume rows fail the volume gate even though no persisted handoff candidate existed. | In acceptance gates, candidate-dependent gates must stay `unknown` when candidate identity fails; preview evidence may be displayed but must not create candidate pass/fail outcomes. |
| 2026-05-17 | self | Repeated the napkin startup mistake during pre-push verification by batching `.Codex/napkin.md` with the napkin skill read. | First Trainer tool call must be only `Get-Content -Raw .Codex/napkin.md`; even mandatory skill reads happen afterward. |
| 2026-05-19 | self | Repeated the napkin startup mistake by batching `.Codex/napkin.md` with `git status` and skill reads before active-mesocycle duplicate-add polish work. | In Trainer, first tool call must be exactly one unbatched napkin read; run status and mandatory skill reads only after that returns. |
| 2026-05-10 | self | A focused explainability coverage change passed, but the optional golden-path increase consumer test failed on stale copy wording (`band` vs current `range`). | Keep scoped patches from absorbing unrelated snapshot/copy expectation fixes; report the failing consumer separately unless the user asks to update it. |
| 2026-04-28 | self | Used bash-style `node - <<'NODE'` heredoc in PowerShell and hit parser errors. | Use a PowerShell here-string piped to `node` for inline Node scripts. |
| 2026-04-28 | self | Used `&&` command chaining in PowerShell while checking git status/diff and hit a parser error. | Run separate PowerShell commands or use `;` only when sequential shell semantics are intended. |
| 2026-04-28 | self | Put double quotes inside a double-quoted PowerShell `rg` pattern and hit an `Unrecognized token` parser error. | Prefer single-quoted rg patterns in PowerShell when the pattern itself contains double quotes. |
| 2026-04-28 | self | Passed Windows wildcard paths like `trainer-app\src\lib\audit\workout-audit\*.ts` directly to `rg` and hit invalid path errors. | Use `rg <pattern> <directory> -g "*.ts"` instead of shell-style wildcard path arguments in PowerShell. |
| 2026-04-28 | self | Added a new typed planner-only artifact field and initially only fixed the focused mesocycle explain tests. | Update serializer, artifact serialization, and CLI fixtures together whenever `MesocycleExplainPlannerOnlyNoRepair` gains a required field. |
| 2026-04-28 | self | In a V2 selection diagnostic, initially allowed same-muscle fallback to assign a Week 1 exercise to a lane whose existing lane evidence had no selected exercise. | Preserve identities only from explicit lane evidence; use inventory evidence for alternatives and mark missing generic inventory as not evaluated. |
| 2026-04-28 | self | V2 `ExerciseSelectionPlanDiagnostic` can re-harden concentration artifacts already tier-downgraded by `v2TargetVsNoRepairDiff` / acceptance classification. | Reuse the tier-aware no-repair concentration classification for readout status; raw `exerciseConcentration` flags alone are too broad for blocker status. |
| 2026-04-28 | self | The V2 diagnostic also treated `squat_compound` / `squat_or_quad_support` as class mismatches against lower quad-support planned classes, inflating readout blockers and class mismatch counts. | Keep class aliases diagnostic-only and satisfy lower quad-support planned classes through the diagnostic matcher without touching selection or generation. |
| 2026-04-28 | self | Adding a new planner-only sidecar diagnostic requires updates in three places: in-memory type/builders, compact main-artifact summary, and explicit debug sidecar payload. | For future V2 diagnostic fields, test all three surfaces in the same focused loop before running the live audit. |
| 2026-04-29 | self | In the mesocycle-explain compare helper, reused a discriminated `CompareValue` result inline and expected TypeScript to preserve narrowing across repeated calls. | Use a small primitive helper such as `arrayCountValue()` when feeding optional derived numbers into compare extraction. |
| 2026-04-29 | self | Initially preferred the V2 sidecar for all no-repair compare metrics; real sidecars omit some compact main-artifact operator fields like replacement readiness. | Prefer the main artifact for compact/operator no-repair summary fields and use the sidecar only for sidecar-only V2 gate/diagnostic details. |
| 2026-04-29 | self | While searching for serializer fixtures, embedded escaped double quotes inside a PowerShell double-quoted `rg` command and the shell ate the regex. | Use single-quoted PowerShell patterns without backslash-escaped quotes, or simplify the search before adding regex alternation. |
| 2026-04-29 | self | Repeated the known PowerShell `&&` parser mistake while chaining a search and file read. | Keep commands separate in PowerShell during Trainer work, especially in quick test/inspection loops. |
| 2026-04-29 | self | A Vitest assertion array of support-policy labels inferred as `string[]`, causing `tsc --noEmit` to fail against a literal-union label type. | Type expected literal arrays when asserting against exported string-literal unions. |
| 2026-04-29 | self | Mixed CommonJS `require()` with top-level `await` in an inline Node query and hit Node's ambiguous module format error. | Wrap ad hoc `pg` probes in an async IIFE when using `require()` from PowerShell-piped Node scripts. |
| 2026-04-29 | self | In a V2 support-lane diagnostic, initially treated missing `matchedClass` as direct-floor evidence, so collateral pulls/presses could count as curls/isolation. | For support direct-floor diagnostics, require class match or a narrow direct-isolation name fallback; unknown class should not satisfy direct floors by default. |
| 2026-04-29 | self | Asserted current live V2 policy-gap counts inside a narrower unit fixture and failed the focused mesocycle-explain test. | In unit tests, assert the interpretation derives from that fixture's diagnostics; reserve exact live scoreboard counts for the live audit artifact assertion/readout. |
| 2026-04-29 | self | In a V2 diagnostic-only test, passed `equipment` as a string to a selection-v2 fixture and failed `tsc --noEmit`. | Selection-v2 exercise fixtures expect `equipment` as an array of equipment tags, e.g. `["bodyweight"]`. |
| 2026-04-29 | self | Added a synthetic planning-reality duplicate-continuity fixture without the exact diagnostic metadata/type literals and failed `tsc --noEmit`. | When overriding typed diagnostic subobjects in tests, preserve the real `version` / `source` / `readOnly` / `affectsScoringOrGeneration` fields and literal union values, or cast through `unknown` only when intentionally partial. |
| 2026-04-29 | self | Asserted global V2 policy-gap counts in a minimal no-repair fixture and got failures from unrelated target-skeleton lanes. | For V2 target-diff unit tests, assert the lane under test unless the fixture populates the full skeleton enough to make global scoreboard counts meaningful. |
| 2026-04-29 | self | New `v2SelectionCapacityPlanDiagnostic` initially trusted `v2ExerciseSelectionPlanDiagnostic.selectedIdentity` first and inherited its row-binding ambiguity for `upper_a/row_anchor`, reporting Cable Crossover instead of the row. | For capacity/headroom readouts, prefer lane-matched `v2TargetVsNoRepairDiff.currentEvidence.selectedExercises` for displayed identity/sets, then fall back to selection diagnostics; verify against `slotPlans` by lane aliases. |
| 2026-04-29 | self | Adding a required planner-only sidecar field passed focused serializer tests but `tsc --noEmit` caught a typed CLI fixture missing the new field. | After adding required `MesocycleExplainPlannerOnlyNoRepair` fields, run `tsc --noEmit` before broad verify and patch every typed CLI/serializer fixture. |
| 2026-04-29 | self | The requested Vitest command with `src/lib/engine/planning/v2/*.test.ts` reported only 3 files under PowerShell/Vitest, so it did not prove every V2 planning test ran. | For V2 planning verification on Windows, run the requested command and then an explicit file list for all `planning/v2` tests when exact coverage matters. |
| 2026-04-29 | self | While building the seed-safe V2 accepted planner DTO, the first shape reused a raw policy concentration value `diagnostic_only`, which is readout language rather than future seed metadata. | Map diagnostic/readout-flavored pure policy markers into seed-safe intent terms such as `optional_lane`, and test DTO strings as well as keys for forbidden readout leakage. |
| 2026-04-29 | self | The first slot-plan seed metadata sanitizer preserved only the obvious lane policy fields and dropped allowed DTO fields like duplicate class distinctness, clean-alternative requirements, and continuity variation. | When whitelisting a DTO for seed persistence, assert exact round-trip equality with the seed-safe DTO before asserting forbidden debug fields are stripped. |
| 2026-04-29 | self | Repeated the PowerShell quote parsing mistake on a broad `rg` import search with mixed double/single quotes. | Use single-quoted patterns or split the search into simpler commands before adding alternation that contains quotes. |
| 2026-04-29 | self | A dry-run V2 materialization output intentionally added `blockers`, which collided with the existing pure-policy boundary test's broad diagnostic key scan. | When adding dry-run planner artifacts with explicit blockers/omissions, narrow older readout-key boundary tests to the modules they were designed to police and add artifact-specific boundary tests. |
| 2026-04-29 | self | Added a vertical-press negative guard for the token `chin` and accidentally matched the substring inside `machine`, blocking Machine Shoulder Press. | Avoid bare short substring guards in taxonomy text matching; prefer phrase tokens like `chin up` / `chinup` when using `includes()`. |
| 2026-04-29 | self | Reused a double-quoted PowerShell `rg` pattern containing embedded quotes while tracing V2 materialization callsites and hit a parser error. | Use single-quoted `rg` patterns for import/callsite searches that contain quotes or regex alternation. |
| 2026-04-29 | self | A new V2 materialization report test would have been easy to place in a new `dry-run-report.test.ts`, but the requested verification command did not include that file. | When the user provides an exact focused test command, put new diagnostic coverage in an included nearby test file or update/run an explicit extra test command. |
| 2026-04-29 | self | Reintroduced the Windows wildcard-path `rg` mistake while trying to search `trainer-app\src\lib\api\*.test.ts`. | Use `rg <pattern> <directory> -g "*.test.ts"` for PowerShell searches; do not pass wildcard paths directly. |
| 2026-04-29 | self | Expected the full V2 dry-run harness to have `unsupportedClassCount=0`, but current policy intentionally surfaces optional unsupported class omissions while still producing a compatible required-lane preview. | For dry-run readiness tests, distinguish required blockers from optional unsupported omissions; assert compatibility/materializer status separately from the total unsupported-class count. |
| 2026-04-29 | self | Inline `tsx` could not named-import the new TS debug harness from stdin and exposed only `default` / `module.exports`. | For ad hoc `tsx` stdin probes against this repo's TS modules, import the module default and destructure exports when named ESM imports fail. |
| 2026-04-29 | self | Promotion-readiness tests initially supplied an isolated `exerciseSelectionPlan` and missed that the dry-run report treats missing full planner policy as a promotion blocker. | For V2 promotion contract fixtures, include planner-policy evidence explicitly when the scenario is meant to isolate write gates or seed-shape behavior. |
| 2026-04-29 | self | Tried `Get-Content trainer-app/src/app/api/mesocycles/[id]/...` and PowerShell treated `[id]` as a wildcard character class. | Use `Get-Content -LiteralPath` for Next.js App Router paths with bracketed route segments. |
| 2026-04-29 | self | In the V2 materialized seed acceptance helper test, first treated a passed `materializedPlan` as enough for guarded-write readiness and forgot the dry-run report still blocks missing planner policy and inventory availability. | For V2 materialized-seed eligibility fixtures, provide explicit planner policy, exercise selection plan, taxonomy, inventory, lane coverage, and production gates; a seed-shaped plan alone is not promotion-ready evidence. |
| 2026-04-30 | self | While adding the V2 acceptance probe, initially called `buildV2ExerciseMaterializationPlan()` directly from the API helper and tripped the architecture boundary test. | Keep dry-run materializer calls inside `src/lib/engine/planning/v2/materialization`; expose derived lane-coverage evidence from the dry-run report and let API probes summarize it. |
| 2026-04-30 | self | Reused a double-quoted PowerShell `rg` command containing escaped double quotes and hit the known terminator parser error. | Use a single-quoted PowerShell pattern for `rg` searches that include literal double quotes. |
| 2026-04-30 | self | Tried a complex quoted `rg` regex for `describe(\".*accept...` in PowerShell and hit parser/escape issues twice. | Use simple literal searches first, and keep regex/path arguments clearly separated with single-quoted patterns. |
| 2026-04-30 | self | Used a PowerShell double-quoted `Select-String -Pattern` containing Markdown backticks and broke string termination. | Use single-quoted patterns or avoid literal backticks in PowerShell search patterns. |
| 2026-04-30 | self | Repeated the Windows wildcard-path `rg` mistake while searching `trainer-app\src\lib\audit\workout-audit\*.test.ts` for sidecar tests. | Use `rg <pattern> <directory> -g "*.test.ts"` even for quick audit-test searches. |
| 2026-04-30 | self | Treated the mandatory Trainer pre-edit audit/plan as a final stopping point and made the user prompt me to continue. | After emitting required audit/plan, continue directly into implementation unless the user explicitly asked for plan-only. |
| 2026-04-30 | self | Repeated the PowerShell quote parsing mistake with a double-quoted `rg` pattern containing escaped quotes while tracing audit tests. | Use single-quoted `rg` patterns or simpler literal searches whenever the search text contains quotes. |
| 2026-04-30 | self | The first V2 strategy-input diagnostic change let the no-input path gain extra missing/limitation counts and assumed partial persisted handoff summaries had full lifecycle fields. | Preserve legacy no-input diagnostic counts, and make adapters tolerate partial read-model summaries by labeling missing fields instead of dereferencing them. |
| 2026-04-30 | self | Vitest passed the V2 historical-review loader tests, but `tsc --noEmit` failed because the test double did not satisfy the full Prisma delegate type. | Keep production adapter reader types strict; cast narrow test doubles at the callsite or define an explicit minimal test reader type before trusting focused Vitest. |
| 2026-04-30 | self | The first live historical source classification labeled one completed pre-V2 mesocycle `unknown` because it lacked parseable seed metadata. | For this migration, classify completed non-V2 historical mesocycles as `legacy_projection` while still using only performed/review evidence as strategy input, never old prescribed shape. |
| 2026-04-30 | self | Added new V2 strategy DTO evidence types in `types.ts` but initially forgot to re-export them through `src/lib/engine/planning/v2/index.ts`, so adapter imports failed `tsc --noEmit`. | Any new public V2 strategy type consumed outside the pure file needs the index export in the same patch. |
| 2026-04-30 | self | The first live block-response readout included low-priority secondary under-hit examples such as Abductors/Core, making the strategy evidence noisy. | Filter recurring under-hit block-response examples to A/B target-tier muscles; allow secondary/implicit muscles only as over-concentration or fatigue-driver evidence when relevant. |
| 2026-04-30 | self | While adding strategy hypothesis promotion readiness, used lowercase readout wording `blockers` inside a pure V2 strategy file and tripped the architecture-boundary diagnostic-key scan. | Keep pure V2 strategy vocabulary away from reserved audit/readout keys; use terms like `globalBlockers` only as typed output fields and avoid lowercase readout-key strings in pure policy files. |
| 2026-04-30 | self | In a promotion-diff serializer test, first asserted that all of `v2Summary` omitted a skipped-set evidence string, but compact recommendation evidence legitimately still includes that old summary field. | When proving new shard detail stays out of the main artifact, assert against the specific new compact summary object rather than broad sibling summaries that intentionally retain compact evidence. |
| 2026-04-30 | self | While adding the combined-hypothesis projection diff, typed `projectionMode` as the full union but only assigned estimate/not-projected values, so `tsc --noEmit` rejected a comparison to `shadow_projection`. | For first-slice estimate-only diagnostics, make the measured/projection capability explicit (`measured=false`) until a real shadow projection path exists. |
| 2026-04-30 | self | Initially placed the V2 planner guard skill under mandatory usage even though it is contextual, not universal. | For non-universal guard skills, keep trigger-rich skill metadata and add an `AGENTS.md` contextual/recommended reference instead of making it mandatory. |
| 2026-04-30 | self | The first live V2 strategy shadow audit discarded diagnostics from an error-shaped planner-only no-repair projection, so the artifact stayed in estimate mode even though planningReality existed. | For diagnostic-only no-repair/shadow projections, read diagnostics/planningReality from both success and failed projection returns; the error is a limitation, not a reason to drop measurable evidence. |
| 2026-04-30 | self | The first conflict-aware refinement used the broad evidence-derived donor list even when a measured shadow projection supplied a narrower candidate donor list, so the focused test retained Front Delts unexpectedly. | When a measured shadow projection exists, resolve donor/protected conflicts against `shadowProjection.candidateStrategy` first; use evidence-derived candidates only for estimate mode. |
| 2026-04-30 | self | A conflict-focused unit fixture changed strategy input evidence enough that the promotion diff no longer entered measured `shadow_projection` mode, so only estimate-mode overlap was tested. | For projection-diff conflict tests, keep a known-ready strategy input and vary the supplied shadow projection evidence when the test is about measured deltas. |
| 2026-05-01 | self | Added a new CLI operator-summary line for V2 pre-shadow filtering and initially updated code before the exact CLI expected-output fixture. | Whenever adding a compact audit summary line, update `workout-audit-cli.test.ts` exact arrays in the same patch before rerunning focused audit tests. |
| 2026-05-01 | self | While adding the slot-owned demand adjustment feasible test, briefly fed Hamstrings-only donor evidence while expecting Glutes safe-surplus eligibility. | In policy tests, make the strategy evidence source and the filter/floor evidence source name the same donor before asserting feasibility. |
| 2026-05-01 | self | The first over-concentration-only donor-margin test still inherited Glutes fatigue-driver evidence from the fixture, so the candidate was classified as both over-concentration and fatigue. | For diagnostic tests that isolate a candidate reason, clear both concentration and fatigue evidence before asserting that over-concentration alone cannot imply floor surplus. |
| 2026-05-01 | self | Used the MCP `nodeRepl` helper inside a plain PowerShell-piped `node` script and got `ReferenceError: nodeRepl is not defined`. | Keep plain Node artifact parsers free of MCP-only helpers; use `console.log` or run through the Node REPL tool when `nodeRepl.write` is needed. |
| 2026-05-01 | self | Repeated the Windows wildcard-path `rg` mistake with `trainer-app\src\lib\engine\planning\v2\*.test.ts`. | Use `rg <pattern> <directory> -g "*.test.ts"` or rely on `rg --files` filters in PowerShell. |
| 2026-05-01 | self | Repeated the wildcard-path mistake while searching generated audit artifacts with `artifacts\audits\2026-...*.json`. | Search the artifact directory and pass the filename pattern through `-g`, e.g. `rg <pattern> artifacts\audits -g "2026-...*.json"`. |
| 2026-05-01 | self | Forgot the known stdin `tsx` named-import quirk when probing the V2 planner policy and got a missing named export error. | For ad hoc `tsx` stdin probes, import the module default from the local TS file and destructure exports from that object. |
| 2026-05-01 | self | Repeated the stdin `tsx` module-wrapper mistake with dynamic import and called named exports directly from the wrapper. | After `await import("./src/.../index.ts")` from stdin, destructure from `mod.default` rather than `mod`. |
| 2026-05-01 | self | After reducing Lower B calf sets to make V2 base allocation role-sensitive, left a stale test expecting Calves in `coveredMuscles` even though they intentionally moved to below-preferred but above-floor. | When a support lane is intentionally floor-level, update both `belowPreferredMuscles` and `coveredMuscles` expectations together. |
| 2026-05-01 | self | Repeated the PowerShell Markdown-backtick parser mistake while searching docs for `SlotDemandAllocationByWeek`. | Avoid literal backticks in PowerShell search strings; search a simpler substring or use single-quoted patterns without Markdown formatting. |
| 2026-05-01 | self | Repeated the Windows wildcard-path `rg` mistake while searching V2 planning tests with `trainer-app\src\lib\engine\planning\v2\*.test.ts`. | Use `rg <pattern> <directory> -g "*.test.ts"` or `rg --files` filters; never pass wildcard paths directly in PowerShell. |
| 2026-05-01 | self | After adding a new required `V2SetDistributionIntent.guardrails` field and basis literals, focused V2 tests passed but `tsc --noEmit` caught stale audit typed fixtures. | When changing pure V2 policy object shapes, update audit serializer/CLI typed fixtures before trusting focused policy tests. |
| 2026-05-01 | self | While probing pure `v2SetDistributionIntent`, assumed week rows exposed `plannedTotalSets` and hit a runtime error. | Sum `week.slots[].targetSessionSets.preferred` for set-distribution week totals; the week row intentionally only carries week/phase/progression metadata plus slots. |
| 2026-05-01 | self | While adding selection-plan set-budget assertions, guessed lower hamstring max budgets from the target spec instead of the actual `V2SetDistributionIntent` rows and failed focused tests. | For V2 policy tests, assert exact budgets from the upstream owner being consumed; if unsure, inspect the built policy before writing expected ranges. |
| 2026-05-01 | self | Compared resolved `V2ExerciseClassId[]` against `V2ExerciseClassMatch.classId`, which is still typed as `string`, so `tsc --noEmit` failed after Vitest passed. | After changing pure V2 type surfaces, run `tsc --noEmit` before broad verify and keep local comparisons compatible with existing public diagnostic/string types. |
| 2026-05-01 | self | Repeated the `V2ExerciseClassMatch.classId` narrow-type mismatch inside the new base-plan validation diagnostic and also relied on `Boolean(row)` to narrow an optional row. | Normalize resolved class ids into `Set<string>` / `Map<string, number>` at diagnostic boundaries, and use explicit `row ? ... : false` narrowing before trusting focused Vitest. |
| 2026-05-01 | self | Repeated a complex PowerShell `rg` regex with bracket/backslash/quote mixing while searching V2 test expectations and hit an escape parse error. | Use separate simple literal `rg` searches in PowerShell instead of one broad alternation when the pattern contains brackets or quotes. |
| 2026-05-01 | self | In the first V2 base-plan compare test, expected only one repair dependency per fixture row and guessed max slot sets from Upper A, missing that repair-added rows intentionally count separately and Upper B is 17 sets. | For diagnostic-count tests, assert the actual classification model and inspect representative slot totals before writing exact count expectations. |
| 2026-05-01 | self | The V2 base-plan compare identity row classification widened to `string` under a ternary and only `tsc --noEmit` caught it after focused Vitest passed. | Annotate local classification variables with the exported literal union when returning typed diagnostic rows from map callbacks. |
| 2026-05-01 | self | In the first shadow-consumption trial patch, returned `shadowRemainingDependencyCount` while the computed variable was named `shadowRemainingRepairDependencyCount`, causing focused Vitest to fail at runtime. | After adding similarly named diagnostic summary fields, run the focused test immediately and prefer consistent full names through the return object. |
| 2026-05-02 | self | Repeated the stdin `tsx` module-wrapper mistake while importing `closePrismaResourcesForAuditCli` in a read-only Lower B swap preview probe. | For `tsx` stdin probes, normalize every local import with `const api = mod.default ?? mod` before destructuring named exports. |
| 2026-05-01 | self | Used the reserved `repairedProjection` wording inside pure V2 materialization diagnostics and tripped the architecture-boundary readout-key scan. | In pure V2 files, keep the existing neutral `repairedPlan` vocabulary; reserve repaired-projection wording for audit/operator surfaces outside the pure policy boundary. |
| 2026-05-01 | self | Serializer fixture Vitest passed but `tsc --noEmit` failed because the new shadow-consumption fixture omitted required nested `interpretationRules` and detailed `changes` sections. | For new typed audit DTOs, make serializer fixtures satisfy the full exported type before trusting artifact-focused tests. |
| 2026-05-01 | self | Assumed user-supplied audit artifact paths were repo-root relative and hit `ENOENT`; Trainer audit artifacts are under `trainer-app/artifacts/audits/` from the repo root. | Resolve audit artifact paths relative to `trainer-app/` first when the prompt names `artifacts/audits/...`. |
| 2026-05-01 | self | Repeated the Windows wildcard-path `rg` mistake while searching `mesocycle-handoff*.ts` files. | Use `rg <pattern> <directory> -g "mesocycle-handoff*.ts"` instead of passing wildcard paths directly in PowerShell. |
| 2026-05-01 | self | A handoff V2 probe fixture widened seed preview roles to `string` and used a readonly `as const` array, so `tsc --noEmit` failed after Vitest passed. | Type V2 materialization report fixtures with the exported DTO type and keep role literals / mutable arrays compatible with the production contract. |
| 2026-05-01 | self | In the accepted-seed compare fixture, first mis-summed the V2 comparison plan's total sets as 34 instead of 42. | Sum every slot explicitly when asserting seed-shape totals; do not do mental arithmetic from dense multi-slot fixtures. |
| 2026-05-01 | self | Expected legacy biceps/triceps support coverage to be false, but the legacy comparison fixture already provided support via Bench/Row secondary muscle evidence. | For class/lane compare assertions, distinguish direct-only gaps from direct/support coverage before marking legacy missing. |
| 2026-05-01 | self | Repeated a PowerShell quote parsing mistake while searching CLI tests with a double-quoted `rg` pattern containing escaped quotes. | Use single-quoted `rg` patterns or search unquoted simple symbols first when locating test describe blocks. |
| 2026-05-01 | self | Repeated the PowerShell quote parsing mistake while searching for `vi.mock("@/lib/engine/planning/v2")` with a double-quoted regex. | Use a simple single-quoted symbol search first, e.g. `rg 'engine/planning/v2' ...`, before adding regex punctuation. |
| 2026-05-01 | self | In a PowerShell-piped `tsx` DB probe, destructured named exports from `./src/lib/db/prisma` directly and hit `closePrismaResourcesForAuditCli is not a function` because stdin import wrapping exposed the module under `default`. | For ad hoc `tsx` stdin probes, unwrap `const mod = raw.default ?? raw` before destructuring local module exports, even for DB helpers. |
| 2026-05-02 | self | Built a PowerShell + `tsx` probe with the PowerShell env-loading prelude inside the piped script, so Node tried to parse PowerShell syntax. | Set env vars in PowerShell first, then pipe only the JavaScript/TypeScript here-string to `npx tsx -`. |
| 2026-05-02 | self | Tried to call `loadProgramPageData()` from a standalone `tsx` probe and hit Next `headers()` request-scope enforcement through the UI-audit fixture helper. | For page loaders that call `next/headers`, validate through a request/server path or import pure builders/mocked tests instead of direct standalone invocation. |
| 2026-05-02 | self | Repeated the Windows wildcard-path `rg` mistake while searching `src/lib/api/analytics*`, `history*`, and `closeout*`. | Search the owning directory and use `-g "analytics*.ts"` / `-g "closeout*.ts"` filters instead of wildcard path arguments. |
| 2026-05-02 | self | Repeated the stdin `tsx` named-import quirk with `scripts/audit-cli-support` and got a false missing export error for `loadAuditEnv`. | In PowerShell-piped `tsx` probes, dynamically import local modules and unwrap `raw.default ?? raw` before using exports. |
| 2026-05-02 | self | The first transition-week backfill exercise resolver treated preferred and fallback search terms equally, so `Close-Grip Pulldown` became ambiguous when `Lat Pulldown` also existed. | Resolve exercise search terms in priority order: exact match per term first, then contains fallback per term. |
| 2026-05-02 | self | Repeated the Windows `rg` path/pattern mistake during the downstream seed-consumer audit by mixing wildcard-like paths and quoted alternation. | Keep `rg` calls boring on Windows: search a real directory plus `-g` filters, and use one simple single-quoted pattern at a time. |
| 2026-05-02 | self | In a read-only mesocycle DB probe, selected nonexistent `Mesocycle.createdAt/updatedAt` fields and Prisma rejected the query. | Check `prisma/schema.prisma` or Prisma error field hints before adding timestamp selects to Trainer models; Mesocycle does not expose those timestamps. |
| 2026-05-02 | self | In a read-only swap audit probe, selected nonexistent `User.name` and Prisma rejected the query. | Keep Trainer user selects to actual schema fields such as `id`, `email`, and `createdAt`; profile data is under the related `profile` model. |
| 2026-05-02 | self | While instrumenting the empty-mesocycle V2 replacement dry-run, stubbed `buildDryRunReport` with `null` instead of pass-through delegation and crashed the read-only path. | Dependency wrappers used for diagnostics must call the real implementation first, then capture inputs/outputs; never replace core builders with placeholder returns. |
| 2026-05-02 | self | Assumed `normalizeV2MaterializationText` was exported from the V2 barrel index during a ranking probe and got a runtime missing-function error. | Import materialization taxonomy helpers from `src/lib/engine/planning/v2/materialization/taxonomy` when probing candidate ranking; the V2 index does not expose every helper. |
| 2026-05-02 | self | Repeated the V2 barrel export mistake in a live materializer ranking probe and failed on `normalizeV2MaterializationText is not a function`. | For ranking probes, import `normalizeV2MaterializationText` / class helpers from the materialization taxonomy module first, and only use the V2 barrel for public builders/types. |
| 2026-05-02 | self | Repeated the Windows wildcard-path `rg` mistake while searching `trainer-app\src\lib\engine\planning\v2\*.test.ts` and got an invalid path error. | Use `rg <pattern> <real-directory> -g "*.test.ts"` or `rg --files` filters for V2 test searches on PowerShell. |
| 2026-05-02 | self | During a taxonomy audit, trusted an initial file read that appeared to show `quad_isolation` still collapsed into `squat_pattern`, but the working tree already had local uncommitted taxonomy edits splitting it out. | Check `git diff`/current line-numbered file reads before declaring an issue fixed or unfixed, especially when the user says an investigation is already in progress. |
| 2026-05-02 | self | Exported `lane-selection-intent-audit` through the pure V2 barrel and tripped the architecture boundary regex because import paths containing `audit` are forbidden in pure policy imports. | Keep diagnostic helpers with `audit` in the filename direct-import-only, or choose a boundary-safe module name before adding barrel exports. |
| 2026-05-02 | self | In V2 anchor-quality validation, let `resolveV2ExerciseClassIds()` infer a narrow set while comparing against `V2ExerciseClassMatch.classId: string`, and left an unreachable switch case after a prior guard. | Normalize class-id diagnostic sets to `Set<string>` and let TypeScript narrowing remove impossible branches before rerunning full `tsc --noEmit`. |
| 2026-05-02 | self | Looked for `v2-set-distribution-intent.ts` under `planning/v2/` even though it lives one directory up and is re-exported by the V2 folder. | Use `rg --files trainer-app/src/lib/engine/planning | rg "set-distribution-intent"` before assuming the path from memory. |
| 2026-05-02 | self | Built another over-broad `rg` alternation with escaped quotes and braces and hit a regex parse error while checking stale mesocycle-demand expectations. | Prefer separate simple single-quoted `rg` searches for each stale literal on Windows. |
| 2026-05-02 | self | Looked for V2 base-plan validation directly under `planning/v2/` and hit a missing path during a warning-body probe. | Base-plan validation lives under `src/lib/engine/planning/v2/materialization/base-plan-validation.ts`; check `rg --files` before opening remembered V2 paths. |
| 2026-05-02 | self | Repeated a PowerShell terminator error by mixing embedded double quotes inside a broad `rg` alternation while checking stale backfill names. | Keep stale-literal checks in single-quoted `rg` patterns or split into boring separate searches. |
| 2026-05-02 | self | The first guarded Week 1 backfill write used the documented CLI path and hit Prisma's default 5s interactive transaction timeout after the safety checks passed. | Confirm rollback state, then rerun the same helper with explicit write/confirmation and a longer `$transaction` timeout wrapper; do not hand-build the DB write. |
| 2026-05-02 | self | During the Lower B read-model audit, repeated the Windows wildcard-path `rg` mistake with `src\lib\api\projected-week-volume*` and then hit the known `tsx` stdin default-wrapper import issue for Prisma. | Keep Windows `rg` paths concrete with `-g` filters, and unwrap dynamic stdin imports with `const mod = raw.default ?? raw` before using local exports. |
| 2026-05-02 | self | In the runtime north-star doc update, first placed runtime prescription/coaching before accepted seed/runtime replay in the stack even though prescriptions transform seed rows. | For seed/runtime strategy docs, order accepted seed and replay before runtime prescription/edit/save layers so the prose reinforces executable seed truth. |
| 2026-05-02 | self | Repeated the Windows wildcard-path `rg` mistake while checking V2 exports with `planning\v2\*.ts`. | Use `rg <pattern> <real-directory> -g "*.ts"` or read the specific barrel file directly. |
| 2026-05-27 | self | Parallelized focused Vitest reruns through `multi_tool_use` and the Vitest fork pool timed out before workers responded. | Run Trainer Vitest verification serially, especially for audit/serializer files; keep `multi_tool_use` for reads, not test runner invocations. |
| 2026-05-29 | self | While adding finish-deload-early, the first helper pass would have skipped any incomplete workout on the mesocycle, not only deload-scoped incomplete workouts. | Before terminally closing workouts in lifecycle helpers, classify the workout's canonical session semantics (`deriveSessionSemantics`) and reject unclear/non-target scope instead of broad status-based updates. |
| 2026-05-29 | self | A live `finish-deload` call to localhost:3000 returned a branded 404 because port 3000 was serving another Next app, not Trainer. | When a live Trainer route returns an HTML/branded 404, verify the owning process/app for the port before treating it as a route or DB ownership failure; if needed, start Trainer on a free port and call the same implemented app route there. |
| 2026-05-29 | self | Adding a third V2 Chest exposure initially diluted the ownership allocation enough to drop the existing chest anchor from 4 sets to 3, which would have kept Chest below its hard floor. | When adding V2 ownership rows, rerun set-distribution/materializer tests before trusting the policy; preserve anchor budgets explicitly when extra support ownership is added. |
| 2026-05-29 | self | `tsc --noEmit` caught a helper copied from the materializer into base-plan validation without a local definition. | After moving validation semantics across V2 materialization files, run `tsc` even when Vitest passes; localize tiny helper logic instead of reaching into materializer internals. |
| 2026-06-01 | self | The first pre-session readiness seed-match pass assumed Prisma test mocks would include newly selected `exercises`, causing old read-model mocks to throw on `flatMap`. | When extending Prisma selects in shared read seams, map optional relation arrays defensively or update all mocks before broad focused tests. |
| 2026-06-02 | self | While adding the app-owned pre-session readiness producer, a source-dependency guard treated any `slotPlanSeedJson` read as a forbidden seed mutation even though the producer only passes the seed into provenance evidence. | In source guards, distinguish read-only provenance/identity reads from mutation/write patterns; guard against update/create/write callsites rather than raw seed field names. |
| 2026-06-02 | self | The first no-artifact pre-session readiness smoke used `owner@local`, whose DB owner had no derivable next-session intent, so the smoke failed before exercising the readiness path. | For live pre-session readiness audit smoke, use a known active owner or pass an explicit intent/mesocycle context; keep `owner@local` for generic tests only when it has active runtime state. |

## User Preferences
- Keep Trainer implementation and audit work concise, direct, and production-friendly.
- In the long-running planning/facilitation thread, create new-session prompts with best judgment rather than reusing a fixed template; omit prompts for already-addressed work.
- Keep goal prompts under 4k characters; write them as bounded execution loops with mission, context, allowed/forbidden scope, process, success criteria, stop conditions, and final report format.

## Patterns That Work
- For planning-session handoffs, treat pasted implementation-session dumps as context but verify live `git status --short` before deciding whether the next prompt should consolidate dirty work or start fresh.
- For Home pre-session readiness card IA, add display-only fields at the app-owned contract/DTO seam (`pre-session-readiness-contract` -> `pre-session-readiness-gym-card`) when Home lacks safe data such as workout preview rows; keep the component rendering DTO fields only and avoid raw contract/audit prose parsing.
- For immediate post-save completed-review DTOs, add a narrow read-only route over the existing app read model (`resolveOwner()` -> `loadCompletedWorkoutReviewReadModel()` -> display DTO/null) and have the client render the shared card additively. Keep the explanation endpoint and producer/contract logic unchanged, and guard the client/route against audit, producer, contract, persistence, and mutation imports.
- For app-owned post-session review DTO integration, keep the first visible-noop slice in `src/lib/api`: expose a `PostSessionReviewDisplayDto | null` field through a completed-workout review read model, return `null` for missing/unauthorized rows, map not-ready/invalid producer blocks to safe display DTOs, and have workout pages load but not render the field until the UI slice explicitly consumes it.
- For the `/workout/[id]` post-session review UI slice, render only the app-owned `PostSessionReviewDisplayDto` through a presentation card, suppress empty sections, keep "No seed or plan changes made" visible from `source.noMutationNote`, and source-guard the page/component so they do not import audit, producer, contract, or immediate post-save review paths.
- For Home pre-session readiness UX, keep the page as a manual snapshot workflow: show `Check readiness` only when a next start/resume action exists and no valid `preSessionReadinessCard` is loaded, POST the existing prepare route, `router.refresh()` on success, and render only the DTO fields. Do not auto-generate readiness on Home load or gate Start Workout behind readiness.
- For pre-session readiness producer ownership, move current-week guidance and narrow prior-week fatigue evidence into `src/lib/api/pre-session-readiness-evidence-builder.ts`; keep full `weeklyRetro` reconciliation/operator payloads in workout-audit and let audit import the app builder/facade instead of the app producer importing audit producers.
- For pre-session readiness evidence ownership, keep builder input DTOs in `src/lib/api/pre-session-readiness-evidence.ts`; let audit payloads adapt into those DTOs at the audit boundary instead of importing `workout-audit/types` into the app builder.
- For pre-session readiness snapshot work, keep the shared typed contract, validator, and Home gym-card adapter under `src/lib/api/pre-session-readiness-*`; workout-audit is only an `audit_readout` producer, and Home should not require `auditOnly=true` / `consumedByProduction=false` to trust future app-owned snapshots.
- For mid-week post-session weekly-retro readouts, classify future `PLANNED`/`IN_PROGRESS` sessions scheduled after the latest `COMPLETED`/`PARTIAL` workout as future planned/incomplete work, not missed completed-session work; replacement-like pair counts should count planned-side pairs, not both planned and added rows.
- For V2 accepted-seed handoff retries, guard every existing-successor return path before any source-state update: both `COMPLETED` and still-`AWAITING_HANDOFF` sources must compare the successor `slotPlanSeedJson` against `nextSeedDraftJson.acceptedSeedDraft.slotPlanSeedJson` when present.
- For V2 handoff accept regressions, keep `handoffSummaryJson.recommendedNextSeed` legacy while `nextSeedDraftJson.acceptedSeedDraft` is V2 in the fixture. Mirroring V2 into both places masks sanitize/fallback bugs that only appear in the real stored-draft path.
- For live V2 next-mesocycle acceptance, compare the pre-accept persisted draft seed against the post-accept successor seed, not just post-accept replay coherence. A successor can replay `persisted_slot_plan_seed` safely while still being authored from `handoff_slot_plan_projection` instead of the accepted `v2_materialized_seed` draft.
- For pre-accept execution-readiness work, keep seed identity/set-count proof separate from load prescription proof: handoff dry-run and acceptance gate can prove candidate seed/runtime-contract readiness, but exact load/confidence source maps should be post-accept audit readouts over existing `prescriptionReadouts`, not a pre-accept recomputation of progression policy.
- For V2 target-vs-no-repair bridge work, use pure V2 materialization compare/shadow evidence only as diagnostic readout alignment: preserve the no-repair warning/repair evidence, demote the lane from `nextBestMigrationSlice`, and assert acceptance-gate candidate truth separately.
- For Trainer week-close lifecycle work, separate target-deficit review evidence from actionable optional-workflow state: `AUTO_DISMISSED` partial rows can preserve deficit truth without creating Home/Program blockers or delaying deload.
- For pre-session readiness stdout changes, run the live `--no-artifact --operator-debug` smoke after focused formatter tests; it can reveal cross-section wording contradictions that unit snippets miss, such as a new closure section allowing a top-up while the legacy avoid list still suppresses it.
- For deload pre-session readiness readout, the top `[workout-audit]` line can inherit prior-week/weekly-retro `week=N` summary text even when the active state is `ACTIVE_DELOAD`; assert the header separately from the body and prefer `diagnostics=deload deload_week=N accumulation_reference_week=M`.
- For Program page presentation tests, scope existing Projected Week Finish assertions to that section before adding a new volume snapshot that can reuse the same muscle names.
- For V2 planner/repair questions, treat repaired projection as evidence of legacy downstream responsibility, not as the target architecture.
- For V2 pure-policy extraction, add engine-owned builders first and leave planningReality adapters untouched unless equality/serialization evidence proves a safe no-drift adapter swap.
- For V2 `SelectionCapacityPlan`, keep the pure engine plan in `src/lib/engine/planning/v2` and assert audit/workout sidecar schemas do not expose it until an explicit artifact slice is requested.
- For no-drift diagnostic adapter swaps, `v2SetDistributionIntent` can be safely sourced from `buildV2PlannerMesocyclePolicy()` when the emitted sidecar object is structurally equal and main/sidecar artifacts diff clean after ignoring timestamps and artifact links.
- For verify cleanup, remove stimulus fallback warning noise by adding explicit fixture-only `stimulusProfile` values that match the fallback math, while leaving `src/lib/engine/stimulus.test.ts` as the intentional fallback-warning coverage.
- For V2 strategy recommendation layers, keep ranking in pure `mesocycle-strategy.ts`, expose only compact status/count/evidence summaries through audit serialization/CLI, and assert demand/materializer/generation non-consumption in both pure and audit tests.
- For V2 strategy promotion readiness, define the full evidence/owner/gate requirements in the pure strategy seam, but keep the main audit artifact to compact counts/top-missing summaries and leave full detail to the V2 sidecar.
- For V2 donor-surplus diagnostics, keep normalized donor evidence as a pure sibling of the promotion diff, let slot-owned planning consume it only diagnostically, and expose only compact counts in the main artifact/index while full rows stay in `v2-promotion-diffs`.
- For post-accept successor checks, put the proof in the read-only workout-audit seam: source/successor lifecycle, minimal executable seed rows, slot sequence stability, future-week replay, projected-week-volume, Program/Home read models, and provenance coherence. Do not patch runtime replay or handoff writes from this audit.
- For explicit lifecycle closeout actions, combine owner/state gates with canonical session-semantics classification before mutating related workouts; terminally skipped rows should be non-performed and scoped to the intended workflow.

## Patterns That Don't Work
- Do not promote suspicious downstream repair rows unless the owning V2 layer is clearly upstream and compatible with the target architecture.

## Domain Notes
- 2026-06-13 read-only next-track selection: after low-axial lane intent and candidate-quality/materializer-delta lab are complete/capped, pivot the next highest-ROI slice to runtime execution quality evidence (prescription confidence, readiness, swaps, post-session review, performed-reality loop) before more lane families or owner-specific V2 probes, unless fresh measured owner-specific evidence appears.
- 2026-06-13 self: Repeated the Windows `rg` wildcard path mistake by searching `trainer-app\docs\0*.md` / `10*.md` directly. Use directory searches with `-g` filters in PowerShell (`rg ... trainer-app\docs -g "0*.md" -g "10*.md"`).
- Repo skill review: keep the protective Trainer skills, but improve trigger precision instead of removing overlap. V2/audit skills should prefer source-attributed plan-quality benchmark gates over legacy repair-row probing as the default work queue. Skill/workflow-only edits should not require app verification unless they claim behavior, contracts, seed/runtime, or DB changes.
- For V2 concentration materializer projections, apply the trial lane budget across representative accumulation weeks for the scoped slot/lane. Applying only the diagnostic Week 1 row can produce a false zero-delta because the dry-run materializer reads the representative policy set, not just the selected warning row.
- V2 concentration `concentrationWarningDelta=-1` is currently a readout/benchmark signal, not behavior policy. Before promotion, the benchmark should surface `behaviorReadiness=diagnostic_only`, the missing promotion gates, and non-consumption proof; the next safe slice is a read-only acceptance/cross-week projection, not copying the lane-cap trial into SlotDemandAllocation, SetDistributionIntent, materializer ranking, seed, runtime, or repair.
- For V2 slot/week donor-capacity projections, test the pure allocation math before wiring audit readouts: row reducers must reduce `targetSetRange` values, and every new benchmark summary field needs both producer and exported `types.ts` coverage or the CLI can compile-fail after a presentation-only change.
- For the Calves bounded behavior-promotion slice, the code target is a small pure V2 helper/config in `src/lib/engine/planning/v2/slot-demand-allocation.ts`, not audit/planning-reality/materializer/runtime. The gate is exact: Weeks 2-4 `lower_a:calves` 4->3 and `lower_b:calves` 4->5, same muscle/lane, net weekly delta 0, protected coverage and materializer non-regression, exact duplicate clean, `mustFixW1=0`, no seed/runtime/receipt/DB/production-materializer consumption.
- V2 concentration-quality projection baseline: read-only live audit now attributes `fatigue_distribution` to `v2_concentration_materializer_projection` with `concentrationWarningDelta=-1`; benchmark remains `pass=4 warn=4 fail=0 missing=0`, exact duplicate reuse remains `v2DuplicateExactExercises=0`, and the slice stayed read-only with no seed/runtime/receipt/persistence/generation/DB impact.
- V2 duplicate/concentration slice fixed a true pure V2 issue in `ExerciseSelectionPlan`: optional activation requiring clean alternatives now prevents exact optional top-up reuse (`v2DuplicateExactExercises` 1 -> 0) without seed/runtime/repair changes. Remaining `duplicate_concentration_risk` is class-family reuse / unclear identity classification; next safe slice should measure `concentration_quality` projection deltas to separate `SlotDemandAllocationByWeek` policy gaps, materializer/ranking artifacts, and no-repair readout noise.
- V2 plan-quality benchmark now reports `pass=4 warn=4 fail=0 missing=0`: support floors, direct work, session size, and materializer omissions pass from pure V2 evidence. `duplicate_concentration_risk` remains the pure V2 watch item (`v2_base_plan_validation.duplicate_distinctness`, unclear identity classification, exact Cable Triceps Pushdown duplicate, and duplicate class-family reuse); `lane_preservation` is shadow-diagnostic warning, `fatigue_distribution` is no-repair projection warning, and `week_1_trainability` is acceptance/no-repair warning. Future benchmark prompts should require `gate`, `status`, `ownerSeam`, and `evidenceSource`; do not infer pure V2 failure from no-repair summaries when base-plan compare exists.
- Next north-star direction after repeated no-impact repair-quarantine probes: do one cleanup/deprecation analysis for misleading support-floor readout evidence, then pivot to a first-principles V2 plan-quality benchmark; use repair only as safety-net evidence and deprecate plan-author repair paths once benchmark/non-regression proof shows they no longer improve candidate quality.
- For V2 class/taxonomy mismatch ladder work, drive the read-only materializer comparison target from the selected ranked mismatch row; a fixed default lane can leave the selected proof blocked even when a no-drift probe is available. As of 2026-06-11, the live selected mismatch `week_2:lower_b:calves` measured `measured_no_drift`, so the representative taxonomy row is diagnostic-only and the next safe slice should move to the next ranked non-noop gap.
- For V2 set-distribution budget ladder work, pair the ranked set-budget inventory with a representative read-only materializer projection before inferring policy value. As of 2026-06-11, selected `week_1:upper_b:side_delt_isolation` measured zero identity/set/blocker deltas, so the selected SetDistributionIntent gap is diagnostic-only/no-impact until cross-week or acceptance gates prove otherwise.
- For V2 support-direct-floor ladder work, pair the ranked support-floor inventory with a representative read-only materializer projection before inferring policy value. As of 2026-06-12, selected `week_1:upper_a:side_delt_isolation:side_delts` measured zero identity/set/blocker/regression deltas, was owned by `audit_readout_cleanup`, and classified diagnostic-only/no-impact; the next safe slice should clean/disprove support-floor readout evidence before changing SetDistributionIntent or support policy.
- Latest V2 repair-quarantine gap inventory ranked `selection_capacity_pressure` first, then `class_taxonomy_mismatch`, `set_distribution_budget`, `support_direct_floor`, and `concentration_quality`; measured proof for `selection_capacity_pressure` via `upper_a_max_exercise_count_plus_one_projection_only` had zero identity/set/blocker deltas and `safeForBehaviorPromotion=false`, so next safe slice should pivot to `class_taxonomy_mismatch` with a no-drift taxonomy bridge probe.
- As of the June 11, 2026 read-only mesocycle-explain audit, repair quarantine is `legacyRepairQuarantine=evidence_only behaviorCandidates=0 quarantined=46 staleArtifacts=15`, grouped as `upstreamOwned=0 safetyRepairOnly=8 collateralAmbiguous=14 staleArtifact=15 missingEvidenceOrGate=9`; behaviorCandidates=0 is currently correct, and the next safe slice should prove one owner-specific upstream gap with measured no-repair/materializer projection before behavior promotion.
- Repo-local Codex skills live under `.codex/skills/<name>/SKILL.md`; there is no central skills index as of 2026-04-30, so a new practical guard skill can be added as a single `SKILL.md` unless UI metadata is explicitly needed.
- For Program/read-model DB truth checks, Vitest and `audit:* --env-file .env.local` resolve to `.env.local` when the shell has no pre-set `DATABASE_URL`; `.env.audit.local` can point at an older schema missing `Mesocycle.slotPlanSeedJson`, so do not use it to validate current seeded-slot behavior.
- V2 target path: MesocycleDemand -> WeeklyDemandCurve -> SlotDemandAllocationByWeek -> ExerciseClassDistributionBySlot -> SetDistributionIntent -> ExerciseSelectionPlan -> Accepted Seed -> Runtime Replay.
- V2 planner intelligence audit: current pure chain is still fixed-skeleton/muscle-lane derived; before promotion, add an upstream strategy layer for user profile, phase/macro intent, block objective, performed-history learning, and continuity/variation policy rather than treating pure V2 policy as elite by default.
- V2 Lower B hinge-anchor readouts should keep `hinge_compound` narrow for RDL/SLDL/deadlift-like hinges; Glute Bridge / hip-thrust-like patterns belong in the explicit diagnostic class `low_axial_hip_extension_anchor`, and generic `hinge` must not satisfy `hinge_compound`.
- V2 repair promotion readouts should keep `repairMaterialityAfterShadowAllocation` as raw evidence and put suspicious/cap-trim/removal/collateral/materiality-none rows into safety or diagnostic buckets; only positive slot-owned likely-avoidable rows should count as behavior-promotion candidates.
- After V2 support-lane projection shows B-support direct floors met, `likelyAvoidableMaterialRepairCount` is legacy repair pressure, not behavior-promotion pressure; current blockers should be read from V2 set-distribution/concentration gaps, class/taxonomy gaps, safety non-regression rows, and accepted seed/runtime consumption gates.
- V2 cross-week readiness distinction: `accumulationWeekProjection` may repeat Week 1 shape for diagnostic Weeks 2-4, but replacement readiness still treats Weeks 2-4 as unprojected while `slotDemandAllocationByWeek` / `preselectionDistributionPolicyByWeek` have missing per-week slot distribution, fatigue carryover, continuity, and identity policy.
- The V2 cross-week gate currently recognizes Weeks 2-4 readiness only when both `slotDemandAllocationByWeek` and `preselectionDistributionPolicyByWeek` use statuses containing `planner_owned`; `v2SetDistributionIntent` by itself remains diagnostic-only.
- For accepted V2 planner promotion, keep `slotPlanSeedJson` as the runtime replay truth for identities/set counts; put only compact planner provenance/intent metadata beside it, not in workout receipts until generated sessions are stamped.
- For accepted-seed provenance audits, do not conflate runtime `compositionSource="persisted_slot_plan_seed"` with seed authorship. Runtime replay can be correct while `slotPlanSeedJson.source` / seed planner metadata / transaction provenance still show legacy or handoff authorship.
- For V2 materialized seed acceptance, the API-owned helper is the only production-side exception allowed to consume the dry-run report, and it must remain explicit opt-in, fail-closed, and serializer-delegated.
- For V2 materialized seed source labels, keep the serializer default legacy and pass `source: "v2_materialized_seed"` only from the ready guarded helper path; read-only probes/previews should not become persistence truth.
- For V2 materialization, keep the first slice pure and dry-run-only under `src/lib/engine/planning/v2/materialization`; use a canonical taxonomy bridge before exact exercise selection and assert no production callsite or audit/seed/runtime serialization exists.
- For V2 materializer taxonomy, prefer token/phrase-boundary matching over substring text checks, and let explicit stimulus profiles override broad primary-muscle labels for anchor-quality decisions.
- For V2 materialization selection quality, source dry-run selection requirements from `V2SetDistributionIntent` plus class ownership rows, use the first accumulation week as the base representative plan, and treat managed collateral / zero-budget optional lanes as omissions rather than direct work.
- For replace-empty V2 dry-run artifact visibility, keep candidate identity summaries selected-identity-only in the dry-run report and mark ranking details unavailable unless the materializer emits real ranking diagnostics.
- Current V2 architecture audit result: `v2-set-distribution-intent.ts` is pure engine-side set-budget policy, but most other V2-shaped objects still live under `src/lib/api/planning-reality` or `workout-audit/mesocycle-explain.ts` and are diagnostic/readout. Extract pure MesocycleDemand/WeeklyDemandCurve/SlotDemandAllocation/ExerciseClassDistribution before persisting `acceptedPlannerIntent`.
- For V2 class/taxonomy diagnostic readout cleanup, no-drift proof should compare `repairPromotionScoreboard.rawRepairEvidence`, `v2TargetVsNoRepairDiff`, and `slotPlans`/selected identities between baseline and new sidecars; class mismatch counts may change while those remain equal.
- Lower B low-axial hip-extension limitation is a planner-only no-repair readout, not a selection policy: derive curl-vs-hip-extension contribution and true-hinge exposure from `planningReality.finalSlotPlan`, keep full evidence in the V2 sidecar, and keep the main artifact compact.
- V2 selection-capacity diagnostics are a read-only planning-reality layer between class/set intent and selection-plan readout. They should classify capacity/cap/headroom pressure from existing V2 policy and no-repair evidence, not promote behavior or reinterpret repaired projection.
- V2 support-lane boundary diagnostics should distinguish support policy authorship, set-distribution budget authorship, and exercise-selection preservation before assigning ownership; authored/budgeted lanes that disappear after selection belong to materializer/exercise-selection capacity readout, not planner demand miss readout.
- V2 strategy promotion protected candidates can be broader than the CLI's "target-tier under-hit" line: the candidate list includes target-tier recurring under-hit plus target-tier below-MEV lagging flags, while the CLI line may show only the recurring-under-hit subset. Check the promotion-diffs shard before assuming secondary noise leaked in.
- For mesocycle-explain wording-only cleanup, `workout-audit-cli.test.ts`, artifact serialization tests, targeted `mesocycle-explain.test.ts -t ...`, and live `--no-artifact --operator-debug` smoke give useful signal even when the full broad mesocycle-explain file has unrelated V2 diagnostic expectation drift.
- For audit diagnostic drift after V2 policy changes, run a narrow `mesocycle-explain.test.ts -t ...` filter first, confirm fixtures still match the source-of-truth owner logic and current set-distribution budgets, then update expectations/fixtures before running the full file plus `tsc`.
- For Strategy B candidate-gate cleanup, keep base-plan shadow-consumption evidence in the candidate evaluator as diagnostic signal only: surface `delta/remaining/current/regressions/consumedByProduction/nextSafeAction`, but do not let it affect acceptance thresholds, seed/runtime behavior, or production materializer consumption.
- For Strategy B Candidate Evaluator extraction, bundle candidate-quality assessments first (`repairBurden`, `supportLaneBoundary`, `shadowConsumption`) and let the acceptance gate consume that bundle for gate rows/watch items/decision summary; this keeps the gate as decision wrapper instead of drifting back into quality computation.
- For Strategy B architecture work, compact evaluator readouts are useful when they classify existing diagnostics by owner seam (metadata vs selection/ranking vs capacity) and stay out of acceptance scoring, materializer policy, seed shape, and runtime replay.
- For Strategy B demand learning, keep zone semantics distinct: floor evidence protects trainability, productive misses require recurrence before strategy action, stretch misses are not demand by default, and cap/MAV plus skipped-set hard-week evidence points first to fatigue or redistribution hypotheses.
- For Strategy B demand-zone learning, the safe owner is `V2MesocycleStrategyDiagnostic`: expose compact counts/read-only flags in audit output, assert demand/materializer non-consumption, and do not let performed-history learning mutate current seed/runtime or acceptance thresholds.
- For Strategy B strategy-to-demand bridge work, keep the first slice as an owner-labeled read-only diff beside `demandZoneLearning`; it may classify future `MesocycleDemand`, `WeeklyDemandCurve`, `SlotDemandAllocation`, or `SetDistributionIntent` actions, but `MesocycleDemand` and downstream policy objects must remain unchanged until a separate read-only projection/non-regression slice exists.
- For Strategy B strategy-to-demand projection work, make the first projection a pure non-mutating join from `strategyToDemandDiff` to current static `MesocycleDemand`: projected ranges equal current demand, non-mutation gates pass, behavior gates stay unknown, and compact audit/CLI output reports counts rather than row bulk.
- For Strategy B measured projection work, separate current no-mutation measurement from future behavior trials: `measuredCurrentNonRegressionSummary` can prove zero deltas against static `MesocycleDemand`, but behavior projection gates must stay unknown until a separate bounded trial exists.
- For Strategy B bounded behavior trials, keep the first trial row-level and diagnostic: a one-set floor-buffer candidate may be framed from static demand, but it must block on net-new volume and unmeasured downstream weekly/slot/set/materializer gates until slot-owned redistribution context exists.
- For Strategy B bounded behavior trial readouts, count redistribution readiness only across actual `trial_candidate` rows. Context can advance `nextSafeAction` to downstream projection, but it must not make net-new-volume, weekly/slot/set, or materializer gates pass by implication.
- For Strategy B downstream behavior projection, start with context inventory only: weekly curve, slot allocation, and set distribution availability can advance the next diagnostic action to measured redistribution projection, but donor-offset math, net-new-volume preservation, and materializer non-regression stay unknown and non-executable until measured explicitly.
- For Strategy B measured redistribution projection, keep compact per-candidate rows serialized, not just the summary. The next behavior/design slice needs donor offsets, slot owners, protected coverage, deltas, gates, and readiness visible in the artifact to avoid guessing from rollups.
- For Strategy B measured redistribution blockers, expose both row-level reasons and projection-level required evidence. A combined shadow projection that uses one donor for every candidate should say it is not an independent candidate projection before any behavior-trial design.
- For Strategy B demand-zone measured redistribution, keep unmeasured downstream gates visible as structured counts in the pure `strategyToDemandProjection` blocker summary; `materializerNonRegression` unknown should be operator-visible, not hidden inside a generic unknown-evidence rollup.
- For Strategy B alternate-donor diagnostics, keep no-row measured projections `not_available`; only call the donor pool blocked when measured rows exist and existing slot-owned/pre-shadow evidence shows no non-current eligible donor or slot owner.
- For Strategy B capacity-policy trials, a cap delta is not behavior-ready just because the dry-run materializer stays valid. Rerun the materializer on a projection-only capacity/selection plan, report identity/set deltas and gates, and keep promotion blocked when acceptance is unknown or the cap delta produces no candidate impact.
- For read-only materializer comparison harness work, keep reusable baseline-vs-trial deltas in the pure V2 materialization seam (`compareV2MaterializedPlans()`); audit harnesses should orchestrate dry-runs and gates while compact changed-slot rows remain diagnostic evidence, not materializer or seed/runtime policy.
- For lane-intent materializer shadow trials, make the diagnostic override explicit in the materializer input and guard it with `readOnly`, `affectsScoringOrGeneration=false`, `dryRunOnly`, and a comparison-trial reason. Keep production `laneSelectionIntent` consumption allowlists unchanged; the audit builder can compare baseline/trial dry runs, but the result must stay out of seed/runtime, receipts, DB, UI, and production materializer policy.
- When making a materializer shadow trial operator-visible, attach it to the existing `plannerOnlyNoRepair` diagnostic and `v2-materialization` debug shard/index summary rather than creating a new artifact surface or expanding main artifact bulk.
- For Home workout-start UX changes, keep the canonical read-model resume path (`mode: existing` / active resume links) separate from the generate-card happy path; when changing the primary button behavior, preserve preview/manual-save tests for metadata round-trip and `orderIndex` save ordering.
- For Home active-week checklist UX, derive expected rows from `readRuntimeSlotSequence()` and current-week workout receipts/statuses inside `loadHomeProgramSupport()`; keep the page rendering display-only and avoid importing the richer Program page slot-map when Home only needs label/status/href.
- For active-mesocycle Priority 1 readiness copy changes, pair focused contract/card/Home tests with `npm run audit:workout -- --mode pre-session-readiness --no-artifact --operator-debug`; the audit proves read-only guarantees and persisted seed replay while showing the exact trainable coaching readout.
- For Priority 2 performed-reality work, the safe first contract owner is `src/lib/api/post-session-review-contract-builder.ts`: derive load/reps/RPE coherence from persisted `WorkoutSet` targets plus `SetLog` actuals, keep it evidence-only, and do not feed explainability next-exposure, progression policy, receipts, seed/runtime, or persistence.
- For weekly retro calibration in app surfaces, summarize existing `PostSessionReviewContract.performedReality.rows` in `src/lib/api`; do not consume `src/lib/audit/workout-audit/weekly-retro` artifacts or collapse duplicate current workout rows by catalog `exerciseId`.
- For post-session recent-exposure calibration, filter prior rows through `deriveSessionSemantics().countsTowardPerformanceHistory` and trust that boundary; optional/body-part performed rows may still count as performance-history evidence, so do not add local exclusion rules unless the canonical semantic changes.
- For next-mesocycle acceptance work, keep reusable candidate-quality computation in `src/lib/audit/workout-audit/next-mesocycle-candidate-evaluator.ts`; the acceptance gate should wrap those assessments into gates/findings/watch items/final result rather than owning completed-block or materializer-quality evidence directly.
- In next-mesocycle acceptance live smokes during an active mesocycle, diagnostic preview floor failures must not label repair burden as `candidate_truth`; when `candidateFound=false`, classify repair burden as diagnostic/legacy context even if preview volume rows fail floors.
- For exercise inventory metadata quality, keep the first slice in candidate-evaluator readouts: summarize existing `exerciseClassUnresolvedCauses` and guarded V2 selection mismatches as compact examples, but do not feed them into materializer scoring, seed serialization, runtime replay, receipts, or persistence.
- For Priority 5 lane intent hardening during active training, the safe first move is explicit non-consumed `laneSelectionIntent v0` on a high-risk lane. Prove `consumedByMaterializer=false` before any measured behavior trial; intent metadata alone must not change dry-run selection, production generation, seed/runtime, receipts, DB, or acceptance thresholds.
- For low-axial hip-extension lane-intent design, keep the warning diagnostic until a future behavior slice proves value. The intended contract is low-axial posterior-chain support: loadable direct/high-support hip extension with meaningful Glutes stimulus, low axial fatigue, clean variation preference, hip-thrust/bridge/pull-through/reverse-hyper family, and explicit disallowance of true hinge overload, hamstring curl substitution, back-extension/lower-back-heavy closure, and generic glute accessories. Do not add enum values if doing so would force production materializer switch handling in a design-only slice.
- For read-only low-axial lane-intent contract work, prefer structured benchmark coverage-gap strings over widening `laneSelectionIntent v0` movement/class unions. This keeps the remaining contract measurable (`low_axial_hip_extension`, `low_axial_hip_extension_anchor`, stimulus/fatigue/loadability/substitution/duplicate/failure fields) without creating production materializer switch or ranking obligations.
- Reverse Hyperextension low-axial ontology cleanup resolved the phrase gap in `src/lib/engine/planning/v2/materialization/taxonomy.ts`: it now maps to `low_axial_hip_extension_anchor`, while Back Extension remains excluded, RDL/SLDL/deadlift remain `hinge_compound`, hamstring curls remain `knee_flexion_curl`, and generic glute accessories remain excluded. Treat future low-axial warnings as lane-intent contract work unless new measured materializer drift appears.
- The measured low-axial support-coverage projection should apply the exact future contract as an audit-only selection-plan class override, not by faking a new v0 movement/class. Live 2026-06-13 proof: `lower_b:hinge_anchor` changed `Stiff-Legged Deadlift:3 -> Cable Pull-Through:3`, preserved `Seated Leg Curl:3`, total sets/blockers unchanged, low-axial Glutes sets `0 -> 3`, high-fatigue sets `-3`, exclusions preserved, and seed/runtime/receipt/DB/acceptance/repair non-consumption held.
- Low-axial lane-intent promotion is now a reusable checklist pattern: ontology correctness -> lane contract -> audit-only projection -> acceptance/non-regression -> tiny owner-seam behavior -> seed/runtime boundary review -> stop unless concrete user-facing/read-model evidence appears. No-impact can still be architecture value only when it improves semantic correctness, ownership, benchmark fidelity, or safety; otherwise pivot.
- For the first V2 candidate-quality lab slice, keep fixtures pure under `src/lib/engine/planning/v2` and consume the existing lane-intent benchmark instead of re-authoring policy. Each scenario should name equipment/inventory constraints, expected and actual pass/warn/fail/watch outcome, owner seam, evidence source, gap kind, next safe action, and explicit non-consumption.
- After the first candidate-quality lab passes all seven lane-intent fixtures, the next high-ROI lab slice is materializer-delta fixture coverage: reuse the pure fixture model, add baseline/trial identity/set/blocker/protected-coverage/non-consumption evidence for selected scenarios, and keep it read-only before choosing another lane behavior.
- For Strategy B Side Delts `protect_floor`, the owner-scoped projection should stay blocked when evidence is only a combined strategy shadow projection: source evidence and upper-slot context are useful, but behavior needs a bounded delta plus owner-specific class/capacity/selection and materializer identity/set/blocker deltas before promotion.
- For Strategy B Side Delts `protect_floor`, measured owner-specific materializer projection can regress the target lane even when the in-memory allocation delta is `+1`. As of 2026-06-13, `week_1:upper_b:side_delt_isolation` produced identity delta `0`, total set delta `0`, lane set delta `-1`, blocker delta `0`, protected coverage regressed, concentration improved, and readiness blocked; do not infer behavior readiness from allocation-only evidence.

- 2026-06-13: During low-axial hip-extension ontology inventory, a direct stdin tsx import of stimulus.ts resolved as CommonJS-shaped default/module.exports, and a double-quoted rg pattern with embedded quotes caused a PowerShell parser error. For future read-only inventory probes, inspect module export shape first or use default/module.exports fallback, and use single-quoted rg patterns for quote-heavy TypeScript searches.
- 2026-06-14: For runtime swap row-anchor exceptions, keep catalog metadata unchanged and express the allowance in `src/lib/api/runtime-exercise-swap.ts` against existing source lane/class/fatigue/stress facts. Service tests should prove receipt/seed context is only mapped into policy, while optional DTO fields like `caution` should be asserted separately with `.toBeUndefined()` when omitted.
- 2026-06-15: For V2 class taxonomy mismatch probes, check selected identities against `materialization/taxonomy.ts` before treating `selectedClass=null` readout rows as ontology or ranking gaps. In the live Calves probe, `Seated Calf Raise` and `Leg Press Calf Raise` both matched `calf_isolation`, so the likely first owner is audit/selection-diagnostic bridge labeling, not materializer behavior.
- 2026-06-15: When cleaning V2 taxonomy mismatch readouts, preserve raw mismatch inventory rows but separate active policy pressure from stale/noise rows after pure materializer taxonomy labeling. The useful live proof is `taxonomyMismatchInventory rows=8 owners=audit_readout_cleanup=8` plus absence of `class_taxonomy_mismatch` from ranked gap inventory, not disappearance of the raw diagnostic rows.
- 2026-06-15: For V2 base-plan compare/readout, do not collapse "materialized diagnostic rows exist" into "validation passed." A failed base validation should keep materialized slots/totals/shadow rows diagnostically available while `v2_base_validation_failed` blocks promotion; otherwise the benchmark emits contradictory `v2=no` / `shadow_slot_unavailable` noise and hides true class-policy gaps.
- 2026-06-15: In low-axial V2 materializer work, broad disallowed text checks can accidentally reject explicitly allowed class matches; `Cable Pull-Through` was allowed as `low_axial_hip_extension_anchor` but rejected by generic `hinge` text. Let explicit lane-intent class matches win over broad text disallows, and avoid top-level `await` in `tsx -e` probes because this repo runs eval as CJS.
- 2026-06-15: Live V2 promotion can be `blocked_by_missing_evidence` even when the benchmark is `pass=5 warn=4 fail=0 missing=0 mustFixW1=0`. Treat `materializer_omissions` passing from `pure_v2_base_plan` as resolved, but keep suspicious repair rows (`weekly_demand_owned_elsewhere` / raw suspicious do-not-promote) quarantined as safety/readout evidence until a debug shard exposes row-level owner proof and a bounded projection proves impact.
- 2026-06-15: For V2 promotion-readiness readout cleanup, do not let non-actionable fresh inventory rows with `readiness!="candidate_for_read_only_projection"` keep the evaluator in actionable missing-proof status. Classify them as no-action/low-ROI while preserving raw row proof in the debug shard, so the CLI can explicitly say no next projection is recommended.

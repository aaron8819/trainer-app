interface:
  display_name: "Audit Workflow"
  short_description: "Route audits and validate Trainer output"
  default_prompt: "Use $audit-workflow to choose an authorized Trainer audit, verify its target, and judge the canonical output artifact."

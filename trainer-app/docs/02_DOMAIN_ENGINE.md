# 02 Domain Engine

Owner: Aaron
Last reviewed: 2026-03-19
Purpose: Canonical reference for workout-generation domain logic, including selection, progression, periodization, readiness, and explainability.

Current V2 readout note, 2026-06-13: the promoted Weeks 2-4 Calves allocation (`lower_a:calves` 4 -> 3, `lower_b:calves` 4 -> 5) is baseline planner policy in pure `SlotDemandAllocationByWeek`. Audit materializer/readout code should classify that exact proof as `measured_promoted_baseline_idempotent` / bounded-owner watch evidence, not no-impact pressure. Support-floor/readout rows should preserve raw source evidence such as `direct_floor_below`, but carry a separate compact readout classification so measured no-impact or stale audit-cleanup rows do not become owner-policy work. All detail remains diagnostic-only and out of seed/runtime/receipt/DB behavior.

Current V2 strategy-to-demand note, 2026-06-13: `V2PlannerMesocyclePolicy.strategyToDemandProjection.ownerScopedProjection` gives selected owner-specific strategy rows compact read-only projections, and `plannerOnlyNoRepair.v2StrategyRowMaterializerProjection` measures exactly one selected row through downstream policy and the dry-run materializer. The latest live owner row is `SlotDemandAllocationByWeek:Biceps:protect_floor`; the materializer trial applies a projection-only `+1` single-set floor buffer to `week_1:upper_b:biceps`, rebuilds class distribution, selection capacity, and exercise selection from that in-memory allocation copy, then compares baseline versus trial materialization. Live evidence measured baseline/trial selected and materialized Biceps as `Barbell Curl:2 -> Barbell Curl:2`, with selected-identity delta `0`, total-set delta `0`, target-lane set delta `0`, materializer-blocker delta `0`, protected coverage `preserved`, concentration `preserved`, loss cause `not_measured` / `target_lane_not_regressed`, readiness `diagnostic_no_impact`, and next safe slice `pivot_to_higher_roi_track`. The earlier Side Delts row also remains measured no-impact with the same non-consumption guardrails. These rows are evidence only: static demand, canonical allocation, production materializer behavior, acceptance thresholds, seed/runtime replay, receipts, DB, and UI remain unchanged.

Current V2 promotion-candidate evaluator note, 2026-06-13: `plannerOnlyNoRepair.v2PromotionCandidateEvaluator` is the compact read-only work-queue layer for future V2 planner slices. It ranks candidates from existing owner-specific diagnostics only: strategy-to-demand projection, clean preselection feasibility, V2 plan-quality benchmark watches, repair-scoreboard gap inventory, materializer comparison projections, and selected proof rows. It scores measured owner-specific positive impact, materializer non-regression, protected coverage, acceptance/watch status, seed/runtime/receipt/DB non-consumption, source attribution quality, prior probe result, and estimated implementation scope. It stops stale/noise, measured no-impact, repair-only, combined-shadow-only, missing bounded delta, materializer regression, diagnostic artifact, and already-promoted-baseline rows before they can become behavior work. The Biceps protect-floor row is now measured by `plannerOnlyNoRepair.v2StrategyRowMaterializerProjection` and classified `diagnostic_no_impact` / `measured_no_impact`; the lower-b Hamstrings clean-preselection row is similarly measured by `plannerOnlyNoRepair.v2PreselectionMaterializerProjection` as `Seated Leg Curl:3 -> Seated Leg Curl:3` with zero identity/set/lane/blocker deltas. Fresh owner-specific rows without this kind of projection can still move the evaluator to `blocked_by_missing_evidence`; that means one bounded read-only projection or acceptance/non-regression proof is still required, not that behavior is ready. The main artifact carries only the compact recommendation and counts; candidate row detail belongs in the V2 debug index / promotion-readiness shard. This evaluator does not feed demand, weekly curves, slot allocation, set distribution, materializer ranking, production generation, repair, seed serialization, runtime replay, receipts, UI, API writes, DB, or persistence.

Current V2 lane-intent benchmark note, 2026-06-13: `buildV2LaneSelectionIntentBenchmark()` is the pure read-only ontology/lane-job expectation slice for high-risk materializer jobs. It evaluates calf direct, hamstring curl, side-delt direct, rear-delt direct, chest-biased press support, vertical-pull anchor, and optional low-axial hip-extension intent against existing `laneSelectionIntent v0` audit evidence. Required jobs currently pass as explicit intent rows; low-axial hip extension remains a diagnostic warning because hinge overload and low-axial posterior-chain support are not yet separated into a consumed lane intent. The V2 plan-quality benchmark consumes this as the `lane_intent_explicitness` gate with source `pure_v2_lane_selection_intent_audit`; it can create watch items and future ontology/contract work, but it does not change `MesocycleDemand`, weekly curves, slot allocation, set distribution, materializer ranking, production generation, repair, seed serialization, runtime replay, receipts, UI, API writes, DB, or persistence.

This doc covers:
- Selection and session construction
- Progression/load assignment
- Periodization and readiness/autoregulation
- Explainability generation

Invariants:
- Selection and generation logic live in `src/lib/engine` and are invoked by `src/lib/api`.
- Persisted session enums must stay aligned with `docs/contracts/runtime-contracts.json`.
- Logged set data is the primary progression feedback input.
- Live workout coaching cues are non-canonical UI feedback; they must not change generator-owned next-session progression decisions.

Sources of truth:
- `trainer-app/src/lib/engine/selection-v2`
- `trainer-app/src/lib/engine/progression.ts`
- `trainer-app/src/lib/progression/load-coaching.ts`
- `trainer-app/src/lib/engine/template-session.ts`
- `trainer-app/src/lib/engine/periodization`
- `trainer-app/src/lib/engine/readiness`
- `trainer-app/src/lib/engine/explainability`
- `trainer-app/src/lib/planning/session-opportunities.ts`
- `trainer-app/src/lib/api/template-session.ts`
- `trainer-app/src/lib/api/workout-context.ts`
- `trainer-app/src/components/log-workout/useWorkoutSessionFlow.ts`

## Selection and generation
- Intent and template generation both rely on engine-level session construction and selection primitives.
- Selection-v2 beam search implementation is under `src/lib/engine/selection-v2`.
- Template session orchestration bridges API data to engine inputs in `src/lib/api/template-session.ts`, with planning semantics centralized in `src/lib/planning/session-opportunities.ts` and dedicated seams in `src/lib/api/template-session/role-budgeting.ts` and `src/lib/api/template-session/closure-actions.ts`.
- Repeated-slot planning policy is now split intentionally across the slot contract and resolver seams. `src/lib/api/mesocycle-slot-contract.ts` is the canonical authored-semantics source for accepted slot placement (`slotArchetype`, `primaryLaneContract`, `supportCoverageContract`, `continuityScope`), while `resolveSessionSlotPolicy()` in `src/lib/planning/session-slot-profile.ts` resolves that authored contract into current-session and future-slot policy for generation. Legacy repeated-slot heuristics remain only as an explicit compatibility fallback for persisted slot sequences that predate authored semantics. For repeated `upper` slots, the resolved contract enforces both `press` and `pull` lanes when viable; authored protected support is Chest/Triceps/Rear Delts for horizontal-balanced upper slots and Chest/Triceps/Side Delts for vertical-balanced upper slots. For repeated `lower` slots, it governs one dominant `primary` lane. The session-shape layer stays soft and additive for support work, but its required movement-pattern coverage is now honored during selection when viable instead of depending on closure-only replacement. Required coverage uses the existing compound/accessory distinction: compound matches can satisfy required slot coverage, while supportive non-compound pattern matches remain useful for scoring and support work but do not close the requirement on their own. Repeated `upper` slots must therefore keep at least one compound `horizontal_pull` each when viable, while `upper_a` still preserves complementary compound `vertical_pull` coverage and the existing duplicate/over-budget support penalties remain soft. Repeated `lower` slots keep the same primary split of `lower_a` squat-led and `lower_b` hinge-led, but the authored lower-pair scaffold now expects `lower_a` to carry hamstring-support/calves support while still requiring one viable hinge-pattern contribution and `lower_b` to carry the existing squat/quad-support expectation without becoming a second squat-primary day. The same seam also exposes the intentionally minimal future-slot bias used by remaining-week planning before closure/diversification.
- The canonical advancing pre-load composition seam now lives in `composeIntentSessionFromMappedContext()` within `src/lib/api/template-session.ts`. That helper reuses the normal intent-generation stack through `buildSelectionObjective()` and `runSessionGeneration()` but intentionally stops before load assignment, receipt building, and response-only metadata so handoff slot-plan projection can reuse the same selection logic without forking it.
- Projection-driven upper/lower repair for handoff slot-plan projection must stay inside the normal session exercise-cap envelope. A lightweight weekly-obligation pass in `src/lib/api/mesocycle-handoff-slot-plan-projection.weekly-obligations.ts` computes Week 1 hard primary targets for Chest, Lats, Quads, and Hamstrings through `getWeeklyVolumeTarget()`, allocates those obligations across compatible authored slots before slot-local selection, and adds cross-slot duplicate-reuse pressure without changing seed schema or runtime replay. The projection pass also promotes a bounded `SlotPreselectionDemand` into slot-local selection for compatible upper-slot authored Side Delts protected-support floors only; hard primary obligations remain owned by the weekly-obligation seam and other support remains on the existing repair path for this first behavior step. The current four-day upper/lower hypertrophy slice also passes a projection-only `SlotLanePlanLane[]` from `src/lib/api/mesocycle-handoff-slot-lane-plan.ts` into selection-v2 before exercise selection; beam search treats those lanes as bounded class/set intent and `computeProposedSets()` can use their preferred set counts, but runtime seeded replay, seed schema, receipts, save/log flows, and performed workouts do not read or persist the lane plan. The same guard excludes cross-region or diagnostic collateral such as lower-slot Chest, Front Delts, Upper Back, Core, Lower Back, Adductors, and Forearms. `projectionRepairMuscles` may rebalance exercise choice and closure outcomes toward protected coverage, but it must not unlock a larger max-exercise budget than the standard session-cap contract. Upper protected-support quality is evaluated only in the projection seam with a flat meaningful floor of 2 effective sets per protected upper support muscle; candidate ranking, subordinate fallback gates, and promoted soft-demand consumption use that floor instead of treating incidental trace contribution as satisfied or forcing full soft support landmarks. Week 1 support-floor repair is also bounded in this seam and shared with role budgeting for key support muscles (Chest, Calves, Side Delts, Triceps, Rear Delts, and Biceps), with primary MEV muscles (Chest, Lats, Quads, Hamstrings) protected before support bumps can crowd them out. Projection set viability is enforced before final MAV trimming: main lifts must keep at least 3 hard sets and accessories at least 2, with sub-floor work removed or merged by redistributing sets inside the same slot rather than increasing exercise-count caps or changing seed schema. Support-floor closure uses weighted effective-set accounting, prefers adding another compatible support exercise before stacking sets on existing work, caps projection set counts at 4 accessory sets and 5 main-lift sets, and limits a repair exercise to at most half of the target muscle's practical-floor contribution before allowing further bumps. Promoted soft Side Delts demand that is already consumed at the meaningful preselection floor is recorded as `preselection_demand_consumed` instead of adding extra Side Delts identities late. It then runs final projected-week MEV/MAV verification and emits explicit non-blocking projection diagnostics when real capacity, compatibility, slot identity, exercise-cap, contribution-cap, or effective-weight constraints prevent full closure. For horizontal-balanced upper projections, support repair is direction-aware: redundant non-directional pull accessories may be trimmed only when required pull identity and protected support remain preserved. For hinge-dominant lower projections, candidate quality and final projection ordering keep a viable hinge compound as the primary anchor while allowing squat as subordinate support. Runtime seed parsing, receipts, save/log flows, Accessory Lane policy, and global selection-v2 scoring do not own these projection-only guardrails.
- The same projection seam emits a read-only `planningReality` diagnostic for audit/explain payloads. It snapshots initial slot-local composition before final shaping, compares it to the final projected seed shape, and reports weekly demand ownership, slot allocation basis, projected delivery deltas, repair materiality, and exercise concentration without feeding scoring, generation, seed serialization, runtime replay, or persistence. The diagnostic also includes a shadow upstream planner (`shadowWeeklyDemand`, `shadowSlotDemandAllocation`, `allocationVsInitialDelta`, `allocationVsFinalDelta`, and `repairMaterialityAfterShadowAllocation`) that models which muscles should have been owned before slot-local exercise selection. `shadowRepairSummary`, `suspiciousRepairsNotEligibleForPromotion`, and `promotionCandidates` separate likely upstream-avoidable demand from remaining cleanup and bad downstream repair artifacts that must not be promoted. `weakPreselectionConsumption` flags any consumed preselection demand whose `targetMet=false`; consumed demand alone is not a successful promotion signal when the target remains missed. `slotPrescriptionIntents` is a read-only slot-level semantic bridge between weekly/slot demand and future selection objectives: per slot it records owned muscles, demand type, allowed/forbidden patterns/classes, collateral caps, movement lanes, set/diversity/fatigue budgets, and repair ownership buckets. `setDistributionIntents` is the sibling read-only set-distribution bridge: per slot/muscle it records preferred set spread, single-exercise and single-pattern shares, per-exercise/direct-exercise limits, what to do at the limit, and compact concentration/cap-cleanup/repair-owned evidence. `preselectionDistributionPolicyByWeek` is a read-only diagnostic shadow policy that lifts the current Week 1 evidence into explicit per-slot distribution policy rows, then marks later accumulation weeks and deload as unprojected when weekly demand curves, per-week slot distribution, fatigue carryover, or deload identity/set-reduction policy are missing. `weeklyDemandCurve` is the sibling full-mesocycle demand diagnostic: it records Week 1 current evidence, conservative Week 2-4 demand visibility with missing per-week distribution/fatigue/continuity limitations, explicit deload projection limitations, and cross-week warnings for Chest under-target risk, Hamstrings overdelivery risk, Side Delts under-target risk, duplicate/concentration fatigue, collateral readout risk, and missing deload preservation. Its candidate gate keeps `chest_upper_slot_distinct_exercise_distribution` blocked until the curve can answer whether the behavior improves Weeks 1-4, preserves deload quality, and avoids increasing fatigue concentration. The candidate behavior slices rank Chest upper-slot distinct exercise distribution as the safest future behavior after week-by-week projection exists, while keeping Hamstrings overdelivery control, Side Delts second-slot support, duplicate main-lift suppression, and calf duplicate suppression diagnostic-only or lower priority until their prerequisites exist. `preselectionFeasibility` is a read-only candidate gate for future upstream trials; currently it evaluates only `lower_b` Hamstrings and separates clean knee-flexion curl evidence from dirty Back Extension closure, Glutes/Lower Back collateral, Stiff-Legged Deadlift concentration, cap cleanup, suspicious repair, and weak-preselection risk. It also carries a read-only `candidateInventory` for lower-compatible Hamstrings inventory so clean knee-flexion curls can be seen even when they were not selected into `lower_b`; each row reports candidate class, per-set stimulus, lower/lower_b compatibility, already-selected slot ids, lower_b initial/final selection, availability, capacity/duplicate visibility, and classification bridge mismatches such as inventory `movementPatterns=["flexion"]` versus slot prescription `allowedPatterns=["hinge","isolation"]` with `allowedExerciseClasses=["knee_flexion_curl"]`. The projection repair engine now uses the same single-exercise share policy as a bounded distribution guard for late set-bump repair only: if a proposed set bump would increase an exercise already over the muscle's max single-exercise share, the bump is blocked, a clean existing compatible alternative may receive the bump, and otherwise the demand stays unresolved with compact `distributionGuardActions` evidence. This guard does not add new muscle demand, change slot ownership, alter seed schema, affect accept gates, feed runtime replay, or touch dashboards. The projection repair engine also applies the same forbidden-slot invariant for clear cross-region repair artifacts: final projected slot plans must not keep primary-target exercises for muscles marked `do_not_train_here` / `forbidden` by `slotPrescriptionIntents`; blocked forbidden repair remains owned by compatible slots or unresolved diagnostics instead of being hidden as valid work in the wrong slot. When forbidden cleanup removes a primary exercise, the projection path no longer runs global weekly-obligation closure just because cleanup happened. It records `forbiddenCleanupReroute` with removed exercises, affected hard muscles, clean compatible reroutes, and unresolved demand; only hard weekly-obligation muscles directly affected by the removal can reroute, and only into compatible allocated owning slots. If the only available path would set-bump concentrated work or add collateral-heavy exercises, the demand stays as an unresolved diagnostic. When a future Rear Delts preselection trial is present, `rearDeltCollateralSummary` distinguishes direct Rear Delts closure from Upper Back collateral, pull concentration, cap trim/removal, and suspicious repair burden so consumed preselection demand cannot be treated as success by itself. This shadow planner reads target tiers, Week 1 hard obligations, support floors, authored slot contracts, and read-only preselection diagnostics only; it does not mutate generated exercises, seed schema, runtime replay, dashboards, progression, or persistence.
- V2 capacity-policy trials remain diagnostic until proven through the candidate pipeline. `plannerOnlyNoRepair.v2SelectionCapacityPlanDiagnostic.capacityBehaviorProjection` measures direct cap-delta evidence from existing selection output only. `plannerOnlyNoRepair.v2CapacityMaterializerProjection` is the stronger read-only follow-up: it rebuilds a projection copy of `SelectionCapacityPlan` and `ExerciseSelectionPlan` for the candidate slot-cap delta, reruns the V2 dry-run materializer against baseline and trial policies, and reports target-slot identity/set changes, compact changed-slot rows from the engine-owned `compareV2MaterializedPlans()` helper, floor-critical lane survival, materializer validity, seed-shape compatibility, duplicate/session-size/five-set gates, and unknown acceptance or over-MAV gates. A trial with zero identity, set, target-slot, blocker, changed-slot, and improvement deltas is explicitly a capacity-track stall signal, not a behavior-promotion candidate. This field is audit-only and must not feed demand, weekly curves, slot allocation, materializer ranking, production generation, repair, accepted seed serialization, runtime replay, receipts, UI, API writes, DB, or persistence.
- Lane-intent materializer trials use the same read-only comparison boundary. `plannerOnlyNoRepair.v2LaneIntentMaterializerProjection` reruns baseline materialization against a projection-only diagnostic override for a scoped lane such as `upper_b:chest_second_exposure`, then compares identity/set/blocker deltas with `compareV2MaterializedPlans()`. The override is accepted only when it declares `readOnly=true`, `affectsScoringOrGeneration=false`, `dryRunOnly=true`, and `reason="read_only_materializer_comparison_trial"`. The V2 debug index carries compact status/delta fields and the `v2-materialization` shard carries detailed readout evidence. This is not a production lane-intent allowlist change and must not feed demand, selection, repair, seed serialization, runtime replay, receipts, UI, API writes, DB, or persistence.
- Set-budget materializer trials use the same quarantine boundary. `plannerOnlyNoRepair.v2SetBudgetMaterializerProjection` selects one representative `set_distribution_budget` row from the repair scoreboard's set-budget gap inventory, applies the suspected lane budget only to an in-memory `V2SetDistributionIntent` copy, rebuilds projection-only selection capacity/selection plans, and reruns the V2 dry-run materializer against baseline versus trial. It reports target-lane identity/set deltas, materializer blocker deltas, changed-slot rows, regression counts, and non-consumption flags. A no-impact result means the representative set-budget gap is diagnostic/no-impact for behavior promotion; any positive delta still remains blocked on cross-week, acceptance, seed/runtime, and production non-consumption gates. This field is audit-only and must not feed demand, weekly curves, slot allocation, canonical set policy, materializer ranking, production generation, repair, accepted seed serialization, runtime replay, receipts, UI, API writes, DB, or persistence.
- Concentration materializer trials use the same quarantine boundary. `plannerOnlyNoRepair.v2ConcentrationMaterializerProjection` selects one representative concentration warning from the exercise-selection diagnostic, applies a one-step lane set-cap trial only to an in-memory `ExerciseSelectionPlan` copy across representative accumulation weeks, and reruns the V2 dry-run materializer against baseline versus trial. It reports identity/set/blocker deltas, concentration warning/share/fatigue proxy deltas, compact changed-slot rows, and non-consumption flags. Its `donorOffsetRedistributionProjection` is the read-only follow-up for `SlotDemandAllocationByWeek`: it chooses same-muscle slot-owned donor-offset lanes from allocation evidence, applies a bounded source -1 / donor +1 allocation policy trial to an in-memory `SlotDemandAllocationByWeek` copy before materialization, rebuilds downstream projection-only class/set/capacity/selection plans, reruns the V2 materializer, and reports source/donor rows, donor capacity before/after, protected coverage impact, identity/set/blocker deltas, concentration warning delta, regression causes, alternate donor candidates, acceptance/trainability status, readiness, blockers, and `nextSafeSlice`. The nested `slotWeekAllocationProjection` is the pure first-principles readiness model: concentration relief is valid only when a slot-owned donor measurably absorbs the required set, weekly volume nets to zero, protected coverage is preserved, and materializer non-regression is measured. Current live evidence on 2026-06-13 proves exact donor absorption across representative accumulation weeks: Weeks 2-4 Calves move `lower_a:calves` 4 to 3 and `lower_b:calves` 4 to 5 with `netWeeklySetDelta=0`, protected coverage preserved, no materializer regression, and no over/under-absorption rows. The bounded production slice is now implemented in pure `SlotDemandAllocationByWeek`; the audit projection treats the promoted 3/5 shape idempotently so it can keep proving non-regression without reapplying the move. The resulting readout remains `slotWeekAllocationReadiness=candidate_for_acceptance_projection`, `blockedRows=0`, and `slotWeekAllocationNextSafeSlice=run_acceptance_non_regression_projection`; the benchmark-owned `slotWeekAllocationAcceptanceProjection` judges the exact slice as `accepted_with_watch_items`, not an acceptance-threshold change, because watch items remain even though non-regression and non-consumption pass. The current watch triage is `accepted=6 boundedOwner=2 ownerFix=0 staleNoise=1 blockers=0`: Week 1 trainability warnings are accepted bounded watches, duplicate/class-family and fatigue/concentration rows are explicitly bounded owner watches, and the lane-preservation shadow ambiguity is diagnostic noise unless it becomes a measured regression. Duplicate/class-family is bounded only when pure V2 has no exact duplicate reuse and no base-plan regression; fatigue/concentration is bounded only when donor absorption, net-zero weekly sets, preserved coverage, and materializer/concentration non-regression are measured. The standalone `base-plan-validation.test.ts` fixture is reconciled to the current V2 base shape (`63` sets, `20` exercises, no optional triceps materialization, one flat-allocation quality warning) and is not a blocking cleanup item. Its `crossWeekReadiness` block source-attributes pure V2 materializer projection rows, no-repair diagnostic selection evidence, repaired projection as evidence-only, and no-repair Week 1 trainability evidence; it then classifies diagnostic rows as `diagnostic_only`, `candidate_for_bounded_policy_design`, `blocked_by_evidence`, or `not_worth_pursuing`. Cross-week coverage is separate from non-regression: measured Weeks 2-4 rows can still block a future trial when protected coverage, net-volume preservation, identity/set/blocker, duplicate, or concentration regressions appear. A positive single-lane warning delta is measured evidence only; the plan-quality benchmark may attribute `fatigue_distribution` to this projection and surface readiness plus missing acceptance gates, but it is not behavior-ready. After this bounded slice, the next safe slice is materializer/readout cleanup for remaining no-impact concentration noise, not generalized slot-allocation policy. This field is audit-only and must not feed demand, weekly curves, canonical slot allocation beyond the explicit pure helper, canonical set policy, materializer ranking, production generation, repair, accepted seed serialization, runtime replay, receipts, UI, API writes, DB, or persistence.
- Calves bounded behavior-promotion implementation: the production policy slice is a pure V2 change in `src/lib/engine/planning/v2/slot-demand-allocation.ts`, owned by `buildV2SlotDemandAllocationByWeek()`, that materializes only the proven Calves lower-slot redistribution after the static slot-exposure allocation is built and before `ExerciseClassDistributionBySlot`, `SetDistributionIntent`, capacity, selection, or materialization consume it. The production gate matches the proven shape exactly: same muscle `Calves`; same lane id `calves`; same source/donor slots `lower_a -> lower_b`; accumulation Weeks 2-4 only; source `-1`; donor `+1`; net weekly set delta `0`; exact downstream baseline targets `4 -> 3` and `4 -> 5`; slot-owned donor row present; protected Calves coverage preserved; no materializer identity, set, blocker, duplicate, concentration, or session-size regression in the read-only proof; `mustFixW1=0`; and seed/runtime/receipt/DB/materializer production non-consumption remains true. The implementation exposes a small policy helper/config inside the slot allocation module rather than consuming audit diagnostics, lane IDs from the materializer, acceptedPlannerIntent, or repair output. The post-change guard remains fail-closed: the live benchmark/read-only audit must keep `accepted=6 boundedOwner=2 ownerFix=0 staleNoise=1 blockers=0` and `benchmark pass=4 warn=4 fail=0 missing=0`. Rollback is removal of the Calves helper/config to restore the prior balanced lower-slot allocation; no migration, reseed, receipt, or runtime rollback is required because the accepted seed remains the only executable truth.
- `preselectionDistributionPolicyByWeek` keeps artifact size down with section-local `limitationCatalog`, `evidenceCatalog`, and `affectsCatalog` refs on Week 1 muscle rows. Resolving those refs restores the same diagnostic evidence, limitations, and affects booleans; the refs are artifact compaction only and do not feed generation, scoring, repair, seed serialization, runtime replay, UI, or persistence.
- Pure V2 planner policy now has an engine-owned boundary under `src/lib/engine/planning/v2/*`. That folder owns the target upper/lower skeleton, a read-only `MesocycleStrategyDiagnostic` above demand, the pure `V2MesocycleStrategyInput` DTO consumed by that diagnostic, first-class `MesocycleDemand`, `WeeklyDemandCurve`, `SlotDemandAllocationByWeek`, `ExerciseClassDistributionBySlot`, slot-lane policy, weekly progression modifiers, deload transform policy, V2 set-distribution builder, read-only V2 support-lane policy, pre-selection `SelectionCapacityPlan`, pre-inventory `ExerciseSelectionPlan`, and the dry-run-only `materialization/` bridge from normalized inventory into seed-shaped slot identities. `src/lib/api/v2-mesocycle-strategy-input-adapter.ts` is the narrow read-only app adapter that can assemble available handoff/profile/review/readiness evidence into that DTO while labeling missing data and keeping historical mesocycles as validation data, not V2 policy targets. The strategy diagnostic reports present/missing input groups, normalized block response evidence, exercise response/tolerance evidence, continuity/variation readiness, volume/fatigue evidence readiness, the current static balanced base demand source, and north-star gaps while keeping phase/objective classification unknown until strategy reasoning exists; it remains `readOnly=true`, `affectsScoringOrGeneration=false` and does not feed selection, repair, seed serialization, runtime replay, receipts, UI, or persistence. It also carries a read-only `V2MesocycleStrategyRecommendation` layer that maps normalized performed-response evidence into non-binding hypotheses such as lagging-muscle protection, late-block volume caps, overlap-fatigue reduction, progression preservation, deload-execution improvement, and narrow accessory rotation; each hypothesis declares future owners it could eventually inform plus explicit `generation`/`selection`/`repair`/`seed`/`runtime`/`receipts` non-consumption guardrails. The sibling `V2StrategyHypothesisPromotionReadiness` layer defines what each hypothesis would still need before behavior: a clear owner, bounded scope, required evidence, known risks, non-regression gates, audit comparison path, rollback criteria, and the next safe read-only action. It is diagnostic-only, remains `readOnly=true` and `affectsScoringOrGeneration=false`, may recommend a read-only diff, and is not consumed by `MesocycleDemand`, materializer ranking, generation, selection, repair, seed serialization, runtime replay, receipts, accepted mesocycles, UI, API, DB, or persistence. Block and exercise response signals are performed-history evidence only: skipped-set trends, hard-week effort, deload execution, under-hit/over-concentrated muscles, exercise progression/stall/regression/skips, and explicit pain/swap/tolerance signals can be summarized for future strategy work, but old prescribed plan shape is excluded from response policy. `MesocycleDemand` now starts from a static balanced upper/lower base policy before slot allocation: it defines per-muscle min/preferred/max effective sets, target exposure counts, default/maintenance/managed-collateral mode, direct-set floors, collateral credit limits, required class intents, and cautions for collateral fatigue drivers such as Front Delts, Glutes, and Lower Back. Skeleton lanes provide coverage/class evidence for that policy instead of being summed as demand. Historical strategy adjustment remains future work and is not consumed by demand. These pure builders derive from the static base policy, V2 skeleton, volume landmarks/target tiers, weekly progression, slot/lane role policy, and normalized exercise inventory before any accepted seed or runtime replay path. `SelectionCapacityPlan` is the pure capacity envelope between class/set/support policy and future exercise selection: it records slot exercise capacity, target session set ranges, lane budgets, per-exercise caps, optional activation rules, cap-aware expansion rules, and clean-alternative requirements without selected identities, no-repair output, repaired projection, audit readouts, or artifact fields. `ExerciseSelectionPlan` translates upstream class, set, support, and capacity policy into lane-level selection requirements, duplicate/class-distinctness rules, direct-floor requirements, clean-alternative timing, continuity policy, and planner-owned `laneSelectionIntent v0` metadata without selected exercise ids/names, candidate inventory, repair/projection output, audit artifacts, seed/runtime data, receipts, UI, or persistence. `materialization/` owns the canonical V2 class taxonomy bridge plus a deterministic dry-run materializer that consumes `ExerciseSelectionPlan`, normalized inventory, constraints, continuity hints, and the guarded Stage B `laneSelectionIntent v0` allowlist for only `vertical_pull_anchor`, chest-biased press support, and `hamstring_curl`; those intents can filter/rank materializer candidates but still do not change seed shape or runtime replay. It emits `dryRunOnly: true` seed-shaped slots with `exerciseId`, `role`, `setCount`, and `laneIds` plus explicit blockers/omissions; it does not serialize to audit artifacts, write `slotPlanSeedJson`, call selection-v2, repair output, seed serialization, runtime replay, receipts, UI, API, or DB paths. `buildV2MaterializationDryRunReport()` wraps the same dry-run-only materializer in a read-only readiness report that records planner policy, taxonomy, inventory, materializer status, seed-shape compatibility counters, stripped non-executable metadata, blockers/omissions, and an executable preview containing only slot id, optional intent, exercise id/name, role, and set count; it always reports `safeToPromoteToProductionWrite=false` and remains outside audit serialization, receipt, UI, DB, repair, and runtime replay paths. `buildV2MaterializationPromotionReadiness()` is the next read-only contract layer over that report plus explicit lane-coverage and production-gate evidence; it separates required materialization blockers, optional omissions, seed-shape failures, production-write gates, runtime replay, receipt, audit, and rollback blockers, and it defaults every production gate to false so materialized dry-run output cannot become eligible by omission. The API-owned `src/lib/api/mesocycle-handoff-v2-materialized-seed.ts` helper may consume those read-only V2 reports only when acceptance preparation explicitly passes `enableV2MaterializedSeedWrite: true`; it fails closed on any blocked readiness and still delegates final seed JSON to `buildMesocycleSlotPlanSeed()` so executable seed truth remains `exerciseId`, `role`, and `setCount`. Helper and probe results carry compact `V2MaterializedSeedAcceptanceProvenance` with source (`v2_disabled`, `v2_blocked_fail_closed`, or `v2_materialized_seed`), dry-run/readiness versions, mapped gate booleans, blocker categories only, serializer identity, `dbWriteOccurred=false`, and the invariant that runtime consumes only `slotPlanSeedJson.slots[].exercises[{ exerciseId, role, setCount }]`; provenance must not embed lane ids, blocker/omission bulk, inventory evidence, dry-run debug payloads, or executable previews. Transaction-level `AcceptedSeedPersistenceProvenance` is separate API-owned persistence evidence: it can report `legacy_projection_seed`, `v2_disabled`, `v2_blocked_fail_closed`, or `v2_materialized_seed`, marks source selection before the transaction, and sets `dbWriteOccurred=true` only after the existing acceptance transaction persists `slotPlanSeedJson`. The same API seam exposes `buildV2MaterializedSeedAcceptanceProbe()` as a read-only acceptance probe: it calls the helper only with opt-in disabled, reports dry-run/promotion readiness and caller-owned evidence, labels all all-gates-provided checks as `simulated_opt_in_readiness`, and always reports `safeToPromoteToProductionWrite=false`. `src/lib/audit/workout-audit/v2-materialization-live-context-dry-run.ts` is the isolated read-only bridge that can load owner/mesocycle context and live normalized exercise inventory for the same dry-run report and acceptance probe; it does not write seeds, serialize accepted seed contracts, alter audit artifacts, or feed runtime replay. `buildV2AcceptedPlannerIntentDto()` is the read-only seed-safe contract adapter over the same pure planner policy; it keeps only compact future seed metadata candidates such as skeleton identity, week phases, slot/lane ids, target tiers, class intent, set budgets, support floors, collateral limits, per-exercise caps, concentration thresholds, duplicate/clean-alternative/optional/continuity markers, and deload transform summary. It intentionally omits raw guardrails, evidence/source arrays beyond the top-level policy source, diagnostics/readouts, selected exercises, audit artifacts, seed serialization, runtime replay fields, receipts, UI display DTOs, and persistence. The support-lane policy gives Triceps, Side Delts, Rear Delts, and Biceps explicit direct-floor, collateral-credit, optional-activation, expansion, and tier-aware concentration semantics; it marks collateral as supplemental only and keeps direct floors unsatisfied by collateral. Planning-reality and workout-audit modules may adapt or serialize those policies, but diagnostic readouts such as target-vs-no-repair diffs, cross-week gates, repair materiality, warnings/blockers, sidecar manifests, and readout suppression stay outside the engine policy boundary and do not feed accepted seeds or runtime replay.
- Fresh V2 base-plan materialization ranks deterministic candidate identity by class/directness, then lane intent, direct stimulus-to-fatigue, and stable hypertrophy anchors before favorites or name tie-breakers. Exact continuity can outrank fresh intent only when the materialization input explicitly opts into identity preservation. Anchor quality is taxonomy-owned: chest anchors require press-like loadable/stable press eligibility with meaningful Chest stimulus when explicit stimulus data is present; chest fly lanes can see direct fly-style options such as pec deck and cable crossover; squat anchors prefer loadable squat, leg-press, hack, smith, front/high-bar, and belt-squat style options before support fallbacks; hinge anchors prefer true hamstring-biased RDL/SLDL/deadlift/good-morning variants before low-axial hip-extension fallbacks; vertical-pull lanes remain restricted to pulldown, pull-up, and chin-up variants and exclude pullover/straight-arm pulldown patterns; and rows prefer stable loadable row variants. Quad-isolation/leg-extension lanes no longer collapse into generic squat-pattern matches. Base-plan validation reports blockers when fallback/accessory anchors are selected despite visible ideal alternatives and warnings when fallback is the only visible option. These rules live only in `src/lib/engine/planning/v2/materialization` and do not change seed serialization, runtime replay, Program/Home/UI, repair, persistence, or DB state.
- Strategy B demand-zone learning is future strategy input only, not current-seed mutation. `V2MesocycleStrategyDiagnostic.demandZoneLearning` classifies performed evidence into floor protection, productive monitoring, stretch monitoring, and cap/redistribution signals before any demand promotion. `V2MesocycleStrategyDiagnostic.strategyToDemandDiff` labels future demand actions by owner seam (`MesocycleDemand`, `WeeklyDemandCurve`, `SlotDemandAllocation`, or `SetDistributionIntent`) and readiness without mutating those seams. `V2PlannerMesocyclePolicy.strategyToDemandProjection` then joins that diff to the current static `MesocycleDemand` as a read-only, non-mutating projection: projected ranges equal current demand, non-mutation gates pass, and `measuredCurrentNonRegressionSummary` proves only the current no-mutation projection has zero range and net-volume deltas. Its `candidateInventory` is the compact owner-labeled bridge for the next true planner-intelligence slice: rows preserve source attribution, classify evidence as performed reality, benchmark watch, no-repair projection, or repair-only, name the proposed future owner seam, readiness, and required proof before behavior, and keep `consumedByDemandOrMaterializer=false`. Its `boundedBehaviorTrial` is still diagnostic-only: it can frame a row-level static demand delta such as a one-set floor buffer, read compact `V2SlotOwnedDemandAdjustmentPlan` context to distinguish missing redistribution ownership from a context-available trial, and inventory existing `WeeklyDemandCurve`, `SlotDemandAllocationByWeek`, and `V2SetDistributionIntent` context for trial candidates. That downstream inventory only answers whether existing pure policy objects can observe the candidate; it does not choose donor offsets, preserve net-new volume, rerun materializer ranking, measure materializer non-regression, or prove behavior safety. The measured redistribution blocker summary explicitly counts unmeasured gates such as `materializerNonRegression` so a projection with unknown materializer evidence cannot be mistaken for behavior-ready. Floor misses and repeated thin-floor margins are trainability/protection evidence; productive-zone misses are monitoring evidence unless they become recurring target-tier under-hit; stretch-target misses are not default demand increases; cap/MAV pressure, high single-exercise share, skipped-set trends, and hard-week effort are fatigue/redistribution evidence. Performed reality may enter future `MesocycleDemand`, `WeeklyDemandCurve`, or `SlotDemandAllocationByWeek` only through read-only strategy diagnostics, promotion readiness, read-only diffs/projections, measured non-regression gates, and an explicit behavior slice. It must not feed materializer ranking, repair, accepted seed serialization, runtime replay, receipts, UI, API writes, or the current accepted candidate by implication.
- V2 accepted-seed preparation probes now also expose compact gate results for base-plan validation, materializer status, seed-shape compatibility, required-lane coverage, no required blockers, promotion readiness, production gates, provenance, runtime replay, and fallback policy. `prepareV2AcceptedSeedPreparationProbe()` derives the real stored handoff draft/design slot sequence and then calls the read-only probe without invoking legacy slot-plan projection, repair, support-floor closure, weekly-obligation closure, late set bumping, cap trim, duplicate cleanup, or dirty collateral cleanup. The probe may validate a seed-shaped preview only by calling `buildMesocycleSlotPlanSeed()` and returns serializer counts/boundary facts instead of exposing `slotPlanSeedJson`; helper/preparation provenance stays separate from transaction persistence provenance and always reports no DB write. `prepareV2AcceptedSeedPreparationCompare()` is the adjacent disabled-by-default read-only comparison: it can prepare the current legacy projection seed as baseline evidence, prepare a V2 serialized preview from the materialization probe without routing V2 through legacy projection or repair, and compare seed shape, exercise identities, class/lane coverage, repair dependency, and no-write provenance. The compare is not consumed by production, does not enter the acceptance transaction, does not persist anything, and does not change runtime replay, receipts, repaired projection, repair, or seed serialization. The `v2-accepted-seed-prepare-compare` workout-audit mode is the operator surface for this helper; it resolves a live handoff candidate, writes a compact read-only artifact section, and reports `v2ProductionWriteEligible=true` only when the read-only V2 preview is available and every production gate is satisfied.
- Pure V2 `SlotDemandAllocationByWeek` now reports static base exposure ownership through `exposureOwnershipPolicy` and lane-level `demandShare` / `classIntent` rows. It consumes the balanced `MesocycleDemand` and `WeeklyDemandCurve` targets rather than summing skeleton lane occurrences: Chest splits across `upper_a` and `upper_b` with distinct class intent, Quads split Lower A primary plus loadable Lower B support, Hamstrings conserve one balanced target across hinge and knee-flexion curl ownership, Calves distribute across lower slots, Side Delts split direct support ownership across both upper slots, Biceps/Rear Delts/Triceps get direct support ownership, and Front Delts get bounded vertical-press pattern support while still remaining managed-collateral demand. Glutes and Lower Back stay zero-demand managed-collateral markers. `buildV2SlotWeekDonorCapacityProjection()` is the read-only projection over this seam for concentration donor-capacity design: it reports protected weekly demand, source lane pressure, eligible donor slots, measured donor absorption/headroom, protected coverage impact, materializer non-regression status, readiness, and next safe slice. `buildV2SlotWeekAllocationPolicyTrial()` is the bounded pure trial sibling: it clones `SlotDemandAllocationByWeek`, applies one same-muscle source -1 / donor +1 movement to slot-owned allocation rows, reports donor capacity/headroom before and after, marks the trial as non-production/read-only, and leaves the canonical allocation unchanged. This remains pure/read-only (`readOnly=true`, `affectsScoringOrGeneration=false`) and does not feed generation, selection, repair, seed serialization, runtime replay, receipts, UI, API, DB, or persistence.
- Pure V2 `ExerciseClassDistributionBySlot` consumes those allocation ownership rows directly instead of re-reading loose skeleton lanes as class demand. Class lanes now carry the source `classIntent`, `demandShare`, `ownershipKind`, owning slot/lane, allocated target range, and explicit lane kind (`owned_class_lane`, `support_class_lane`, `optional_recoverable_lane`, or `managed_collateral_marker`). The sibling `SetDistributionIntent` consumes those class lanes plus slot capacity and support-lane policy to plan conservative lane set budgets from ownership, not raw skeleton lane targets: owned/required support lanes get capped direct budgets, support direct floors are planned before repair, optional lanes stay zero until activation, and managed collateral rows stay zero direct-set budget. The static balanced base uses role-sensitive preferred budgets rather than making every lane 4 sets: Chest clears the hard floor with a 4-set upper-A anchor, 3-set upper-B second exposure, and 3-set chest-biased upper-B press support; Side Delts keep two direct isolation exposures; Rear Delts keep a direct floor; Lower B knee-flexion curl stays at 3 sets; and Calves clear the hard floor through 4-set direct support on both lower slots. Glutes and Lower Back remain visible for fatigue/collateral accounting but do not become required exercise-class lanes; optional rows stay non-required unless a later activation policy proves the need. This is still pure/read-only and does not feed live generation, selection, repair, seed serialization, runtime replay, receipts, UI, API, DB, audit serialization, or persistence.
- Pure V2 `buildV2BasePlanValidation()` is a read-only diagnostic over the dry-run materialized base plan. It compares the materialized representative upper/lower week against the balanced static `MesocycleDemand`, class/set/selection policy, taxonomy, normalized inventory, and deload transform policy, then reports compact muscle coverage, slot shape, class coverage, set quality, duplicate/distinctness, vertical-press alignment, deload compatibility, and coach-quality warnings. The aligned static base policy now expects recoverable vertical-press or shoulder-press exposure, two direct side-delt isolation exposures, at least 8 direct hamstring sets from hinge plus curls, and loadable Lower B quad-support quality when inventory allows it. It warns when those pattern-quality expectations are missing, treats glutes as managed collateral with optional recoverable direct work only, disallows standalone one-set hypertrophy exercises by default, accepts same calf reuse when no clean variant exists, and keeps flat allocation as a warning-only smell that clears only when the materialized base is actually role-sensitive. It remains `readOnly=true`, `affectsScoringOrGeneration=false`, and does not feed generation, selection-v2, repair, repaired projection, seed serialization, runtime replay, receipts, UI, API, DB, or persistence.
- Pure V2 `buildV2BasePlanCompare()` is the read-only base-plan comparison over the clean materialized V2 base plan, planner-only no-repair output, and repaired projection evidence. It treats the V2 base plan as the static candidate north-star, the repaired projection as evidence rather than target policy, and the no-repair output as current planner behavior before repair. It classifies differences as `v2_improves`, `v2_preserves`, `v2_regresses`, `unclear`, or `not_comparable` across slot shape, muscle coverage, class coverage, repair dependencies, exercise identity/materializer quality, and deload readiness. It reports `readOnly=true`, `affectsScoringOrGeneration=false`, and explicit non-consumption guardrails for generation, selection-v2, repair, seed serialization, runtime replay, receipts, and materializer/demand consumption.
- Pure V2 `buildV2BasePlanShadowConsumptionTrial()` is the read-only companion diagnostic over the same clean base-plan materialization. It adapts the V2 base plan into the projection-comparison view, reports `consumedByProduction=false`, compares shadow/V2/no-repair/repaired set totals, classifies slot shape, muscle coverage, class coverage, repair dependency, exercise identity, and deload readiness with the same `v2_improves` / `v2_preserves` / `v2_regresses` / `unclear` / `not_comparable` labels, and records a diagnostic repair-dependency delta. It does not rerun production projection, persist V2 output, alter repair, write seeds, feed runtime replay, or affect receipts.
- `V2StrategyHypothesisPromotionDiff` is the read-only audit gate after promotion readiness. It currently evaluates only `protect_lagging_muscles_earlier` and `cap_late_block_volume` when they are ready for read-only diff, uses target-tier under-hit evidence for lagging-muscle protection, uses skipped-set plus hard-week evidence for late-block cap analysis, reports interaction risk, and keeps `readOnly=true`, `affectsScoringOrGeneration=false`, and `consumedByDemandOrMaterializer=false`. Its nested `V2StrategyHypothesisProjectionDiff` is the conservative combined-pair shadow frame: it prefers redistribution from over-concentrated or fatigue-driver muscles before net-new late-block volume, derives candidate protected muscles only from target-tier lagging evidence, derives candidate donor muscles only from over-concentration/fatigue evidence, and computes pass/fail/unknown non-regression gates from measured before/after deltas when a flagged planner-only no-repair shadow projection is available. Before constructing that shadow override, `preShadowCandidateFilter` reads base planner-only no-repair coverage and keeps only donor/protected exchanges with measurable surplus floor margin, stricter target-tier donor evidence, compatible slot ownership, no overloaded receiving slot, no net-new volume, zero max slot increase, and no pre-rerun concentration-risk flag; unknown floor margin excludes the donor. If filtering leaves no safe donor/protected material, that is a valid diagnostic result and readiness stays `needs_better_projection`. The projection diff also carries a read-only `conflictAwareRefinement` that reports protected/donor overlap, target-floor preservation conflicts, missing slot owners, session-size cap conflicts, and blocked net-new volume before any behavior readiness can be inferred. The sibling `V2DonorSurplusEvidence` normalizes candidate donor reason, measured baseline coverage, floor/preferred set landmarks when available, protected overlap, slot ownership compatibility, and eligibility confidence before the slot-owned plan reads it. The sibling `V2SlotOwnedDemandAdjustmentPlan` defines the intended upstream policy directly before projection: protected demand comes only from target-tier under-hit evidence, donor demand only from over-concentration/fatigue evidence, protected/donor overlap or unknown surplus margin blocks donor eligibility unless safe surplus is proven, net-new volume and slot increases default to zero, and every adjustment requires slot ownership, floor preservation, and priority coverage preservation. A blocked plan is valid and means do not repair around it; it is serialized as compact counts/status in the main artifact and index while full rows stay in `v2-promotion-diffs`. The refinement may exclude unsafe donor muscles diagnostically, requires redistribution inside the existing slot budget, allows no net-new volume, and keeps all fields `readOnly=true` / `affectsScoringOrGeneration=false`. The shadow rerun compares the base planner-only no-repair projection against a candidate planner-only no-repair projection; repaired projection and old prescribed plan shape are not targets. Gates pass or fail only from measured deltas, and unmeasured fields remain `unknown`. `ready_for_bounded_behavior_trial` is allowed only when every necessary computed gate is measured and passes and the conflict-aware refinement has no measured conflicts. It must not feed `MesocycleDemand`, `WeeklyDemandCurve`, `SlotDemandAllocation`, materializer ranking, generation, selection, repair, seed serialization, runtime replay, receipts, accepted mesocycles, UI, API, DB, or persistence.
- `weeklyDemandCurve` keeps the same diagnostic meaning in a compact artifact shape with section-local `sourceCatalog`, `limitationCatalog`, and `muscleCatalog` refs. Resolving a muscle row through those catalogs plus the week-level limitations restores muscle identity, target tier/status, role, targets, Week 1 evidence, progression intent, sources, and limitations; the refs are artifact compaction only and do not feed generation, scoring, repair, seed serialization, runtime replay, UI, or persistence.
- `mesocycle-explain` serialization applies an additional readout-only compaction pass to large `planningReality` sections. The in-memory projection diagnostic remains full for operator summaries and tests, while the written artifact may use section-local catalogs, representative rows, omitted counts, and summary/top-failure rows for `slotPrescriptionIntents`, `setDistributionIntents`, `preselectionFeasibility`, `repairMaterialityAfterShadowAllocation`, `repairMateriality`, `exerciseClassDistributionBySlot`, `exerciseClassAlignment`, `accumulationWeekProjection`, and flagged `plannerOnlyDryRun`. Flagged `plannerOnlyNoRepair` is a separate audit-only diagnostic that reruns projection with downstream repair/shaping disabled and reports missing lanes, unresolved demand, validation failures, severity-bucketed concentration rows, repaired comparison, `acceptanceClassification`, and V2 planner diagnostics without feeding accepted seed/runtime behavior. The main `mesocycle-explain` artifact keeps only the operator no-repair summary, replacement readiness, compact V2 summary, compact `v2BasePlanCompare` status/counts, compact `v2BasePlanShadowConsumptionTrial` status/counts/delta, compact `crossWeekProjectionGate` statuses/counts, compact `repairPromotionScoreboard` counts/interpretation, compact `v2SupportLaneProjectionDiagnostic` summary/counts, top findings, and optional debug-artifact manifest. When `--v2-debug-artifact` is enabled, that manifest points to a compact `v2_debug_index` plus focused shards for strategy, promotion readiness, promotion diffs, repair evidence, materialization summary, cross-week projection, and selection alignment; detailed base-plan compare and shadow-consumption rows live in `v2-materialization`, not the main artifact. The index records shard metadata, relative paths, hashes, byte sizes, statuses, detail level, and explicit budgets; default shard detail is `compact`, while full detail is reserved for explicit future opt-in and is not emitted by the current CLI flag. `plannerOnlyNoRepair.v2BasePlanShadowConsumptionTrial` adapts the clean V2 base plan into the projection-comparison plan view in read-only shadow mode and reports what would change if production projection consumed that shape; it always reports `consumedByProduction=false`, `readOnly=true`, and `affectsScoringOrGeneration=false`, and its repair dependency delta is diagnostic evidence rather than a measured behavior change. `repairPromotionScoreboard` is readout-only: it preserves raw repaired-planning repair counts and rows as evidence, then classifies only positive slot-owned likely-avoidable rows as promotion candidates while keeping suspicious, cap-trim/removal, forbidden cleanup, collateral, and materiality-none denominator rows visible as safety or diagnostic evidence. Its `interpretation` block separates raw legacy repair pressure from current V2 policy gaps, safety/non-regression rows, stale repaired-projection artifacts, quarantine groups, and missing proof before behavior; this split is derived from existing V2 diagnostics and does not reclassify raw repair counts. `plannerOnlyNoRepair.crossWeekProjectionGate` separates Week 1 basic no-repair shape from accumulation Weeks 2-4 planner-owned read-only projection, deload diagnostic projection, and broad replacement readiness; it is read-only, never marks `safeToPromoteBehavior=true`, and treats the Weeks 2-4 projection as limited until selection, accepted seed, and runtime replay consume it. `v2MesocyclePlan` records the stable upper/lower 4x skeleton, Week 1 lane evidence, Weeks 1-5 progression modifiers, deload transform intent, validation-rule statuses, and replacement-readiness blockers as read-only planner context only. `plannerOnlyNoRepair.v2SetDistributionIntent` is a pure planner-owned V2 set-budget diagnostic derived from balanced demand, slot ownership rows, exercise class ownership rows, support floors, and slot/session capacity; it exposes lane min/preferred/max set budgets separately from cap and concentration policy, ignores skeleton lane budgets/no-repair/repaired/seed/runtime output as policy input, and does not feed scoring, generation, repair, seed serialization, runtime replay, UI, receipts, or persistence. `plannerOnlyNoRepair.v2SupportLanePolicy` is the sibling pure support-lane policy for B-support muscles; the main artifact exposes only its read-only summary while the debug index can point to compact shard summaries instead of carrying large policy arrays in the main artifact. `plannerOnlyNoRepair.v2SupportLaneProjectionDiagnostic` applies that pure policy to the current planner-owned Weeks 1-4 no-repair projection, keeps collateral credit separate from direct-floor satisfaction, labels optional Triceps and delt second-exposure rows as diagnostic-only, reports expansion recoverability limitations, and always keeps `safeForBehaviorPromotion=false`. `plannerOnlyNoRepair.plannerOwnedAccumulationProjection` derives Weeks 2-4 weekly muscle demand and slot/lane set budgets from planner policy sources without using repaired projection, accepted seed, runtime replay, repair output, cleanup, or post-hoc set bumps as targets. `plannerOnlyNoRepair.v2TargetVsNoRepairDiff` compares that V2 target skeleton against the current experimental no-repair output; repaired projection evidence is secondary and is used only to label repair-dependent lanes or legacy rescue behavior, not as the target. Its lane classification consumes `v2SetDistributionIntent` as read-only evidence and emits compact `setPolicy:*`, `setBudget:*`, `justification:*`, `selectionFeasibility:*`, `capacityPressure:*`, `capAwareExpansion:*`, and `expansionStatus:*` strings so in-budget, allowed-expansion, warning, justification-needed, hard-blocker, session-cap pressure, stale Week 1 artifacts, and future cap-aware expansion limitations stay distinct without duplicating policy objects per lane. `acceptanceClassification.basicMesocycleShapeStatus` separates basic Week 1 no-repair shape from `replacementReadinessStatus`; raw unresolved/missing/validation counts and migration scoreboards do not hard-fail basic shape by themselves. Its concentration buckets are `acceptanceFailures`, `qualityWarnings`, `diagnosticRows`, and `ignoredRows`; only acceptance failures participate in hard-blocker classification. Required summaries, suspicious repairs, unresolved demand/failure reasons, forbidden status, weak preselection status, distribution guard actions, cleanup feasibility, and calves candidate blockers remain visible; the compact serialized shape and debug index/shards do not feed generation, scoring, repair, seed serialization, runtime replay, UI, receipts, or accepted mesocycle behavior.
- The main artifact may expose only compact promotion-diff and projection-diff counts/status/readiness/gate-count/conflict-count fields plus compact donor surplus evidence status/counts, measured-margin count, top reason counts, and compact pre-shadow filter status and eligible/excluded counts. Detailed `V2StrategyHypothesisPromotionDiff`, `V2DonorSurplusEvidence`, and `V2StrategyHypothesisProjectionDiff` arrays, donor baseline/floor/preferred/surplus rows, pre-shadow donor/protected eligibility rows, target-tier examples, skipped-set evidence, candidate protected/donor muscle lists, conflict reasons, donor-resolution reasons, projected-delta notes, computed gate detail, interaction risks, and guard lists belong in the focused `v2-promotion-diffs` shard.
- Current pure V2 set-distribution policy derives `plannerOnlyNoRepair.v2SetDistributionIntent` from balanced demand, slot allocation ownership rows, exercise class lane ownership, support floors, and slot/session capacity. Its static base policy keeps primary anchors in the 3-4 set range, secondary/support compounds in the 2-3 set range when floor coverage is already protected, direct support isolation in the 2-4 set range according to direct-floor policy, optional lanes at 0, and managed collateral at 0 direct sets. It does not use repaired projection, no-repair output, old prescribed plans, accepted seeds, runtime replay, receipts, or historical strategy recommendations as policy input.
- `slotDemandAllocationByWeek` is the read-only diagnostic layer between `weeklyDemandCurve` and future per-week preselection policy. It allocates Week 1 demand to slots from existing shadow allocation/current projection evidence, marks Weeks 2-4 as not allocated when per-week slot composition, fatigue carryover, progression-adjusted targets, duplicate justification, or weekly exercise identity policy are missing, and marks deload allocation as unprojected when identity preservation, set-reduction projection, and hard/support target adjustment are unavailable. It must stay diagnostic-only and must not feed scoring, repair, seed serialization, runtime replay, or accepted mesocycle behavior.
- `plannerOnlyNoRepair.v2ExerciseSelectionPlanDiagnostic` is a flagged read-only V2 debug diagnostic. It evaluates Week 1 no-repair selected identities/class lanes against `plannerOwnedAccumulationProjection` for Weeks 2-4 using existing class, duplicate, concentration, inventory, fatigue, and capacity evidence only. Lower B keeps `hinge_compound` narrow for RDL/SLDL/deadlift-like hinges, and may separately report Glute Bridge / hip-thrust-like anchors as `low_axial_hip_extension_anchor` when that explicit V2 policy class is allowed. Generic `hinge` still does not satisfy `hinge_compound`, and the knee-flexion curl lane remains separate required Lower B policy. `plannerOnlyNoRepair.lowAxialHipExtensionLimitation` is the companion read-only limitation readout: it reports that low-axial hip extension is glute-biased, lower hamstring-per-set than true hinge compounds, not equivalent to `hinge_compound`, and acceptable only when the Lower B knee-flexion curl floor, weekly Hamstrings target, and low-axial fatigue-management conditions are met; it also reports curl-vs-hip-extension contribution, true-hinge exposure count, and diagnostic-only Week 3-4 expansion guidance. The diagnostic must not reselect exercises, choose replacements, use repaired projection as the target, or feed production selection, repair, seed serialization, runtime replay, UI, receipts, persistence, or normal unflagged output.
- `plannerOnlyNoRepair.v2SelectionCapacityPlanDiagnostic` remains a flagged audit-side evaluator, not production policy. It applies current no-repair/diagnostic evidence around the pure `SelectionCapacityPlan` envelope and reports per-week slot exercise capacity, target-session set headroom, per-exercise cap headroom, weekly target status, clean-alternative visibility, optional-lane suppression, and lane classifications such as `capacity_pressure` and `cap_aware_expansion_needed`. It distinguishes target-met upper-pull session-cap pressure and Week 4 calf preferred-over-cap limitations from true set-budget blockers. Its projection-only capacity trial design and behavior projection may measure direct slot-cap-delta effects such as headroom relief, unresolved floor rows, session-size regressions, and unknown materializer/acceptance gates, but they still keep `safeForBehaviorPromotion=false` without feeding selection, repair, seed serialization, runtime replay, receipts, UI, persistence, or normal unflagged output.
- `plannerOnlyNoRepair.v2DeloadProjectionDiagnostic` is a flagged read-only V2 debug diagnostic for Week 5 only. It preserves Week 1 selected identities where evidence exists, applies the V2 deload transform and set-distribution intent to project roughly 40-60% set reduction with `targetRir="4-5"`, introduces no new movements, and reports integer-rounding limitations or missing identity evidence without feeding runtime deload generation, repair, accepted seed serialization, replay, UI, receipts, persistence, or normal unflagged output. The cross-week gate may replace `deload_seed_runtime_projection_missing` with `deload_projection_read_only_not_consumed` when this diagnostic is complete enough, but `safeToPromoteBehavior` remains `false`.
- `exerciseClassDistributionBySlot` is the read-only diagnostic layer between `slotDemandAllocationByWeek` and future selection objectives. It derives class-level intent from existing planningReality rows only, including upper-slot Chest distinct class pressure, lower_b Hamstrings hinge-plus-knee-flexion-curl intent, low-collateral Side Delt work, Rear Delts/Triceps collateral cautions, Calves duplicate-isolation caution, and duplicate exercise justification requirements for repeated key lifts. It must stay diagnostic-only and must not feed scoring, repair, seed serialization, runtime replay, receipts, UI, or accepted mesocycle behavior.
- `exerciseClassAlignment` is the read-only diagnostic layer after `exerciseClassDistributionBySlot`. It compares planner class intent with initial slot-selected exercise classes and final repaired exercise classes, flags whether repair improved or worsened class alignment, identifies identity churn, and keeps duplicate-policy warnings visible for repeated Incline DB Bench, Lat Pulldown, SLDL, Barbell Back Squat, and same-session calf isolation variants. The sibling `exerciseClassUnresolvedCauses` classifies unresolved or otherwise notable class-alignment rows by likely owner, such as selection blind spot, inventory/classification gap, slot capacity, duplicate/continuity conflict, support-floor late repair, final shaping/cap cleanup, repair identity churn, true unresolved demand, or diagnostic-only/not-actionable. `duplicateContinuityJustification` is the compact read-only follow-up for those duplicate/continuity rows: it lists the duplicated exercise or same-session variant, involved slots, inferred class/pattern, visible clean alternatives, justification, risk, and future policy recommendation. These fields are derived only from existing `planningReality` rows and snapshots and must not feed generation, selection, scoring, repair, seed persistence, receipts, runtime replay, UI, or accepted mesocycle behavior.
- `cleanupCandidateFeasibility` is the read-only pre-trial gate for duplicate-cleanup candidates. The first row evaluates `lower_b_calf_duplicate_cleanup`: when same-session lower_b calf variants exist, it compares the current calf exercises, Calves support floor, projected weekly Calves delivery outside lower_b, and current set/direct-exercise/slot caps to decide whether one retained calf isolation can preserve the floor. `not_feasible_under_current_caps` means future cleanup behavior should not be trialed; the issue is support-floor/set-distribution policy, lower_a allocation, or specialization allowance, not duplicate cleanup alone. This field must not feed generation, selection, scoring, repair, seed persistence, receipts, runtime replay, UI, or accepted mesocycle behavior.
- `accumulationWeekProjection` is the read-only diagnostic layer after `slotDemandAllocationByWeek`. It repeats the current Week 1 final slot-plan shape across later accumulation/peak weeks only to expose persistent under-target, over-target, duplicate-reuse, concentration, and collateral-fatigue risks. Its basis must explicitly say `repeat_week_1_final_shape` and list missing progression, per-week slot-distribution, fatigue carryover, exercise-staleness, and deload identity/set-reduction policy. It can mark Chest upper-slot distinct exercise distribution as ready for a bounded trial only when repeated-shape evidence shows Chest remains under target and Chest exercise concentration/duplicate pressure persists; Hamstrings overdelivery control stays not-first, Side Delts support stays diagnostic-only, broad duplicate suppression needs more projection, and calf cleanup stays lower priority. It must not affect scoring, generation, repair, seed serialization, runtime replay, accepted mesocycles, dashboards, or deload policy.
- A bounded `lower_b` Hamstrings repair trial allows projected repair candidate selection to prefer an unused lower-compatible knee-flexion curl before dirty Back Extension-style closure, while preserving hinge-anchor identity, week-wide duplicate filtering, exercise caps, distribution guards, seed schema, and seeded runtime replay.
- Program-quality constraints are a projection-only shaping layer in `src/lib/api/mesocycle-handoff-slot-plan-projection.program-quality.ts`, applied after slot-local exercise selection and again after obligation/support repair before seed serialization. The layer uses additive monotonic soft penalties with fixed priority order: P0 weekly hard target floors, required slot identity anchors, and meaningful primary-slot contribution; P1 weekly movement-pattern balance; P2 per-exercise efficiency/session main-compound count; P3 stimulus diversity and spread; P4 duplicate/redundancy penalties; and P5 deficit-driven isolation completeness. P0 preservation is weekly-level: redistribution may move sets across exercises or compatible slots when hard weekly target floors remain satisfied, required anchors remain present, hard caps are respected, and the authored slot's primary muscles still keep a meaningful floor (2 effective sets, capped by any lower authored allocation). Before allowing a projected exercise to remain above the soft set caps (main 4, accessory 3), P2 must attempt deterministic redistribution in order: existing compatible exercise in the same slot, existing compatible exercise in the paired slot (`upper_a`/`upper_b`, `lower_a`/`lower_b`), existing compatible exercise elsewhere in the week, then a compatible added alternative when slot capacity allows. Stacking is allowed only when diagnostics emit an explicit `blockReason` (`would_break_weekly_target`, `would_break_slot_identity`, `no_compatible_alternative`, `would_exceed_hard_cap`, or `would_worsen_fatigue`). Lower-pair shaping may reduce avoidable duplicate squat support, shift hinge-dominant hamstring excess toward knee-flexion work, and preserve lower_b hinge identity at the minimum contribution floor. Deficit-driven isolation may replace redundant Tier A work with Biceps, Triceps, Side Delts, or Rear Delts support when Tier A floors are already satisfied and the Tier B muscle remains below MEV. It must not change `slotPlanSeedJson` schema, runtime seeded replay, route behavior, receipts, or persistence semantics.
- Accessory Lane v1 is a bounded projection-time completeness pass in `src/lib/planning/accessory-lane.ts`, called only from `src/lib/api/mesocycle-handoff-slot-plan-projection.ts` after main slot selection and before minimal seed mapping. It may append at most one low-fatigue accessory per slot and at most two per projected week for Core, Adductors, Abductors, or Forearms when the slot has room under `SESSION_CAPS.maxExercises`; it does not change runtime seed parsing ownership, receipts, manual edits, or primary slot identity.
- `MANUAL` selection mode bypasses mesocycle continuity enforcement by design; continuity pinning only applies to auto/intention-generated sessions.
- Continuity sourcing for intent generation is owned by `buildSelectionObjective()` in `src/lib/api/template-session/selection-adapter.ts`: it now reads continuity scope from the resolved slot contract rather than inferring repeated-slot meaning locally. When the resolved current session is slot-scoped and has a persisted runtime `slotId`, continuity prefers the most recent performed workout for that same slot; otherwise it falls back to same-intent history. For repeated same-intent slots, current-week same-intent workouts must not become fallback continuity favorites for a later slot in that same week, and Phase 2 narrows compound continuity carryover to compounds that are still allowed by the canonical slot-policy lane contract.
- Final composition cleanup for intent generation remains in `composeIntentSessionFromMappedContext()` in `src/lib/api/template-session.ts` after selection and closure. That post-closure pass may trim redundant accessory overlap, suppress front-delt isolation already covered by pressing, cap accessory hinge stacking, and keep a conservative slot-aware directional backstop for repeated same-intent slots when doing so does not break session minimum structure. Phase 2 keeps `template-session.ts` in a closure/guardrail role only: slot-policy lane ownership is canonical upstream, core-role subordination is owned in the role-budgeting seam, and closure must not reintroduce out-of-lane compounds while viable in-lane options exist.
- For `INTENT` generation, active mesocycle roles are continuity anchors, not full workout cages. Role fixtures are budgeted first, but complete role-list sessions may still supplement from opportunity-compatible inventory when deficits or minimum composition constraints remain unresolved.
- When a mesocycle role exists, section/main-accessory mapping is role-driven:
  - `CORE_COMPOUND -> MAIN`
  - `ACCESSORY -> ACCESSORY`
  - Exercise metadata defaults are only used when no mesocycle role exists.
- Intent role-list completeness is server-owned: a role list is complete iff the current mesocycle has at least one `CORE_COMPOUND` and at least one `ACCESSORY` role for that intent; client `roleListIncomplete=false` is ignored, while `roleListIncomplete=true` can force incomplete-mode reselection.
- Role continuity set floors are lifecycle-budget constrained in accumulation weeks: continuity progression cannot exceed lifecycle weekly muscle targets or the peak-accumulation MAV cap for the configured mesocycle length unless prior-week continuity floors already exceed those caps (no mid-mesocycle reduction in that case).
- `CORE_COMPOUND` role exercises are hard-capped at `MAIN_LIFT_MAX_WORKING_SETS = 5` working sets in role-budgeting logic (`src/lib/api/template-session/role-budgeting.ts`). This cap fires after the continuity ramp, preventing main-lift working-set accumulation from exceeding prescription.
- `MANUAL` sessions are ingested into progression with confidence discounting and anomaly-aware downgrades (see MANUAL Session Contract below) rather than treated as equal-signal to `INTENT` by default.

## Session opportunity and inventory model
- `SessionOpportunityDefinition` in `src/lib/planning/session-opportunities.ts` is the canonical source for current intent/session semantics.
- Each supported session intent (`push`, `pull`, `legs`, `upper`, `lower`, `full_body`, `body_part`) defines:
  - alignment and structural character
  - current-session muscle opportunity weights
  - future-slot opportunity weights for remaining-week planning
  - inventory eligibility for `standard`, `closure`, and `rescue`
  - anchor policy for role fixtures
- This replaces the older pattern where intent alignment, muscle ownership, and future-slot opportunity lived in separate hard-coded maps across filtering and planning modules.
- `body_part` is the first intent with materially different inventory layers:
  - `standard` inventory prefers direct target-muscle matches
  - `closure` and `rescue` inventory allow controlled stimulus-based top-up candidates for the chosen target muscles

## Stimulus accounting boundaries
- Exercise taxonomy (`primaryMuscles`, `secondaryMuscles`, split/pattern metadata) remains classification/filtering input and explainability language input; it is not the canonical hypertrophy contribution math source.
- Canonical hypertrophy contribution math is the weighted `stimulusProfile` model on engine exercises (`src/lib/engine/types.ts`) and must flow through shared helpers in `src/lib/engine/stimulus.ts`:
  - `resolveStimulusProfile()`
  - `getEffectiveStimulusByMuscleId()`
  - `getEffectiveStimulusByMuscle()`
- Effective-set accounting surfaces (`effectiveActual`, planning deficits, session planning contributions, volume compliance) are expected to consume that shared stimulus helper path rather than re-deriving contribution ad hoc.
- Weekly performed-volume read models now share one canonical mesocycle-week adapter in `src/lib/api/weekly-volume.ts` (`loadMesocycleWeekMuscleVolume()`), which returns weighted `effectiveSets` plus raw `directSets`/`indirectSets` as contextual fields. Dashboard rows (`src/lib/api/program.ts`), week-close deficits (`src/lib/api/mesocycle-week-close.ts`), explainability compliance (`src/lib/api/explainability.ts`), and analytics outcome review (`src/lib/api/muscle-outcome-review.ts`) all consume that adapter.
- Outward-facing weekly-volume consumers also share one exposed muscle scope from `src/lib/engine/volume-landmarks.ts`. That exposed scope includes the broader supported muscles (`Forearms`, `Adductors`, `Abductors`, `Lower Back`) and folds internal `Abs` stimulus into external `Core` instead of emitting a separate `Abs` row.
- Transitional fallback policy for missing explicit profiles is centralized in `src/lib/engine/stimulus.ts` and coverage-checked in `src/lib/api/template-session/context-loader.ts` via `validateStimulusProfileCoverage()`.
- Strict fallback enforcement is controlled by `STRICT_STIMULUS_PROFILE_COVERAGE` in `src/lib/api/template-session/context-loader.ts`.
- Coverage reporting command is `npm run report:stimulus-coverage` (`scripts/report-stimulus-profile-coverage.ts`).

## Role and closure guardrails
- Mesocycle exercise roles are anchors for continuity and structure; role-list presence is not a session sufficiency stop condition.
- Session sufficiency remains deficit/constraint outcome-based and now has three explicit planning layers:
  - `standard` inventory for normal session construction
  - anchor supplementation from opportunity-compatible inventory when role fixtures alone are insufficient
  - `closure` inventory for same-session add/expand top-ups when material unresolved deficits remain
- Week-close optional gap-fill is the first explicit `rescue` inventory consumer. Rescue is not globally open; it is a controlled inventory phase selected by the generation path.
- Closure candidate and action diagnostics are persisted in planner diagnostics to keep ranking/filtering decisions auditable from receipts (`src/lib/planner-diagnostics/types.ts`, `src/lib/evidence/session-decision-receipt.ts`).
- Deterministic tie-breaking is required for equivalent-score closure candidates to keep audits/regressions stable (`src/lib/api/template-session/closure-actions.ts`).

## Progression and load assignment
- Progression math is implemented in `src/lib/engine/progression.ts`.
- Load assignment and fallback logic are implemented in `src/lib/engine/apply-loads.ts`.
- Bounded load calibration is centralized in `src/lib/engine/load-calibration.ts`. It classifies equipment reliability, prefers `cable` over `machine` when both are present, scales only cold-start and donor estimate loads, and applies extra confidence scaling only during the first two same-exercise exposures through `src/lib/progression/canonical-progression-input.ts`.
- The same load-calibration policy also supplies bounded progression-promotion guardrails for low-confidence implements: Tier 2/3 equipment down-weights overshoot qualification through an overshoot-only confidence scale, and Tier 3 disables the extra same-exercise catch-up increment. This does not change anchor selection, base increment math, or non-overshoot progression paths.
- Runtime-added exact same-exercise rows have a separate calibration lane. `mapHistory()` keeps runtime-added exercises out of canonical progression/history `exercises`, but records usable performed signal sets under `calibrationExercises` for load prescription only. `src/lib/engine/apply-loads.ts` may use that exact exercise evidence after planned exact history and explicit baselines, but before donor/cold estimates, and stamps `runtime_added_same_exercise_calibration_anchor` so readouts can show lower-trust provenance. This does not count runtime-added work as planned completion, progression truth, ExerciseExposure, seed/runtime replay, planner/materializer input, or workout/save mutation.
- First-session/new-mesocycle exact same-exercise history remains preferred over broad donor history, but `src/lib/engine/apply-loads.ts` downshifts the held anchor when the prior context was materially lower-rep and higher-RPE than the current target. The downshift reuses the same reps/RPE translation used by fallback load migration, keeps the source as history, and stamps a low-confidence caution reason; it does not change seed shape, runtime replay, planner/materializer output, or cross-intent fallback behavior.
- Exact cross-intent same-exercise fallback remains intentionally conservative in `src/lib/engine/apply-loads.ts`, with the default migration discount now set to `0.9` for bounded Week 1 anchoring instead of the older `0.85`. Targeted calibration still applies on top of that default: barbell squat-dominant lower-body lifts, dumbbell pressing, and machine lower-body compounds get a less-discounted translation/cap than the default fallback path, while rows, pulldowns, and calibrated SLDL/RDL-style hinges remain bounded by the same fallback/cap policy rather than opening broader donor pooling.
- Canonical load quantization lives in `src/lib/units/load-quantization.ts`. Dumbbell display/storage flows must use that same 2.5 lb quantization rule through shared UI helpers in `src/lib/ui/load-display.ts`; UI formatting is not allowed to maintain a separate dumbbell snap policy.
- Canonical progression-input assembly is centralized in `src/lib/progression/canonical-progression-input.ts` via `buildCanonicalProgressionEvaluationInput()`. That seam assembles the materially relevant `computeDoubleProgressionDecision()` payload (`workingSetLoad`, `priorSessionCount`, `historyConfidenceScale`, current target reps/RPE, and decision-log `confidenceReasons`) without owning progression math or UI formatting.
- Canonical read-side next-exposure wording is centralized separately in `src/lib/ui/next-exposure-copy.ts`. Surfaces rendering canonical `NextExposureDecision.action` outcomes should consume that formatter instead of inventing local progression-action ladders.
- The live in-session autoreg hint is implemented separately in `src/lib/progression/load-coaching.ts` and called from `src/components/log-workout/useWorkoutSessionFlow.ts`. It is a current-session coaching/explainability seam only.
- Historical training signals are mapped from persisted workouts/logs in `mapHistory()` within `src/lib/api/workout-context.ts`.
- Performed-history filtering (not completed-only filtering) is canonical for load progression and plateau/deload checks via `filterPerformedHistory()` and `isPerformedHistoryEntry()` in `src/lib/engine/history.ts`.
- Effective-reps filtering is enforced at signal derivation: sets logged below `RPE 6` are excluded from modal-load and progression anchoring (data is still persisted).
- Intermediate double-progression decision tree is enforced for load updates (hold at high fatigue; progress load only when reps/RPE thresholds are met; use conservative anchoring under high intra-session load variance).
- Earned progression also has a controlled overshoot lane in `computeDoubleProgressionDecision()` when performed load materially beats prescribed `targetLoad` across enough target-bearing sets. The engine still owns anchor selection; the normal overshoot path stays single-increment, while a bounded catch-up lane can add one extra increment only when exact same-exercise evidence is broad, stable, and still at valid RPE.
- Overshoot promotion is blocked when the current generated target asks for materially more reps at materially easier effort than the prior performed evidence supports. This guard holds the quantized anchor load instead of increasing load; it does not change seed replay, planner/materializer output, equipment increments, or non-overshoot progression paths.
- Repeated intent deviation is detected transiently in `src/lib/progression/canonical-progression-input.ts` from the last three target-bearing progression-history exposures. When repeated materially heavier-than-prescribed, below-floor rep performance is flagged, `computeDoubleProgressionDecision()` suppresses overshoot/catch-up promotion and clamps the next load no higher than the latest deviated exposure's prescribed target-load ceiling. This does not persist drift state or change load-calibration policy.
- Overshoot gating is tiered:
  - standard lane: `modalRpe <= 8.0` with the normal overshoot evidence minimum
  - controlled-hard lane: `modalRpe <= 8.5` only when overshoot coverage is stronger (`>= 75%` of target-bearing sets, minimum `3` when available) and no high-variance trim was required
  - `modalRpe > 8.5` still holds on the overshoot path
- Canonical progression inputs must therefore preserve prescribed `targetLoad` through history/context mapping (`src/lib/api/workout-context.ts`, `src/lib/session-semantics/performed-exercise-semantics.ts`, `src/lib/engine/apply-loads.ts`).
- Hypertrophy main lifts now author uniform working sets in `src/lib/engine/prescription.ts` and receive one resolved working load across those sets in `src/lib/engine/apply-loads.ts`. Main-lift sets carry both the lifecycle-interpolated `targetReps` aim and the effective prescription `targetRepRange`; progression and review should use the range's upper bound for double-progression readiness while keeping `targetReps` as the load-estimation/prefill aim. `computeDoubleProgressionDecision()` in `src/lib/engine/progression.ts` accepts an optional `workingSetLoad` parameter so progression, reps, and effort can describe the same representative working-set object when the caller has already resolved it.
- Progression outlier thresholds and sample-size confidence scaling are centralized in `PROGRESSION_CONFIG` (`src/lib/engine/progression.ts`) and emitted into progression decision logs.
- Bodyweight working sets are canonicalized at write-time to `actualLoad=0` when `targetLoad=0`; `null` is not treated as canonical bodyweight load.
- Scheduled deload load reduction is canonicalized in `src/lib/engine/apply-loads.ts`, not in deload generation. `src/lib/api/template-session/deload-session.ts` leaves `targetLoad` unset, and `applyLoads()` anchors deload load-down to the most recent performed accumulation load for that exercise, then applies the lighter deload prescription with the existing multiplier and canonical quantization. If no accumulation-phase history exists, it falls back to the normal canonical source-load resolver.
- Shared deload defaults are centralized in `src/lib/deload/semantics.ts`. That module is the canonical source for deload phase detection, deload target effort (`RPE 4.5` from `5-6 RIR`), hard-set reduction defaults, decision reduction percent, and progression-history exclusion policy.
- `estimateLoad()` in `src/lib/engine/apply-loads.ts` returns `undefined` (no estimate) for exercises whose equipment list includes `"bodyweight"` when no non-zero load history exists. This prevents phantom load assignments on hybrid bodyweight/machine exercises (e.g., Dip) on their first weighted use.
- Donor-based first-time load estimation must reject invalid donors before scaling. In `src/lib/engine/apply-loads.ts`, donor paths exclude non-finite / `<= 0` donor loads, and they also exclude bodyweight or bodyweight-hybrid donors when the target exercise expects external load. This is a donor-validation guardrail, not a broader estimator retune.
- Bodyweight progression is rep-driven only at `anchorLoad=0` in `computeDoubleProgressionDecision()`; the engine never auto-increments external load from `0` and logs `bodyweight exercise — rep progression only`.
- Empty performed logs are invalid (`LOGGED_EMPTY` is rejected on write); unresolved sets should remain `MISSING` and are treated as unresolved during completion status resolution.
- On first session of a new mesocycle (`accumulationSessionsCompleted=0` or explicit first-session flag), load anchoring history is sourced from accumulation history only: prefer week-4 accumulation, else highest available accumulation week, else any non-deload performed history; deload (`DELOAD`/`ACTIVE_DELOAD`) snapshots are excluded as baseline sources.
- Live cue contract: `getLoadRecommendation()` keeps the `increase | hold | decrease` action shape and evaluates only the current logged set against the next set target. Load-aware copy may use `actualLoad` and `targetLoad` for explanation, but it does not read history, rep-band progression gates, or mesocycle state and must not be treated as canonical next-exposure progression.

## Post-workout data flow
```text
SetLog / logged performance
-> workout save / status resolution
-> deriveSessionSemantics
-> session decision receipt + session semantics consumers
-> post-workout explanation layer
-> next workout generation / canonical progression
```
- `SetLog` is the raw authoritative performed-work source. Set-level facts such as reps, logged RPE, logged load, skipped state, and completion timestamps come from persisted set logs, not from explanation-layer inference.
- Workout save and status resolution are the authoritative completion/state boundary. Performed status, unresolved-set handling, and lifecycle mutation are owned there, not by read-side explanation (`src/app/api/workouts/save/route.ts`, `src/app/api/workouts/save/status-machine.ts`).
- Save-route success payloads now include canonical `workoutStatus`. `mark_completed` remains a requested terminal intent, but the persisted `workoutStatus` returned by save is the authoritative completion truth that clients must render against.
- `deriveSessionSemantics()` is the canonical session-level interpretation bridge. It owns session-level meaning derived from persisted workout fields, including advancing/non-advancing interpretation, weekly-slot consumption, and progression-history eligibility.
- `deriveSessionSemantics()` does not own set-level progression computations such as modal load, anchor load, rep summaries, or effort-classification math. Those remain in canonical progression/history/explainability seams such as `src/lib/engine/progression.ts`, `src/lib/engine/history.ts`, and `src/lib/api/explainability.ts`.
- `selectionMetadata.sessionDecisionReceipt` is the canonical stored generation/evidence context. Read-side consumers should combine that receipt with derived session semantics rather than recreating missing session policy locally.
- `selectionMetadata.workoutStructureState` is the canonical persisted mutation-reconciliation context. It stores the current saved structure summary plus generated-vs-saved reconciliation, and mutation writers own keeping it current.
- Original generation receipt truth and current saved-structure truth are intentionally distinct. Mutation reconciliation must not overwrite `sessionDecisionReceipt` to mimic the mutated workout.
- Post-workout explanation is a read-side interpretation surface. It may explain canonical behavior, but it should not redefine the behavior that generator/progression seams will use for the next exposure.
- App-owned post-session review contracts live in `src/lib/api/post-session-review-contract.ts`, with app-safe evidence DTOs in `src/lib/api/post-session-review-evidence.ts` and pure construction in `src/lib/api/post-session-review-contract-builder.ts`. This seam is read-only evidence over one completed or partial workout: it can summarize set-log performed facts, current workout structure, runtime edit evidence, next-exposure explainability rows, prescription-calibration watch rows, and safe weekly volume impact rows, but it must not mutate workouts/logs, `selectionMetadata.sessionDecisionReceipt`, accepted seed/runtime replay, planner/materializer behavior, progression scoring, weekly-retro output, or persistence.
- Canonical next-exposure progression remains server-side in `src/lib/engine/apply-loads.ts` and `src/lib/engine/progression.ts`.

## Rep target interpretation
- Canonical read-side rep-target interpretation lives in `src/lib/session-semantics/target-evaluation.ts`.
- `resolveTargetRepRange()` is range-first by contract:
  - prefer `targetRepRange`
  - else fall back to `targetRepMin` + `targetRepMax`
  - else fall back to point target `targetReps`
- `evaluateTargetReps()` is the shared read-side helper for comparing actual reps against the effective target. Read-side review/explainability surfaces should not re-derive range vs point-target semantics locally.
- `targetReps` is the suggested aim inside a range, not a substitute for `targetRepRange` / `targetRepMin` / `targetRepMax`. Main hypertrophy lift copy should describe the upper end of the true range instead of synthetic exact bands such as `9-9`.

## Set-state classification
- Canonical read-side set-state interpretation lives in `classifySetLog()` in `src/lib/session-semantics/set-classification.ts`.
- The helper exists to prevent read-side drift around skipped, performed, resolved, and signal-bearing sets.
- Canonical meanings:
  - `isSkipped`: the set was explicitly skipped.
  - `isResolved`: the set is no longer missing; it is either skipped or has any actual logged field.
  - `isPerformed`: the set was actually performed and has meaningful performance evidence (`actualReps` or `actualRpe`) rather than just load-only metadata.
  - `isSignal`: the performed set is eligible as progression signal under the configured RPE floor.
  - `countsTowardVolume`: the set contributes to performed-volume accounting; today this is aligned with `isPerformed`.
- Read-side consumers that need set interpretation should prefer `classifySetLog()` instead of open-coding skip/performed/resolved predicates.

## Periodization and readiness
- Macro/meso/block logic lives in `src/lib/engine/periodization`.
- Readiness, fatigue scoring, and autoregulation logic lives in `src/lib/engine/readiness`.
- API orchestration for readiness and periodization endpoints lives in `src/lib/api/readiness.ts` and `src/lib/api/periodization.ts`.
- Explicit pre-session readiness snapshot preparation lives in `src/lib/api/pre-session-readiness-producer.ts`. It composes existing next-session, generated-preview, projected-week, dose-guidance, and the app-owned `src/lib/api/pre-session-readiness-contract-builder.ts` seam into a typed `PreSessionReadinessContract`, saves only `PreSessionReadinessSnapshot`, and does not change generation scoring, planner/materializer behavior, workouts/logs, accepted seed shape, or runtime replay.
- Generation-facing phase/block resolution now lives in `src/lib/api/generation-phase-block-context.ts` and is loaded by `src/lib/api/template-session/context-loader.ts`. This is the canonical seam where persisted block definitions become generation/runtime `cycleContext`, including optional `blockDurationWeeks` for receipt-backed read-side consumers.
- Session-decision ownership is receipt-first. The canonical flow is defined once in `docs/01_ARCHITECTURE.md`; domain logic here assumes session-level cycle/readiness context is carried only by `selectionMetadata.sessionDecisionReceipt` and parsed by `src/lib/evidence/session-decision-receipt.ts`.
- Mutation truth alignment is persisted-first, not heuristic-first. Read-side summary/explainability surfaces should use `selectionMetadata.workoutStructureState` when deciding whether generated receipt context is still current or must be labeled as original-plan context.
- Default readiness autoregulation policy is conservative down-regulation only (`allowUpRegulation=false`) unless explicitly overridden by policy input (`src/lib/engine/readiness/types.ts`, `src/lib/engine/readiness/autoregulate.ts`).

## Evidence and rule guardrails
- Reactive deload logic in `shouldDeload()` is evidence-gated and requires stronger signals (sustained low-readiness streak or repeated main-lift plateau evidence) rather than flat total-session-rep plateaus alone (`src/lib/engine/progression.ts`, `src/lib/engine/progression.correctness.test.ts`).
- Intent alignment enforcement is diagnostics-first by default (`minRatio=0` unless explicitly requested), with explicit intent diagnostics returned on selection output (`src/lib/api/template-session/intent-filters.ts`).
- Post-hoc optimizer stretch swapping is removed; final selection remains optimizer-owned (`src/lib/engine/selection-v2/optimizer.evidence.test.ts`).

## Workout status semantics
- The split exists to separate adaptation signals from advancement control: partially performed work should inform future load/selection, while schedule/phase advancement remains a stricter completion event.
- Performed-signal consumers use `COMPLETED` + `PARTIAL` via `PERFORMED_WORKOUT_STATUSES` in `src/lib/workout-status.ts`.
- Program advancement remains `COMPLETED` only via `ADVANCEMENT_WORKOUT_STATUSES` in `src/lib/workout-status.ts`.
- Mesocycle lifecycle progression is driven by first transition into performed status (`COMPLETED` or `PARTIAL`). Lifecycle counters (`accumulationSessionsCompleted`, `deloadSessionsCompleted`) are incremented atomically inside the save-workout transaction in `src/app/api/workouts/save/route.ts`; status/action resolution is isolated in `src/app/api/workouts/save/status-machine.ts`; `transitionMesocycleState()` in the lifecycle facade (`src/lib/api/mesocycle-lifecycle.ts`) applies state transitions when thresholds are reached.
- Canonical mesocycle progression counters are `accumulationSessionsCompleted` and `deloadSessionsCompleted` (not `completedSessions`) and drive lifecycle week/phase derivation.

## Session semantics model
- Session semantics are split intentionally between write-side lifecycle contract and read-side interpretation.
- Write-side lifecycle contract remains `Workout.advancesSplit`. Save/lifecycle mutation code should continue treating `advancesSplit !== false` as the only advancement gate (`src/app/api/workouts/save/lifecycle-contract.ts`, `src/app/api/workouts/save/route.ts`).
- Structural mutation writers are also responsible for canonical reconciliation bookkeeping: when the saved workout structure changes, they must update `selectionMetadata.workoutStructureState` and bump `Workout.revision`.
- Read-side policy is now centralized in `deriveSessionSemantics()` (`src/lib/session-semantics/derive-session-semantics.ts`). Readers should derive behavior from persisted fields rather than re-authoring ad hoc checks across progression, next-session, and planning paths.
- Current derived kinds are `advancing`, `gap_fill`, `supplemental`, and `non_advancing_generic`.
- The helper derives those semantics from existing persisted/runtime fields: `advancesSplit`, `selectionMode`, `sessionIntent`, `selectionMetadata`, and optional `templateId`.
- Current read-side policies centralized there are:
  - lifecycle-advancement interpretation for compatibility reads (`advancesLifecycle`)
  - weekly required-slot consumption (`consumesWeeklyScheduleIntent`)
  - progression anchor / progression explainability eligibility (`countsTowardProgressionHistory`)
  - canonical performance-history eligibility (`countsTowardPerformanceHistory`)
  - progression-anchor update eligibility (`updatesProgressionAnchor`)
  - unique-intent subtraction eligibility for remaining-week and next-session reads (`eligibleForUniqueIntentSubtraction`)
- Current derived policy intentionally preserves existing behavior:
  - strict supplemental sessions are non-advancing and progression-ineligible
  - strict gap-fill sessions are non-advancing but progression-eligible
  - scheduled deload sessions still count for compliance, recovery/recent stimulus, and weekly volume, but they are excluded from progression history, progression anchors, and canonical performance-history reads
  - generic non-advancing sessions remain progression-eligible unless a stricter classifier excludes them
  - `null` / `undefined` `advancesSplit` still default to advancing for backward compatibility
- A persisted `sessionKind` enum was intentionally not added. The current system still has more semantic cases than a stable enum captures, including non-advancing but progression-eligible sessions. Adding an enum now would freeze an incomplete taxonomy into schema and migration contracts before runtime policy has settled.

## Explainability guardrails
- Explainability/read-side layers should consume derived session semantics or canonical decision outputs. They should not independently recompute progression-relevant session meaning unless there is a strong reason and the new seam is documented as canonical first.
- Common drift risk: read-side explanation can accidentally describe prior prescription logic or inferred progression in a way that does not match canonical next-exposure behavior.
- Guardrail: when explanation needs to talk about session-level behavior, it should prefer:
  - persisted `SetLog` performance data for raw facts
  - save/status outputs for performed-state truth
  - `deriveSessionSemantics()` for session-level interpretation
  - canonical progression outputs/decision logs for next-exposure load behavior
- When explanation needs to phrase canonical next-exposure action, it should route that wording through `src/lib/ui/next-exposure-copy.ts`. Heuristic or advisory surfaces must not invent stronger progression wording for the same decision shape.
- Avoid local fallbacks that reinterpret session policy inside explainability copy or UI helpers. If a new explanation concept truly requires new canonical semantics, add that seam first rather than encoding it only in read-side copy.
- Future seam rule: if the app ever needs a new canonical set-level post-workout interpretation layer, document and introduce it as a seam separate from `deriveSessionSemantics()`. Do not silently expand `deriveSessionSemantics()` from session-level policy into set-level progression math ownership.

## Optional session policy
- Optional sessions reuse canonical INTENT generation (`intent=body_part`) and do not introduce a separate optimizer path (`src/lib/api/template-session.ts`).
- Pending week-close context is canonical for gap-fill week anchoring. `generateSessionFromIntent()` now passes `optionalGapFillContext.targetWeek` into `loadMappedGenerationContext()` so generation resolves the anchored `weekInMeso` from the pending week-close row, then derives block-relative `weekInBlock` from the active `TrainingBlock` when available (`src/lib/api/template-session.ts`, `src/lib/api/template-session/context-loader.ts`, `src/lib/api/generation-phase-block-context.ts`).
- Optional gap-fill now uses the explicit `rescue` inventory layer from `SessionOpportunityDefinition`. This is the current bridge between week-close deficit snapshots and controlled rescue access without rewriting the planner into a long-horizon system.
- Week-close truth is dual-state rather than single-resolution: `workflowState` says whether the optional gap-fill workflow is still pending, and `deficitState` says whether the weekly deficit is `OPEN`, `PARTIAL`, or `CLOSED`. Optional gap-fill completion can therefore leave `workflowState=COMPLETED` while `deficitState=PARTIAL`.
- Supplemental deficit sessions reuse the same BODY_PART INTENT generation route, but they are user-invoked rather than week-close-driven. The allowed UI path is `IntentWorkoutCard -> POST /api/workouts/generate-from-intent -> POST /api/workouts/save`, with backend-owned receipt stamping (`src/components/IntentWorkoutCard.tsx`, `src/app/api/workouts/generate-from-intent/route.ts`).
- Gap-fill policy read model is surfaced by `loadHomeProgramSupport()` (`src/lib/api/program.ts`) with fields:
  - `requiredSessionsPerWeek`
  - `maxOptionalGapFillSessionsPerWeek`
  - `maxGeneratedHardSets`
  - `maxGeneratedExercises`
- Current default policy: required sessions = active mesocycle `sessionsPerWeek` (min 1), max optional sessions/week = 1, max hard sets = 12, max exercises = 4.
- Override precedence is policy-first and split-agnostic: policy values are resolved centrally in `program.ts`; generation/save do not fork by split type.
- Strict classification for optional sessions uses the shared triplet predicate in `src/lib/gap-fill/classifier.ts`.
- Strict classification for supplemental deficit sessions uses the shared triplet predicate in `src/lib/session-semantics/supplemental-classifier.ts`.
- Read-side interpretation for those strict classifiers is centralized in `deriveSessionSemantics()` rather than scattered boolean checks in each consumer.
- Canonical optional-session receipt/metadata stamping is shared in `src/lib/ui/selection-metadata.ts`; generation and UI callers attach `weekCloseId`, `targetMuscles`, and the `optional_gap_fill` exception through `attachOptionalGapFillMetadata()` instead of duplicating route/component-local mutation logic.
- Canonical supplemental receipt/metadata stamping is also shared in `src/lib/ui/selection-metadata.ts`; generation attaches `targetMuscles` and the `supplemental_deficit_session` exception through `attachSupplementalSessionMetadata()`, and the client persists the returned canonical metadata unchanged.
- Canonical closeout receipt/metadata stamping is shared in `src/lib/ui/selection-metadata.ts`; closeout writers attach `weekCloseId` and the `closeout_session` exception through `attachCloseoutSessionMetadata()`, which also strips any receipt `sessionSlot` so closeout sessions stay outside canonical slot identity.
- Week-close owns closeout creation. `createCloseoutSessionForWeek()` in `src/lib/api/mesocycle-week-close.ts` is the canonical write path for closeout scaffold workouts: it validates the user-owned week-close row against the current active accumulation week, rejects deload or duplicate closeouts, stamps canonical receipt metadata, and persists the workout as slotless/non-advancing without routing through generation.
- Supplemental deficit sessions count toward weekly volume and recovery/recent stimulus, but they are excluded from progression anchors and progression explainability evidence through the derived session-semantics policy (`src/lib/session-semantics/derive-session-semantics.ts`, `src/lib/api/workout-context.ts`, `src/lib/progression/progression-eligibility.ts`, `src/lib/api/explainability.ts`).
- Supplemental deficit sessions are non-advancing by contract: save forces `advancesSplit=false` for strict supplemental classification and blocks split advancement even if the incoming payload requests otherwise (`src/app/api/workouts/save/route.ts`).
- Closeout sessions are also receipt-first and non-advancing by contract: `deriveSessionSemantics()` classifies the `closeout_session` marker as weekly-volume-valid but excluded from progression and performance-history anchoring, and save strips both legacy/top-level and receipt slot identity plus forces `advancesSplit=false` even if the incoming payload requests otherwise (`src/lib/session-semantics/closeout-classifier.ts`, `src/lib/session-semantics/derive-session-semantics.ts`, `src/app/api/workouts/save/route.ts`).

## Supplemental Deficit Sessions
- Purpose: add a small targeted stimulus patch mid-mesocycle without mutating split advancement or corrupting progression-anchor history.
- User intent: the user invokes a `BODY_PART` intent session to patch a weekly deficit, add weak-point work, or add extra recoverable stimulus outside the advancing split schedule.
- Strict classification rules:
  - `selectionMode=INTENT`
  - `sessionIntent=BODY_PART`
  - receipt exception marker `supplemental_deficit_session`
  - persisted `advancesSplit=false`
- Lifecycle semantics:
  - counts toward weekly volume
  - counts toward recovery and recent-stimulus accounting
  - does not advance split lifecycle
  - does not consume a required weekly schedule slot
- Progression isolation:
  - the session remains part of performed history for volume/recovery/readiness consumers
  - the session is excluded from progression history and explainability progression evidence through `isProgressionEligibleWorkout()` and `filterProgressionHistory()`
  - performed history and progression history are intentionally different views over the same persisted workouts
- Generation profile:
  - generation still uses the normal BODY_PART planner pipeline, but route orchestration enables `supplementalPlannerProfile`
  - single-target supplemental sessions narrow to `1-3` exercises
  - multi-target supplemental sessions narrow to `2-4` exercises
  - route defaults clamp uncapped requests to `maxGeneratedExercises=4` and `maxGeneratedHardSets=8`
  - selection prefers target-primary, non-main-lift, lower-fatigue exercises
  - fallback reopens the broader BODY_PART pool only when the accessory-first preferred pool cannot cover the requested targets or cannot satisfy the supplemental minimum session floor
  - continuity carryover and continuity minimum set floors are disabled for this profile
  - multi-target sessions apply a soft per-target floor so each requested target gets one primary-target coverage slot when feasible
  - main-lift slotting is suppressed by default (`maxMainLifts=0`)
  - deficit-aware supplemental set caps narrow the dose:
    - remaining deficit `<= 1.5` -> `1` set
    - remaining deficit `<= 3.5` -> `2` sets
    - remaining deficit `> 3.5` -> `3` sets
  - main-lift-typed fallback work remains capped below normal BODY_PART prescription even when fallback is necessary

## Week-close deficit handling
- Week close is a lifecycle checkpoint, not a normal-flow workout generator.
- At a completed scheduled accumulation-week boundary, `evaluateWeekCloseAtBoundary()` records the canonical weighted weekly deficit snapshot and resolves the workflow immediately. Target deficits are stored as review evidence with `resolution=AUTO_DISMISSED`; they do not create `PENDING_OPTIONAL_GAP_FILL` rows and do not block rollover.
- `resolution=NO_GAP_FILL_NEEDED` means no qualifying target deficit remained. `resolution=AUTO_DISMISSED` can still carry `deficitState=PARTIAL`, which means targets were missed but the lifecycle workflow is complete.
- Week rollover may still fail closed for real lifecycle/data blockers, including incomplete required advancing workouts, invalid lifecycle state, invalid sequencing, pending linked optional closeout work, or missing state needed to enter deload/next week safely.
- Strict optional gap-fill and supplemental classifiers remain for historical/manual non-advancing sessions. They preserve accounting semantics but are not automatically created by week close.

## Mesocycle lifecycle service
- Facade: `src/lib/api/mesocycle-lifecycle.ts`.
- Math module: `src/lib/api/mesocycle-lifecycle-math.ts` (week derivation, RIR targets, lifecycle volume targets).
- State module: `src/lib/api/mesocycle-lifecycle-state.ts` (state transitions + next-mesocycle initialization).
- `transitionMesocycleState(mesocycleId)`: transitions state (`ACTIVE_ACCUMULATION` -> `ACTIVE_DELOAD` -> `AWAITING_HANDOFF`) and freezes handoff artifacts when deload is complete. It no longer creates the successor mesocycle automatically.
- `finishDeloadEarly()` (`src/lib/api/mesocycle-lifecycle-state.ts`): explicit user-invoked deload closeout for `ACTIVE_DELOAD` mesocycles. It marks only unperformed remaining deload workouts as `SKIPPED`, rejects workouts with performed logs, preserves `deloadSessionsCompleted` as performed truth, and enters the same handoff artifact seam as normal deload completion.
- `getCurrentMesoWeek(mesocycle)`: derives effective lifecycle week from `state`, `durationWeeks`, `accumulationSessionsCompleted`, and `sessionsPerWeek`. Accumulation weeks are `durationWeeks - 1`; the final week is deload.
- `loadNextWorkoutContext()` (`src/lib/api/next-session.ts`) must fail closed when an `ACTIVE_ACCUMULATION` mesocycle has already reached the final accumulation threshold and a linked pending closeout/week-close workflow is still actionable. Normal target deficits no longer create that blocker; when required scheduled accumulation sessions are complete and no real blocker exists, lifecycle transition advances into `ACTIVE_DELOAD` instead of modulo-wrapping another accumulation session.
- `getWeeklyVolumeTarget(mesocycle, muscleGroup, week, options?)`: returns lifecycle week-specific target sets from landmarks plus the canonical weekly target profile. Landmark values (MEV/MAV/MRV) are sourced from `VOLUME_LANDMARKS` in `src/lib/engine/volume-landmarks.ts`. The default canonical block-aware path is `mesocycle.blocks` when ordered block coverage is present; `options.blockContext` remains the higher-precedence seam for generation when week anchoring or forced context must override the raw mesocycle row. When block data is unavailable or incomplete, it falls back to duration-only lifecycle interpolation.
- Analytics outcome review for the active mesocycle week is a read-only comparison layer built from `getWeeklyVolumeTarget(...)` + `loadMesocycleWeekMuscleVolume(...)`. It does not own alternate stimulus math or alternate target interpolation (`src/lib/api/muscle-outcome-review.ts`).
- Weekly target placement is centralized in `src/lib/engine/volume-targets.ts` via `buildWeeklyVolumeTargetProfile()` + `interpolateWeeklyVolumeTarget()`. Default 4/5-week behavior is preserved under the default block layouts, but non-default block types can now materially shape target placement:
  - `accumulation`: rising weekly targets toward peak productive volume
  - `intensification`: continued but moderated rise/plateau based on block config
  - `realization`: intentional reduction from prior peak volume while intensity rises
  - `deload`: explicit recovery-oriented reduction from peak accumulation weekly volume target; the scheduled deload session transform separately keeps exercises/reps stable, cuts hard sets roughly in half, and lets the canonical load engine apply the lighter load prescription
- Realization reduction is explicit policy in `src/lib/engine/volume-targets.ts`, not a hidden post-hoc override. The current taper step is `-0.5` base realization reduction plus `volumeTarget` and `intensityBias` weights; for the default low-volume, strength-biased realization week that yields a `-1.15` progress step and therefore a `0.6167` week fraction relative to the prior productive peak.
- When phase/block context is supplied, lifecycle prescription helpers now consume real block type and block-relative week:
  - `getRirTarget(..., phaseBlockContext?)`
  - `getLifecycleSetTargets(..., phaseBlockContext?)`
  - `buildLifecyclePeriodization({ ..., phaseBlockContext })`
  This preserves current default 4/5/6-week behavior under the existing default block definitions while making generation materially block-aware.
- Handoff semantics are now explicit and split across dedicated seams:
  - `enterMesocycleHandoffInTransaction()` freezes `handoffSummaryJson` and seeds editable `nextSeedDraftJson`
  - `loadMesocycleReview()` reads frozen handoff facts plus live derived closeout metrics
  - `loadMesocycleSetupFromPrisma()` reads the mutable setup draft against the frozen recommendation
  - `acceptMesocycleHandoff()` is the only canonical path that creates the successor mesocycle; expensive deterministic projection is prepared before the short persistence transaction
- Mesocycle genesis recommendation ownership remains in `src/lib/api/mesocycle-genesis-policy.ts`, but live evidence normalization now happens once in `enterMesocycleHandoffInTransaction()` through a single `GenesisPolicyContext`. That context carries policy-relevant source profile, constraints, normalized split/frequency preferences, source slot topology, mesocycle closeout/adherence evidence, latest readiness, and receipt-backed carry-forward candidate evidence rather than raw Prisma rows.
- Phase 1 genesis explainability is branch-accurate by contract:
  - structure recommendation reason codes now reflect the actual preferred-vs-default branch that fired
  - preferred split/frequency signals from current handoff constraints are honored when present and still capped by hard availability constraints
  - when current handoff constraints provide an explicit compatible `weeklySchedule`, genesis uses that authored order for the recommended ordered-flexible slot sequence and records `explicit_weekly_schedule_order_honored`
  - carry-forward branch outputs now persist `reasonCodes` plus `signalQuality`, where `high` is reserved for concrete evidence-driven branches and `medium` marks fallback/default behavior
  - accessory continuity keeps are intentionally capped by authored slot capacity during genesis recommendation so seed drafts and slot-plan projection do not preserve every eligible accessory by default; overflow accessories rotate with `accessory_rotation_slot_capacity_cap`
- Acceptance semantics are transactionally strict:
  - sanitize the draft against the frozen recommendation envelope
  - reject `keep` carry-forward selections whose original `sessionIntent` no longer exists after split/session edits
  - create the successor mesocycle with reset lifecycle counters
  - persist canonical `slotSequenceJson` from the accepted ordered-flexible slot sequence
  - persist `slotPlanSeedJson` from the final repaired shared raw handoff slot-plan projection when that projection succeeds without blocking support-floor failures
  - carry forward only `keep` selections
  - update `Constraints.daysPerWeek`, `splitType`, and `weeklySchedule`
  - mark the source mesocycle `COMPLETED`
- `slotSequenceJson` is now the canonical runtime session-order contract for accepted mesocycles. Runtime sequencing, remaining-week planning, UI labeling, and explainability should prefer `slotId + intent`; `weeklySchedule` subtraction remains compatibility-only for legacy mesocycles without persisted slot identity.
- `slotPlanSeedJson` is now the canonical runtime composition source for seeded mesocycles. `generateSessionFromMappedContext()` and `generateDeloadSessionFromIntentContext()` resolve the current slot from persisted runtime slot sequencing, then compose only from `slotPlanSeedJson` for seeded supported intents; legacy intent/role reselection remains fallback-only for unseeded mesocycles and unsupported paths such as `body_part`. New accepted seeds store each exercise's identity, role, and final projected `setCount`, where `setCount` means planned hard sets for that exercise in that slot. Seeded accumulation runtime passes those counts as explicit set-count overrides so projection, seed, and runtime remain set-identical. Runtime previews, generated audit snapshots, projected summaries, and saved-row payload builders must flatten generated working exercises by `WorkoutExercise.orderIndex` instead of `mainLifts` before `accessories` so seeded order remains executable planned truth even when roles interleave. Identity-only seed exercises without `setCount` are legacy-compatible only and warn before using the previous seeded prescription fallback. The seed contract may also carry optional `acceptedPlannerIntent` metadata when it matches the seed-safe V2 accepted planner intent DTO, but runtime accumulation and deload replay ignore it; `slots[].exercises[{ exerciseId, role, setCount }]` remains the only executable seed truth. Active reseed replacements intentionally omit that metadata because reseed is an executable slot-plan upgrade, not an accepted-planner provenance write path.
- Stage C supersedes the earlier Stage B-only materializer wording above by extending the guarded pure-materializer `laneSelectionIntent v0` consumption set to include calf direct support (`calves`), side-delt direct (`side_delt_isolation`), triceps direct (`triceps`), rear-delt direct (`rear_delt`), `row_support`, and `quad_isolation`. This remains dry-run-only V2 materialization behavior: seed shape, runtime replay, receipts, DB writes, non-V2 generation, and acceptance thresholds do not consume lane intent.
- Priority 5 lane-intent hardening starts with explicit, non-consumed v0 intent for `upper_b:chest_second_exposure`: the pure selection plan now records press-or-fly chest support intent, preferred press bias, direct Chest stimulus, stability/fatigue preferences, and variation preference, but `consumedByMaterializer=false` keeps dry-run selection, production generation, seed serialization, runtime replay, receipts, DB writes, and acceptance thresholds unchanged until a separate measured behavior trial promotes consumption.
- V2 materializer user-preference handling is limited to existing `UserPreference.favoriteExerciseIds` as a pre-accept ranking signal for safe chest and squat anchor candidates. It can promote a favorite only after class compatibility, directness, duplicate, and anchor-quality filters pass; it must not change seed shape, runtime replay, receipts, DB write gates, or acceptance thresholds.
- Block-aware prescription semantics are now authored in one shared seam: `src/lib/engine/periodization/block-prescription-intent.ts`.
  - Inputs: `blockType`, `weekInBlock`, `blockDurationWeeks`, `isDeload`
  - Outputs: canonical `rirTarget`, `setTargets`, `setMultiplier`, plus compatibility `modifiers`
  - Lifecycle math consumes that seam for `getRirTarget(...)`, `getLifecycleSetTargets(...)`, and `buildLifecyclePeriodization(...)`
  - Legacy bridge code in `src/lib/engine/periodization/block-config.ts` now reads the same intent instead of re-authoring separate block RIR/intensity/rest policy
- Weekly volume targeting now uses the same canonical seam across generation and read models. `src/lib/api/template-session/context-loader.ts` resolves `phaseBlockContext`, then materializes `lifecycleVolumeTargets` through `getWeeklyVolumeTarget(..., { blockContext })` before remaining-week planning, selection, closure, and rescue. Read-side consumers such as dashboard rows, muscle-outcome review, week-close deficits, and explainability compliance route through that same helper using `mesocycle.blocks` directly.
- Current landmark table includes the weighted-model Biceps retune in `src/lib/engine/volume-landmarks.ts` (`Biceps: MV 6, MEV 6, MAV 14, MRV 22, SRA 36`) and is consumed unchanged by planner targeting, dashboard rows, week-close deficits, and explainability compliance.
- Muscle target priority tiers are explicit display/audit semantics in `src/lib/engine/volume-landmarks.ts`, not lifecycle MEV or generator-scoring changes. `getMuscleTargetSemantics()` classifies primary drivers, support targets, secondary targets, and implicit targets with additive `targetTier`, `generationWeight`, `warningSeverity`, `canBlockOrDriveRepair`, and `dashboardGroup` fields while preserving existing `targetKind` compatibility. Core, Forearms, Adductors, Abductors, and Lower Back remain secondary soft readouts with display ranges and `VOLUME_LANDMARKS.mev=0`. Front Delts remain in `VOLUME_LANDMARKS` and stimulus accounting, but are classified as implicit and hidden from default dashboard/projection rows unless actual historical/current volume exists or debug audit output includes implicit rows. Stage 1 is classification-only: generation, week-close obligations, protected-floor repair, Accessory Lane, programQuality shaping, accept gating, and runtime seed replay continue to consume existing lifecycle targets and do not use `generationWeight`.
- Pull musculature landmarks are split (`lats`, `upper_back`) and rear-delt landmarks are reduced to evidence-aligned defaults (`rear_delts: MEV 4, MAV 12`; `lats: MEV 8, MAV 16`; `upper_back: MEV 6, MAV 14`).
- `getRirTarget(mesocycle, week, phaseBlockContext?)`: returns lifecycle week/state-specific RIR bands, including deload targets. Without block context, default hypertrophy bands remain duration-aware: 4-week total = `3-4 -> 2-3 -> 1-2 -> deload`; 5-week total = `3-4 -> 2-3 -> 1-2 -> 0-1 -> deload`; 6-week total = `3-4 -> 2-3 -> 2 -> 1-2 -> 0-1 -> deload`.
- Prescription ownership is therefore split intentionally, not accidentally:
  - Weekly volume target shape remains owned by `src/lib/engine/volume-targets.ts`
  - Weekly effort/set intent remains owned by `src/lib/engine/periodization/block-prescription-intent.ts`
  - Generator/prescription consumers should read those seams, not reinterpret block policy locally
- Direct next-mesocycle initialization is intentionally fenced behind the explicit handoff contract. Callers must not bypass acceptance by creating successors directly from lifecycle state helpers.

## Deload generation path
- Deload generation has a separate pipeline in `src/lib/api/template-session/deload-session.ts`.
- Route hard gate:
  - `POST /api/workouts/generate-from-intent` (`src/app/api/workouts/generate-from-intent/route.ts`) routes to `generateDeloadSessionFromIntent()` when active mesocycle state is `ACTIVE_DELOAD`.
  - `POST /api/workouts/generate-from-template` (`src/app/api/workouts/generate-from-template/route.ts`) routes to `generateDeloadSessionFromTemplate()` when active mesocycle state is `ACTIVE_DELOAD`.
- During `ACTIVE_DELOAD`, normal accumulation generation paths are unreachable from these routes.
- Canonical scheduled deload contract:
  - for seeded mesocycles, take the exercise list from persisted `slotPlanSeedJson`; for unseeded mesocycles, keep the existing accumulation-history continuity fallback
  - reduce hard sets roughly 50% with floor safeguards (`1 -> 1`, `2 -> 1`, `3-4 -> 2`, `5-6 -> 3`)
  - keep reps stable for movement continuity
  - leave `targetLoad` unset in generation, then let `src/lib/engine/apply-loads.ts` apply the canonical lighter deload load
  - target low-fatigue effort through canonical deload targeting (`5-6 RIR`, approximately `RPE 4.5`)
  - user-facing receipts/explanations should describe lighter loads plus reduced volume for recovery, not promise a fixed percentage reduction
  - deload does not count toward progression history and does not reset progress; next block re-anchors from accumulation work rather than deload performance
  - count toward compliance, recent stimulus, and weekly volume, while remaining excluded from progression anchors and canonical performance-history reads

## Explainability
- Explainability domain modules are in `src/lib/engine/explainability`.
- API explainability facade is `src/lib/api/explainability.ts`, split into `src/lib/api/explainability/query.ts` (read/query) and `src/lib/api/explainability/assembly.ts` (response assembly/scoring).
- Explanation endpoint is `src/app/api/workouts/[id]/explanation/route.ts`.
- Explainability seam ownership is intentionally split:
  - `query.ts`: persisted workout/history/evidence loading
  - `assembly.ts`: response assembly, confidence framing, and presentation-ready summarization
  - `deriveSessionSemantics()`: canonical session-level interpretation for read-side consumers
  - `src/lib/engine/history.ts` + `src/lib/engine/progression.ts`: canonical set-level progression and anchoring behavior
  - `selectionMetadata.sessionDecisionReceipt`: stored generation/evidence context
- `src/lib/api/explainability.ts` should remain a facade over those seams. It should not become an alternate owner of session semantics or next-exposure progression policy.
- Workout explanations include per-exercise progression receipts (`WorkoutExplanation.progressionReceipts` in `src/lib/engine/explainability/types.ts`), derived from performed history and current prescription in `src/lib/api/explainability.ts`.
- Workout explanations also expose per-exercise `nextExposureDecisions` as a read model. These must consume performed semantics from `src/lib/session-semantics/performed-exercise-semantics.ts` and route canonical decision-input assembly through `buildCanonicalProgressionEvaluationInput()` before calling `computeDoubleProgressionDecision()`, so post-workout interpretation cannot drift from the next canonical load decision.
- Session context now includes cycle provenance and readiness availability labels (`SessionContext.cycleSource`, `ReadinessStatus.availability`, `ReadinessStatus.label`) in `src/lib/engine/explainability/types.ts`, produced in `src/lib/engine/explainability/session-context.ts`.
- Explainability is strictly receipt-first: it reads session-level cycle/readiness context only from `selectionMetadata.sessionDecisionReceipt`, and missing canonical receipt means missing session-level evidence (`src/lib/evidence/session-decision-receipt.ts`, `src/lib/api/explainability.ts`, `src/lib/ui/explainability.ts`). When canonical receipt cycle context includes `weekInBlock` and `blockDurationWeeks`, read-side summary/explainability copy should prefer block-relative semantics over mesocycle-relative wording.
- Progression receipts only use recent performed evidence (42-day recency window) when loading `lastPerformed` in `loadLatestPerformedSetSummary()` within `src/lib/api/explainability.ts`.
- Progression receipts include a decision log summarizing which load-progression rule path fired and why.
- Overshoot-based receipts must surface whether `path_5_overshoot` earned the increase or why it was blocked (`effort`, `coverage`, or `variance`) so read-side explanation stays aligned with canonical progression.
- Explainability renders per-exercise progression decision logs in the Evidence tab under `Progression Logic` when logs are available.
- Workout explanations include per-muscle weekly volume compliance (`WorkoutExplanation.volumeCompliance` in `src/lib/engine/explainability/types.ts`), computed by `computeVolumeCompliance()` in `src/lib/api/explainability.ts`. Per-muscle compliance is annotated with `VolumeComplianceStatus` — one of `OVER_MAV | AT_MAV | APPROACHING_MAV | OVER_TARGET | ON_TARGET | APPROACHING_TARGET | UNDER_MEV` — and carries projected weekly totals against week-specific targets.

## Session Composition Constraints
- Canonical session composition caps:
  - `minExercises=3`
  - `maxExercises=6`
  - `maxDirectSetsPerMuscle=12`
- These caps are represented by `SESSION_CAPS` in `src/lib/api/template-session/selection-adapter.ts` and must remain aligned with selection-v2 enforcement comments/rules.

## MANUAL Session Contract
- MANUAL bypasses:
  - Mesocycle role continuity enforcement
  - Lifecycle RIR band prescription
  - Intent/beam exercise-selection logic
- MANUAL still enforces:
  - Lifecycle counter advancement when workout enters performed status
  - Performed-history inclusion for progression (with confidence scaling/discounting)
  - Set-log persistence and audit trail semantics
- MANUAL anomaly handling during progression-context ingestion:
  - Uniform-RPE sessions (`variance=0`) flagged as synthetic
  - Modal load below 50% of most recent INTENT modal load for same exercise flagged as implausible regression
  - `RPE=10` on >50% of sets flagged as unsustainable effort
  - Anomalous MANUAL entries remain included but are downgraded to confidence `0.3`
